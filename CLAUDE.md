# CLAUDE.md — 강제 지침

> 프로젝트: **QUARANTINE No.3 (제3격리병원)**
> 이 파일은 이 저장소에서 작업하는 **모든 AI 세션의 최우선 규칙**이다.
> 세션 시작 시 이 파일과 `PROGRESS.md`를 먼저 읽는다. 사용자 지시와 충돌할 때만 사용자 지시가 우선한다.

---

## 0. 이 프로젝트의 전제 (규칙의 근거)

- 개발자는 **코드를 읽지 못한다.** → 모든 변경은 **화면에서 눈으로 확인 가능**해야 한다.
- 제출 마감 **1주**, 1인 개발. → 완성도보다 **완주**가 우선이다.
- 리뷰어가 없다. → **이 규칙이 리뷰어를 대신한다.**

---

## 1. 절대 규칙 — 위반하면 되돌린다

1. **수치를 코드에 직접 쓰지 않는다.**
   속도·체력·데미지·거리·시간 등 모든 밸런스 값은 `src/config/balance.js`,
   무기는 `src/config/weapons.js` 에만 존재한다. 다른 파일에 매직넘버를 넣지 않는다.

2. **한 번의 요청 = 화면에서 확인 가능한 변화 하나.**
   여러 기능을 한 번에 구현하지 않는다. "리팩터링", "정리", "시스템 개선" 같은
   **화면에 안 보이는 작업은 사용자가 명시적으로 요청할 때만** 한다.

3. **되는 상태가 나오면 즉시 커밋한다.** 메시지는 한글 한 줄. (`git commit -m "손전등 배터리 추가"`)

4. **파일을 새로 만들기 전에 기존 파일을 먼저 찾는다.** 같은 역할의 파일을 두 개 만들지 않는다.

5. **폴더 구조를 바꾸지 않는다.** 아래 §4 구조가 계약이다. 새 파일은 정해진 위치에만 만든다.

6. **외부 CDN·원격 URL 금지.** 의존성은 npm, 에셋은 전부 `public/assets/` 안에.

7. **대규모 재작성 금지.** 기존 파일을 통째로 갈아엎지 않는다. 부분 수정(Edit)만 한다.
   파일 하나가 **250줄을 넘으면** 사용자에게 알리고 분할 여부를 묻는다.

8. **마감 D-2 이후에는 구조를 건드리지 않는다.** 값 조정과 에셋 교체만 허용.

---

## 2. 콘텐츠 규칙 — 법적/윤리 리스크 차단

- **실존 브랜드·기관·인물의 이름, 로고, 사이니지를 넣지 않는다.**
  병원명은 항상 가상: `제3격리병원 / QUARANTINE FACILITY No.3`.
  간판·포스터·서류 텍스처에 실제 제약사, 병원, 지자체명이 들어가면 안 된다.
- AI 이미지 생성 프롬프트에는 항상 `no real-world logos, no brand names, fictional signage` 를 포함한다.
- 고어 표현은 **분위기 중심**으로. 과도한 신체 훼손 묘사를 목표로 하지 않는다.
  공포는 **어둠·소리·정적**으로 만든다. (§5 참조)
- 사용한 AI 툴과 라이선스는 `ASSETS.md` 의 표에 **반드시 기록한다.** 제출물 심사 대응용.

---

## 3. 성능 예산 — 넘으면 기능보다 성능을 먼저 고친다

| 항목 | 상한 |
|---|---|
| 목표 | 1080p / 60fps |
| 드로우콜 | ≤ 300 |
| 삼각형 | ≤ 350k |
| 동시 활성 좀비 | **14** (하드캡, 디자인 값 아님) |
| 실시간 그림자 광원 | ≤ 2 (손전등 + 이벤트 광원 1) |
| 텍스처 총 메모리 | ≤ 256MB |
| 초기 로딩 | ≤ 8초 |

- 좀비는 **반드시 풀링**한다 (`ZombiePool`). 런타임에 `new Zombie()` 를 호출하지 않는다.
- 그림자는 손전등만. 그 외 광원은 `castShadow = false`.
- 같은 재질은 공유한다. 병실 모듈은 **인스턴싱 또는 재질 재사용**이 기본.

---

## 4. 폴더 구조 — 이 배치가 계약이다

```
src/
├─ main.js              부트스트랩만. 로직 금지
├─ config/              ★ 모든 수치는 여기에만
│  ├─ balance.js        플레이어·좀비·연출 수치
│  └─ weapons.js        무기 정의 (데이터 드리븐)
├─ core/                엔진 계층 — 게임 내용을 몰라야 한다
│  ├─ Game.js           루프, 씬 소유, 시스템 조립
│  ├─ Input.js          키보드/마우스/포인터락
│  ├─ EventBus.js       시스템 간 통신 (직접 참조 대신 이걸 쓴다)
│  ├─ Collision.js      AABB 월드 충돌
│  └─ AudioManager.js   사운드 재생·3D 오디오
├─ player/              플레이어
│  ├─ Player.js         이동·체력·스태미나
│  └─ Flashlight.js     손전등·배터리
├─ weapons/
│  └─ WeaponSystem.js   장착·발사·재장전 (무기 종류는 config/weapons.js)
├─ enemies/
│  ├─ Zombie.js         개체 상태머신
│  ├─ ZombiePool.js     풀링 (생성/반납)
│  └─ Director.js       ★ 동적 스폰 — 긴장도 기반
├─ world/
│  ├─ StageLoader.js    구역 로드/언로드
│  └─ stages/           구역 정의 (데이터에 가깝게)
├─ fx/
│  └─ Atmosphere.js     포그·조명·톤매핑 — 공포 분위기 담당
└─ ui/
   └─ HUD.js            DOM 오버레이

public/assets/
├─ textures/  models/  audio/{sfx,ambience}/  ui/
```

**계층 규칙**: `core/` 는 `player/`·`enemies/`·`world/` 를 import 하지 않는다.
위쪽(게임)이 아래쪽(엔진)을 쓰는 단방향만 허용. 통신이 필요하면 `EventBus`.

---

## 5. 공포는 이렇게 만든다 (연출 원칙)

돈과 시간을 여기 쓴다. 순서대로 효과가 크다.

1. **소리 > 시각.** 발소리, 정적, 멀리서 나는 긁는 소리. SFX 하나가 텍스처 열 장보다 무섭다.
2. **보이지 않는 것.** 손전등 사거리 밖은 완전한 검정. 포그 밀도로 시야를 강제로 자른다.
3. **정적의 리듬.** 계속 무서우면 안 무섭다. `Director` 의 RELIEF 구간을 반드시 지킨다.
4. **플레이어의 무력함.** 탄약을 아끼게 만든다. 재장전 중의 취약함이 공포다.
5. 점프스케어는 **스테이지당 2회 이하.** 남발하면 긴장이 죽는다.

---

## 6. 작업 사이클 — 매번 이 순서

```
1) PROGRESS.md 에서 "다음 할 일" 확인
2) 변경 → npm run dev 로 브라우저에서 눈으로 확인
3) 되면 커밋. 안 되면 git restore 후 접근을 바꿔서 재시도
4) PROGRESS.md 갱신 (되는 것 / 다음 할 일)
```

### 3번 룰
**같은 증상이 3번 반복되면 멈춘다.** 마지막 커밋으로 되돌리고,
(a) 다른 방식으로 만들거나 (b) **그 기능을 버린다.**
한 기능에 2시간 이상 쓰지 않는다. 사용자에게 상황을 보고하고 판단을 요청한다.

### 에러 대응
사용자가 콘솔 에러를 붙여넣으면, **추측하지 말고 그 에러부터 해결한다.**
관련 없는 코드를 같이 고치지 않는다.

---

## 7. 세션 종료 시

`PROGRESS.md` 를 갱신한다. 다음 세션이 이 파일만 읽고 이어받을 수 있어야 한다.
- 지금 되는 것 / 안 되는 것
- 다음 할 일 (우선순위 순 3개)
- 알려진 함정
