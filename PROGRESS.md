# PROGRESS — 현재 상태

> **세션 시작 시 CLAUDE.md 다음으로 이 파일을 읽는다.**
> 세션 종료 시 이 파일을 갱신한다. 다음 세션이 이것만 읽고 이어받을 수 있어야 한다.

최종 갱신: 2026-08-09 / **사전 과제 제출 완료**

> **이건 NHN GAME X AI HACKATHON(NAN 2026) 사전 과제다.** 5항목을 전부 냈다 —
> 무엇을 어떻게 냈는지는 아래 「제출 완료」 절에, 올릴 파일은 `제출/` 폴더에 있다.
> **이제부터는 확장 단계다.** 「이제부터 — 확장할 때 알아야 하는 것」 절이 출발점이다.

---

## 지금 되는 것

### 시스템
- [x] 프로젝트 뼈대 (Vite + Three.js) · 1인칭 이동 · AABB 벽 충돌
- [x] 손전등(F) — 배터리 소모, 잔량 낮으면 깜빡임
- [x] 어둠·포그·톤매핑 · 후처리(블룸·비네트·그레인·색수차)
- [x] **빛줄기 속 먼지** — 손전등 원뿔 안 입자만 밝게 (Points 1400, 드로우콜 1)
- [x] **환경맵** — 금속·유리가 형태를 읽히게 (FX.envIntensity 0.16 으로 아주 약하게)
- [x] **타이틀 화면** — AI 키아트(Higgsfield, 59KB)를 **CSS 로 포스터처럼 가공한** 배경 +
      왼쪽 아래 활자 락업. 형광등이 두 번 실패한 뒤 켜지며 활자가 드러나는 등장 연출,
      마우스 시차, 비상등 호흡, 초당 9번 움직이는 필름 그레인.
      조작법은 ESC 일시정지 화면으로 옮겼다. 이미지가 없으면 뒤의 3D 복도가 비친다(폴백)
- [x] **총구 화염** — 칠흑 속 발사 순간 주변이 전부 드러난다 (0.065초)
- [x] **방향성 넉백** — 맞은 방향으로 밀리고 젖혀진다. 뒤에서 맞으면 앞으로 고꾸라진다
- [x] **좀비가 몸을 돌려서 온다** — 회전 속도 제한(종류별). 전에는 방향이 바뀔 때마다 홱 돌았다
- [x] **공격 선딜** — 사거리에 들어와도 팔을 드는 시간이 있다. 전에는 닿는 즉시 깎였다
- [x] **이동감** — 달릴 때 시야각이 넓어지고, 옆걸음에 몸이 기울고, 헤드밥이 8자를 그린다
- [x] **계기판형 HUD** — 라벨+숫자+눈금, 위험 시 맥동, 저탄약 경고색. 콘덴스드 활자
- [x] **시체가 남는다** — 30초 유지 후 가라앉으며 사라짐. 죽은 자리에 핏자국
- [x] **피격 시 피가 튄다** — 맞은 방향으로, 헤드샷이면 더 많이 (재사용 풀, 드로우콜 1)
- [x] **로딩 화면** — 진행 막대 + 지금 무엇을 받는지
- [x] **표면 텍스처 1024** (색상만. 노멀·러프는 512) — AO 를 색에 55% 구워 넣음
- [x] 좀비 상태머신 + 풀링 + Director 긴장도 스폰 (BUILDUP / PEAK / RELIEF)
- [x] **좀비 3종** — 배회체 · 청각체(눈이 없다, 소리에만) · **포복체(신규, 엎드려 기어온다)**
- [x] 무기 시스템 (데이터 드리븐) · HUD · 게임 루프(시작→플레이→사망→재시작)
- [x] **구역 체크포인트** — 죽으면 "이 구역부터" / "처음부터". 전에는 무조건 1구역부터였다
- [x] **상시 목표 표시** — 좌상단에 "지금 할 일". 진행에 따라 갱신(무전기 2/4 등)
- [x] **무기를 구역에서 줍는다** — B1 쇠지렛대 · 2F 라디오(유인) · 3F 소방도끼
- [x] **타격감** — 카메라 흔들림(총·근접명중·피격) · 히트스톱 0.055초(닿았을 때만)
- [x] **좀비 발소리** — 보이기 전에 들린다. 16m 안쪽만, 포복체는 더 낮은 피치
- [x] **피격 방향 표시** — 맞은 쪽만 붉게 물든다
- [x] **적응형 해상도** — 프레임이 예산을 넘으면 스스로 낮춘다 (심사자 PC 를 고를 수 없다).
      **2026-08-06: 이게 그동안 동작하지 않고 있었다 — 아래 "성능" 항목 참고.**
- [x] **스텔스 피드백** — 노출도 계기(앉기 2m → 달리기+손전등 22m) + 감지 상태(경계/발각됨)
      + 좀비가 처음 경계할 때 나는 소리. 손전등을 켜는 대가가 처음으로 화면에 보인다
- [x] 상호작용(E) · 수색/전리품 · 부상 시스템 · 거리 기반 3D 오디오
- [x] **수색에서 9mm 탄약이 나온다** — 한 판 전체 +50발(48 → 98). 전에는 보급이 아예 없었다
- [x] 사운드 **54종** (ElevenLabs) · 좀비 GLB + 애니 **27클립** (Mixamo)
- [x] **1인칭 플레이어 목소리 12종** (2026-08-08) — 그전까지 플레이어가 내는 소리는
      `player_hurt` 하나뿐이었다. 좀비는 8종을 내는데 플레이어는 맞을 때 한 번 끙 하고 마는,
      몸이 없는 카메라였다. **물리는 순간에도 내 쪽은 완전히 무음**이라 남의 일처럼 보였다.
      기합 3(휘두를 때 42% 확률) · 비명 1(물린 순간) · 몸부림 2 · 헐떡임 2(탈진) ·
      신음 2(중상) · 피격 2(기존 1종과 합쳐 3종 교대). **확률과 쿨다운이 핵심이다** —
      매번 나오면 3분 만에 견딜 수 없어진다. 값은 `balance/world.js` 의 `AUDIO.voice`
- [x] **좀비 본체 3종 — 감염된 의료진이 무리의 90%다.** 후드·마스크·고글·니트릴 장갑의
      **전신 방호복**(80%), 수술모·마스크에 등이 찢긴 **수술복 외과의**(10%), 나머지가
      기본 메시. 이 병원이 왜 격리됐는지가 옷으로 읽힌다.
      **텍스처가 아니라 메시를 바꾼 이유**는 아래 "좀비 본체 3종" 항목 참고
- [x] **무기 스윙이 사람 모션 궤적을 따른다** — 사람 근접공격에서 오른손 궤적만 뽑아
      뷰모델에 입혔다. 절차적 사인 곡선과 달리 예비동작에서 뜸을 들이고 타격에서 확 빠진다
- [x] **모션 프레임 단위 점검 도구** `tools/qa_motion.js` — 바닥뚫림·공중부양·배속이상을
      1482프레임에서 자동으로 잡는다

#### 2026-08-08 (D-2) — 타격 · FX · 최적화
- [x] **날붙이와 둔기가 다른 소리를 낸다** — `weapons.js` 의 `blade` 표시.
      그전까지 `melee_hit`(sfx_axe_hit_flesh.mp3)은 **등록만 되고 한 번도 재생되지 않았고**,
      소방도끼와 쇠파이프가 완전히 같은 소리였다
- [x] **빗나간 총알이 벽·바닥·천장에 박힌다** — 회색 먼지 + 둔기음 1.6배속('틱').
      전에는 빗맞히면 총구 화염 0.065초 말고 아무 일도 안 일어나서, 칠흑 속에서
      조준이 맞았는지 알 단서가 없었다. 산탄이어도 추적은 한 발에 한 번만 한다
- [x] **전투 중 바닥에 피가 쌓인다** — 입자가 바닥에 닿는 첫 프레임에 22% 확률.
      전에는 다섯 발을 맞혀도 **죽기 전까지 바닥이 처음과 똑같이 깨끗했다**
- [x] **헤드샷이 화면에서 구분된다** — 입자가 크고 밝고 높이 튄다.
      데미지가 2.2배인데 그전까지 화면은 "입자가 조금 더 많다"만 말했다
- [x] **맞으면 화면 구석이 붉게 닫힌다** (`POST.hurt*`) — 채도가 빠지고 색수차가 벌어진다.
      전에는 HUD 의 빨간 테두리 하나뿐이라 "UI 가 알렸다"로만 읽혔다
- [x] **좀비 발소리가 밟는 바닥을 따른다** — 전에는 다섯 구역 내내 콘크리트 고정이라,
      타일 복도에서도, 젖은 바닥에서도 같은 소리였다 (플레이어는 이미 재질을 듣고 있었다)
- [x] **스폰 직후 발이 바닥을 뚫던 것** (최대 10cm, 20개체 중 2~4개) — `ANIM.spawnFade`
- [x] **비상등 고정 슬롯** — 씬의 광원 수를 6으로 붙박고 지금 화면에 영향을 주는 것만
      싣는다. 광원 14개짜리 구역에서 **화면은 그대로**(픽셀 평균차 0.002)인데
      셰이더 프로그램 누적이 200 → 82 로 줄었다
- [x] **애니메이션 LOD** — 14m 밖 30fps, 26m 밖 20fps. 건너뛴 dt 를 모아 넘겨서 속도는 그대로
- [x] **사망 핏자국 인스턴싱** — 자국 하나가 드로우콜 하나였다(상한 22). 이제 1개, 상한 48
- [x] **손전등이 꺼져 있으면 그림자 패스를 건너뛴다** — 아무것도 안 비추는 4096² 패스가
      계속 돌고 있었다

#### 2026-08-08 이어받기 — 위 작업분을 커밋 전에 검토해서 나온 결함 4건

새벽 세션은 **01:49 에 QA 를 돌린 뒤 `Impact.js`·`PostFX.js`·`WeaponSystem.js`·`balance.js` 를
더 고치고 02:09 에 끊겼다.** 즉 마지막 수정분은 어떤 검사도 통과한 적이 없었다.
전부 위 항목들이 만든 결함이고, 커밋 전에 잡았다.

- [x] **핏자국이 의도의 5~7배로 찍히던 것** — `Impact.update` 가 **중력을 착지 여부와
      상관없이 먼저** 더한다. 그래서 멈춘 입자도 다음 프레임이면 `vel.y ≠ 0` 이 되고
      위치도 바닥 밑으로 다시 내려가, "닿는 첫 프레임에만" 이라던 가드(`vel.y !== 0`)가
      **입자가 사라질 때까지 매 프레임 다시 열렸다.** 64칸 링버퍼가 좀비 한두 마리 만에
      한 바퀴 돌아 `decalFade` 12초는 재생될 기회조차 없었다.
      → `landed` 플래그. 실측 **타격당 7.4 → 1.15개** (의도 1.19), 사망 버스트 12.8 → 1.93
- [x] **몸통을 쳐서 죽여도 헤드샷 반동이 오던 것** — 사망 버스트가 `ZOMBIE_HIT` 를
      `headshot: true` 로 재발행했는데, 이 플래그는 생김새 값이 아니라 **의미** 값이다.
      `Player` 가 헤드샷 전용 화면흔들림을, `HUD` 가 헤드샷 마커를 같이 읽는다.
      → 생김새만 바꾸는 `hot` 을 따로 뒀다(기본값이 `headshot` 이라 평소엔 동작 동일).
      실측 **몸통 처치 반동 0.2 → 0**, 헤드샷은 0.2 그대로
- [x] **구역을 넘어도 앞 구역 핏자국이 따라오던 것** — `EV.STAGE_LOADED` 가 EventBus 에
      정의돼 있고 `Game` 이 구독까지 해 뒀는데(`bloodDecals.clear()`) **발행하는 코드가
      저장소 어디에도 없었다.** → `StageLoader.load()` 끝에서 발행한다. 호출자가
      셋(`startAttract`·`restart`·`_nextStage`)이라 바깥에서 쏘면 한 곳을 빠뜨린다.
      실측 **전환 시 64 → 0**
- [x] **다 옅어진 자국이 사라지지 않고 검게 눌러앉던 것** — `instanceColor` 는 색만
      곱한다. 알파는 텍스처와 `material.opacity` 에서 오는데 둘 다 안 건드리므로,
      0 으로 보내면 투명해지는 게 아니라 **새까매진다.** `mesh.count` 도 안 줄어 계속
      그려졌다. → 다 옅어지면 크기 0 으로 접고 슬롯을 빈칸으로 되돌린다

> **검토 방식**: 커밋 안 된 diff 를 6조각으로 나눠 병렬 검토하고, 지적 하나마다
> 서로 다른 렌즈 3개로 **반증을 시도**해서 2명 이상이 반증하면 버렸다.
> 지적 12건(중복 제거 8건) 중 위 4건 + 아래 함정 2건이 살아남았다.
> 밸런스 값 관련 지적은 전부 반증으로 걸러졌다(`lodNear` 14m 가 손전등 사거리보다
> 작다는 지적 — `FLASHLIGHT.range` 22 는 밝기 반경이 아니라 three 의 **컷오프**다).

**QA 8/8 통과** · 구역 125/125 · 모션 1482/1482 · 콘솔 0 · 네트워크 0

#### 2026-08-08 — 사용자가 직접 플레이하고 보고한 4건

**전부 자동 QA 가 못 잡는 것들이었다.** 배치·모션·콘솔은 전부 정상이었고,
125항목 검사도 통과하고 있었다. **사람이 플레이해야만 나오는 결함이 따로 있다.**

- [x] **주운 근접 무기를 쓸 수 없었다** — `switchTo` 가 `findIndex` 로 그 슬롯의
      **첫 무기**만 골랐다. 근접이 전부 슬롯 1 이므로 1번 키는 영원히 쇠파이프를 잡았다.
      **B1 쇠지렛대 · 3F 소방도끼 · 소화기가 줍는 순간 죽은 무기가 됐다** —
      구역마다 무기를 하나씩 놓아 둔 설계가 통째로 무효였다.
      → 슬롯 키가 그 슬롯 안에서 **순환**한다. 줍는 즉시 손에 쥐고, 안내에 전환 키를 적는다
- [x] **영안실에서 시신 앞이 막혔다** — `prop_bodybag` 은 높이 **0.33m**(무릎)인데
      B1 에서만 `collide: [0.9, 2.0]` 이 걸려 있었다. Collision 은 XZ 전용이라
      **천장까지 닿는 투명한 벽**이 된다. 게다가 간격 2.4m − 박스 깊이 2.0m = **틈 0.4m**,
      플레이어 반지름은 0.35m — 여섯 개가 이어져 **12m 짜리 벽**이었다.
      다른 네 구역(2F·3F·옥상)은 원래부터 충돌 없이 놓고 있었다.
      → B1 도 충돌을 뗐다. 실측 **막힘 57/57 → 0/57**
- [x] **서 있는 시신이 유리 파편 덩어리로 보였다** — 150,000 → **5,000** 삼각형
      데시메이트가 메시를 부쉈다. 원본이 머리카락·옷을 별도 껍질로 들고 있어서
      97% 를 깎으면 껍질끼리 뒤엉킨다. → **20,000** 으로 다시 구웠다(1F 정적 삼각형
      83k → 98k, 예산 350k 안). 금속도 0.54 → 0 으로 내려 표면의 흰 점 반사도 없앴다
      (시신에 금속 반사는 맞지 않는다. 전역 상한 `propModelMetalMax` 0.55 를 내리면
      침대 프레임·링거대가 같이 죽으므로 **그 GLB 파일만** 고쳤다)
- [x] **재장전에 동작이 없었다** — 소리는 나고 있었지만 **총이 미동도 안 했다.**
      화면에 있는 것이 HUD 진행 링뿐이라 "UI 가 기다리라고 한다"로 읽혔지
      **손이 바빠서 못 쏜다**로 읽히지 않았다. `WEAPON_RELOAD` 로 총을 내려 몸 쪽으로
      당기고 탄창이 보이게 눕혔다가 올린다. 음원이 하나뿐이라 **같은 소리를 두 번**
      쓴다 — 뺄 때는 0.92배속, 꽂을 때는 1.28배속(진행률 0.62 지점)

> **교훈: 자동 QA 가 보는 것과 플레이어가 겪는 것은 겹치지 않는다.**
> 위 넷 중 어느 것도 콘솔 에러를 내지 않고, 배치 검사도 모션 검사도 통과한다.
> 무기 전환은 **키 입력이 있어야** 드러나고, 시체가방 벽은 **그 자리에 서 봐야** 알고,
> 깨진 메시는 **눈으로 봐야** 알고, 없는 동작은 **기다려 봐야** 안다.

#### 2026-08-08 — "시야를 크게 돌릴 때마다 조금씩 끊긴다" (해결)

**`Game._precompile` 이 있는데도 증상이 남아 있었다.** `renderer.compile()` 이 굽는 것은
그것이 부르는 시점의 **화면 재질뿐**이고, 두 종류가 빠진다.

| 빠지는 것 | 왜 |
|---|---|
| **그림자 패스의 깊이 재질** | 물체가 손전등 원뿔에 **처음 들어오는 프레임**에 따로 컴파일된다 |
| **후처리 경로의 화면 재질** | 프로그램 캐시 키에 **출력 색공간**이 들어간다. 캔버스는 `srgb`, 후처리 렌더타깃은 `srgb-linear` — **같은 재질인데 다른 프로그램**이다 |

→ `_warmupFrames()` 를 넣었다. 구역을 연 직후 **제자리에서 피치 3단 × 12방향 = 36프레임**을
**`post.render` 로**(게임과 같은 경로) 그려 둔다. 손전등을 강제로 켠다 — 꺼져 있으면
그림자 패스를 건너뛰어 깊이 재질이 하나도 안 구워진다. 값은 `PERF.warmupSteps`.

**실측 (구역 이동 직후 한 바퀴 · 배포 빌드 · 헤드리스)**

| | 늦게 구워진 프로그램 | 최악 프레임 |
|---|---|---|
| 이전 | 8개 (65°·105°·115°·120°·140°·145°·150°…) | **234ms** |
| 캔버스로 워밍업 | 1개 (B1 유리 재질만) | 140ms |
| **후처리 경로로 워밍업** | **0개** (144개 각도/피치 조합) | **3.4ms** |

**로딩에 얹히는 비용은 0 이다 — 다만 그렇게 만드는 데 한 가지가 더 필요했다.**

| | 에셋받기 | 진입까지 |
|---|---|---|
| 워밍업 끔(`warmupSteps: 0`) | 4.82s | 9.45s |
| 워밍업을 **타이틀에서도** 함 | **11.34s** | 16.23s |
| **타이틀에서는 안 함** (지금) | **3.32s** | **9.02s** |

`startAttract()` 와 `restart()` 가 **둘 다 1구역을 열고 굽고 있었다** — 같은 일을 두 번 하는데
그 시간이 통째로 **로딩 막대 뒤에 붙는다.** 타이틀은 같은 1구역을 느리게 훑는 연출이라
거기서 구워지는 것이 어차피 플레이용과 같다. 그래서 타이틀에서는 굽지 않는다
(`_precompile(false)`) — 비용이 연출이 도는 동안 저절로 흩어지고, 시작을 누를 때는
`restart()` 의 워밍업이 이미 구워진 것을 만나 거의 공짜로 끝난다.

> **워밍업은 반드시 아주 작게 그린다.** 컴파일되는 프로그램은 **해상도와 무관한데**,
> 풀 해상도 + 4096² 그림자로 36프레임을 돌렸더니 **로딩이 7초 → 44초**가 되고
> QA 회차가 타임아웃으로 죽었다. `PERF.warmupPixelRatio`(0.1)와
> `PERF.warmupShadowSize`(256)로 픽셀만 줄인다 — **훑는 각도와 렌더 경로는 그대로 둔다.**
> 줄인 뒤에도 늦게 구워지는 프로그램은 0건이다. **그림자 맵 크기를 바꿀 때는
> `shadow.map.dispose(); shadow.map = null` 을 같이 해야** 새 크기로 다시 잡힌다.

> **총 비용은 그대로다 — 위치만 옮겼다.** 로딩·구역 전환 뒤로 보내서 플레이 중에 안 보이게 했다.
> 워밍업을 `renderer.render` 로 하면 **그림자 깊이 재질만 잡히고 화면 재질은 헛수고가 된다.**
> 색공간 때문에 캐시 키가 갈리기 때문이다. 이걸 모르고 재면 "7개는 고쳤는데 하나가 안 잡힌다"
> 로 보인다 — 실제로 그 상태에서 한 번 헤맸다. 어느 프로그램인지는
> `renderer.info.programs` 의 `cacheKey` 를 스윕 전후로 비교하면 바로 나온다
> (`scratchpad/qa/which_program.mjs`).

#### 2026-08-08 — 밝기와 타격감 (해결)

**둘 다 "가장 그럴듯한 값"이 레버가 아니었다.**

- [x] **화면이 너무 어두워 진행이 어렵다** — 손전등을 켠 1F 복도조차 **화면의 74%가
      휘도 0.02 미만**이었다. 먼저 의심한 `ambientIntensity` 는 **6배로 올려도
      74% → 69%** 다. `fogColor` 가 `0x05070a`(거의 검정)라 거리가 있는 픽셀이 전부
      그 색으로 수렴하기 때문이다. 포그 색·밀도·비네트도 각각 5%p 안쪽.
      → **`FX.exposure` 0.95 → 1.5.** 먹통 74.4% → 42.7%.
      노출은 전체에 곱해지므로 **손전등 원뿔과 바깥의 대비는 그대로다** — 어둠을 없앤 게
      아니라 원뿔 안을 읽히게 했다. 5구역 × 사건 전후 10상태에서 **하얗게 타는 픽셀 0%**
      (옥상만 0.01%). 그레인은 톤매핑 뒤에 더해져 같이 커지지 않는다.
- [x] **휘두르는 느낌이 없다** — 히트스톱·반발·척추 플린치는 이미 다 있었다.
      스윙을 프레임 단위로 찍어 보니 **피가 튀는 순간 무기가 궤적의 5.7% 지점**,
      사실상 쉬는 자세였다. 설계값 `contact: 0.46` 을 **`contactMaxDelay: 0.17` 이
      조용히 덮고 있었다** — `_swingDur = min(cooldown×0.92, 0.62)` 라
      **근접 5종 중 4종이 상한에 걸렸고, 무겁고 느린 무기일수록 더 어긋났다.**
      궤적이 `easeIn = t³` 이라 앞쪽이 납작한 것이 이를 증폭한다.
      → **`contactMaxDelay` 0.17 → 0.30.** 쇠파이프 궤적값 **0.057 → 0.394**.
      쿨다운은 클릭 시점에 걸리므로 **연타 리듬은 1프레임도 안 바뀐다.**

- [x] **배포본에서만 근접 궤적이 죽어 있었다** — `swing_curves.json` 이 **404** 였다.
      `src/weapons/SwingCurves.js` 만 `'/assets/...'` 로 박혀 있었고 나머지 모듈은
      전부 `import.meta.env.BASE_URL` 을 쓴다. `base: './'` 라 배포본에서는
      루트 절대경로가 깨진다. → `${import.meta.env.BASE_URL}assets/...` 로 고쳤다.

  > **왜 지금까지 아무도 몰랐나 — 세 가지가 겹쳤다.**
  > (1) 개발 서버는 루트에서 서빙하므로 그대로 열린다.
  > (2) 이 모듈은 **폴백이 설계**다 — 없으면 절차적 사인 스윙으로 조용히 떨어진다.
  > (3) **자동 QA 는 개발 서버밖에 못 본다.** 하네스가 `tools/qa_stages.js` 를 주입하는데
  > 그 파일은 빌드에 안 들어간다(게임 코드가 아니다). 프로덕션 빌드에 대고 돌리면
  > 그 스크립트가 404 나서 회차 자체가 성립하지 않는다. 그래서 배포본에서만, 에러 없이,
  > **모든 근접 공격이 사람 모션 궤적 대신 사인 곡선으로 돌고 있었다.**
  > `index.html` 의 같은 형태(`url('/assets/...')`)는 **Vite 가 빌드 때 다시 써 주지만
  > JS 문자열은 안 고쳐 준다** — 그래서 타이틀 키아트는 멀쩡했다.
  > **점검: `grep -rn "['\`\"]/assets/" src/` 가 아무것도 안 나와야 한다.**
  > 앞으로 배포 뒤에는 `scratchpad/qa/live_check.mjs` 로 **배포 URL 을 직접** 확인한다.

> **교훈: 고치기 전에 재면 안 고쳐도 될 것을 안 고치게 된다.**
> 주변광을 올렸다면 화면은 그대로인 채 성능만 나빠졌고, 타격 이펙트를 더 넣었다면
> 여전히 쉬는 자세에서 피가 튀었을 것이다.
> **함정: 밝기를 잴 때는 손전등을 끈 상태도 같이 재라.** 배터리가 떨어졌을 때
> 아무것도 못 하면 그건 공포가 아니라 막힌 것이다(끈 상태 먹통은 지금도 74%다 — 의도).

QA 4/4 · 구역 125/125 · 모션 1482/1482.

#### 2026-08-08 — 소품 4종이 새까맣게 나오던 것 (해결)

"서 있는 시신이 손전등을 정면으로 받는데도 검은 실루엣이다" 는 제보에서 출발했는데,
**같은 병에 걸린 에셋이 넷이었다.**

| 파일 | 죽은 텍스처 | 어디에 |
|---|---|---|
| `prop_bed.glb` | 3/3장 | **모든 병실의 침대** |
| `prop_ivdrip.glb` | 9/9장 | 2F·3F 병실 |
| `prop_bodybag.glb` | 3/3장 | 영안실 |
| `prop_standing_body.glb` | 2/2장 | 1F |

Blender glTF I/O 가 이미지를 **전 픽셀 0** 으로 내보냈다. 1024×1024 인데 파일이
**1.9KB** — 단색 이미지 크기다. 첫 커밋부터 그랬으니 이 소품들은 **한 번도 제대로
보인 적이 없다.**

**증상이 조명 버그를 완벽하게 흉내낸다.** 재질 값 → 자기 그림자 → 노멀 뒤집힘 순으로
셋을 의심했고 셋 다 틀렸다. 노멀은 런타임에 뒤집었더니 밝기 숫자가 71.8 → 118.6 으로
올라 잠깐 확정한 줄 알았는데, **그림을 보니 화면은 그대로 검었다** — 샘플 사각형이
인물 옆의 벽을 물고 있었다.

가른 것은 **조명을 아예 안 쓰는 `MeshBasicMaterial` 로 갈아보는 것**이다.
그래도 검으면 빛이 아니라 텍스처가 검은 것이다. 대조군으로 같은 재질을 빨강으로
칠해 메시가 그 자리에 있다는 것까지 확인했다.

```
지금 그대로        24.4      ← 검정
castShadow 끔     31.8      ← 그림자 아님
Basic + 텍스처     24.7      ← 빛을 안 쓰는데도 검정 → 텍스처가 범인
Basic 빨강         121.2     ← 메시는 멀쩡히 거기 있다
```

→ `tools/glb_fix_textures.py` 로 원본 모델에서 **이미지 바이트만** 이식했다
(`재질 순서 + 역할`로 짝을 맞춘다). 서 있는 시신 **24.4 → 105.3**, 침대 148.3.
`import_props.py` 재실행은 안 했다 — 24종의 축·크기·원점이 같이 흔들린다.
용량은 4개 합쳐 0.35MB 늘었다. 자세한 절차는 `ASSETS.md §6-3-G`.

> **교훈 둘.**
> **하나 — 숫자 하나만 보고 확정하지 마라.** 이번에 숫자가 두 번 거짓말했다.
> 좁은 영역을 재면 표본이 어디에 떨어졌는지 그림으로 확인해야 한다.
> **둘 — "빛 문제인가 재질 문제인가"는 `MeshBasicMaterial` 한 줄로 가른다.**
> 이걸 먼저 했으면 세 번 헛짚지 않았다.
>
> **점검: 새 GLB 를 넣으면 이미지 최대값이 0 인지 본다** (`--dry-run`).
> 발광 맵이 거의 검은 건 정상이다 — 판정은 평균이 아니라 **최대값**으로 한다.

#### 2026-08-08 — 시체가 공중에서 도는 것 (해결)

제보 사진 두 장("경찰이 기울어진 채 떠 있다", "간호사가 허리까지 파묻혔다")은
**같은 결함의 다른 프레임**이었다. 둘 다 **쓰러지는 도중**이다.

`_groundOffset()` 은 "지금 자세에서 가장 낮은 뼈를 바닥에 붙인다". 다 쓰러진
시체에는 맞는 규칙이고, 실제로 네 종류 모두 **가장 낮은 뼈가 0.06 으로 정확**했다.
그래서 앞선 조사가 "정상"이라는 답을 얻었다 — **다 가라앉은 뒤만 쟀기 때문이다.**

문제는 이 보정이 **쓰러지는 1.85초 내내** 걸린다는 것이다. 변환 때 루트의 수직
이동을 지웠으므로(`fbx_to_glb.py`) 사망 클립에는 떨어지는 성분이 아예 없다.
그래서 몸이 내려오는 대신 **가장 낮은 팔다리가 바닥에 붙박여 그것을 축으로 공중에서
회전한다** — 한 발로 버티며 허리가 솟은 자세가 이래서 나왔다.

→ 접지 높이를 **하한**으로만 쓰고, 거기까지 **시간으로 가속해 내려보낸다.**
`CORPSE.fallEase: 2` (등가속도 낙하와 같은 모양). 바닥은 여전히 안 뚫는다.

| | 0.4초 | 1.0초 | 2.0초 | 4초 뒤 |
|---|---|---|---|---|
| 이전 (가장 낮은 뼈 높이) | 0.06 | **0.06** ← 한 발로 버팀 | 0.06 | 0.06 |
| 지금 | 0.18 | **0.45** ← 넘어가는 중 | 0.08 | 0.06 |

최종 자세는 **한 치도 안 바뀐다**(0.06, 몸높이 0.18~0.34). 바뀐 것은 넘어가는 1.85초뿐이다.

> **함정: "다 끝난 상태"만 재면 과정의 결함이 안 잡힌다.**
> 제보 사진은 과정 중 한 프레임일 수 있다. 시간대별로 찍어야 한다
> (`scratchpad/qa/corpse_timeline.mjs` — 0.4 / 1 / 2 / 4 / 8초).

**이 수정으로 `qa_motion` 의 전제가 낡아서 같이 고쳤다.** 검사는 사망 프레임의
가장 낮은 뼈가 0.616m 아래일 것을 요구했는데, 그 값이 성립했던 이유가 바로
**붙박여 있었기 때문**이다. 즉 검사는 결함을 통과시키고 있었다. 이제 쓰러지는
동안은 뜨는 것이 정상이므로 `FALL_ABOVE_MUL = 3.4` (0.95m) 로 잡았다 — 실측 최대는
0.77m. **다 쓰러진 뒤의 접지는 `walk_audit.mjs` 가 층마다 본다**(20/20 이 0.06).
20회 중 1회꼴로 나던 `f59 공중 0.622m` 가 이것이었다.

#### 2026-08-08 — 다섯 층 전수 검수 (`scratchpad/qa/walk_audit.mjs`)

자동 QA(`qa_stages`)는 **값**을 보지 그림을 안 본다. 이번에 나온 결함 둘은 **둘 다
그림으로만** 잡혔다. 그래서 층마다 실제 이동 판정으로 걸어 다니며 찍고, 좀비를
죽여 재는 검수를 따로 만들었다.

| 층 | 갈 수 있는 칸 | 바닥 구멍 | 시체 4종 안착 |
|---|---|---|---|
| 1F 격리병동 | 2538 | 0 | 4/4 |
| B1 영안실 | 2193 | 0 | 4/4 |
| 2F 중환자실 | 1398 | 0 | 4/4 |
| 3F 수술부 | 1881 | 0 | 4/4 |
| 옥상 | 5494 | 0 | 4/4 |

시체는 **20/20 전부** 4초 뒤 가장 낮은 뼈 0.06 · 몸높이 0.22~0.45(누움).
옥상에서 12번 더 죽여 봤고 사망 클립 3종 모두 정상이었다. 브라우저 오류·요청 실패 0건.

> **함정: "갈 수 있는 칸"을 격자 전체로 잡으면 안 된다.**
> 첫 회차는 −9 ≤ x ≤ 9 격자를 전부 검사해서 **건물 바깥**(폭은 ±8m)을
> "바닥 구멍 66~154칸"으로 보고했다. 벽 바깥은 충돌 박스가 없으니 이동 판정이
> "지나갈 수 있다"고 답한다. **시작점에서 플러드필**로 실제 도달 가능한 칸만
> 검사해야 한다 — 그렇게 하니 구멍은 다섯 층 모두 0 이었다.

판정을 내린 것 하나 — 자판기 몸통(`vending_machine_bottom`)은 텍스처가 없지만
**원본 모델의 의도된 검은 캐비닛**이다(`baseColorFactor` 0.019). 죽은 텍스처
(이미지 최대값 0)와는 다른 것이므로 손대지 않는다.

#### 2026-08-08 — 배터리 · 2F 병목 · 닫힌 방 (해결)

- [x] **배터리가 한 통에 83초였다.** 층 하나를 도는 **순수 이동만** 1F 132m · 2F 113m 다
      (상호작용을 가까운 순서로 다 들르는 최소 경로, `scratchpad/qa/battery_econ.mjs`).
      손전등을 끄면 화면의 74%가 먹통이라 끄고 버티는 선택지가 없다 — 배터리는
      사실상 제한시간이다. 특히 후반이 굶는다: 수색처가 1F 12개인데 **3F 4개 · 옥상 2개**.
      → `FLASHLIGHT.drain` **1.2 → 0.75** (한 통 133초). 층별 소모 대비 획득이
      1F 1.30 · B1 1.03 · 2F 1.14 로 자급되고, **3F 0.74 · 옥상 0.78 은 적자로 남긴다.**
      넉넉해진 게 아니라 모자란 채로 굴러간다.

- [x] **2F 복도에 30cm 병목이 있었다.** 접수데스크(x −2.40~0.60)와 컴퓨터카트(1.10~1.70)가
      엇갈려, 사람(반지름 0.35)이 설 수 있는 자리가 **x 2.05~2.35** 만 남았다.
      지나갈 수는 있지만 벽으로 읽힌다 — 다른 네 층의 가장 좁은 복도는 1.35m 이상이다.
      → 카트를 데스크 뒤로 물렸다. 가장 좁은 통로 **0.35 → 1.45m**.

- [x] **판자로 막힌 방이 "왜" 막혔는지 안 보였다.** 복도 쪽에 봉쇄한 흔적을 놓았다 —
      왼쪽은 끌어낸 링거대와 끌린 핏자국, 오른쪽은 문을 등지고 밀어붙인 수납장.
      통행 폭 1.35 → 1.15m, 도달성 14/14 그대로.

> **함정: 도달 판정의 격자 크기가 결론을 만든다.**
> 0.5m 격자로 재니 2F 상호작용 7개(무전기 3·4번, **계단실 문 포함**)가 "못 닿음"으로
> 나와 층이 클리어 불가능해 보였다. 30cm 틈을 격자가 통째로 건너뛴 것이다.
> **0.1m 격자로 다시 재니 다섯 층 전부 못 닿는 것 0개다**(`fine_reach.mjs`).
> 다만 그 30cm 는 사람에게는 벽이므로 병목 자체는 고쳤다(`squeeze.mjs`).

#### 2026-08-08 — 드물게 시체가 선 채로 굳던 것 (해결)

**원인은 접지가 아니라 애니메이션 가중치였다.** `_updateAnim` 은 클립을 바꿀 때
**직전 것 하나만**(`curAnim`) 페이드아웃한다. 평소엔 충분한데,

- 전환이 페이드보다 빨리 일어나면(달리다 → 맞고 → 공격) 앞의 것이 다 빠지기 전에
  다음 것이 올라오고,
- `_startSwing` 은 공격 클립을 `_updateAnim` 을 거치지 않고 `reset().play()` 로
  되감는다 — 그 순간 가중치가 다시 1 이 된다.

그래서 **run·attack·hit 이 셋 다 가중치 1 로 남은 채** 죽는 경우가 생긴다.
사망 클립이 끝까지 재생돼도(가중치 1 · 시각 4.97/4.97) 최종 자세가 **넷의 평균**이라
시체가 선 채로 굳는다. 실측된 실패 사례는 전부 `죽기 전 state = ATTACK` 이었다.

→ `key === 'death'` 일 때 **나머지 클립을 전부 페이드아웃**한다. 사망은 되돌아올 수
없는 상태이므로 안전하다. 재현 하네스(`scratchpad/qa/repro.mjs`)에서
**20구당 1건 → 80구 0건.**

> **함정: 통제된 자리에서만 재면 이런 건 절대 안 잡힌다.**
> 가만히 세워 놓고 죽이면 78회 전부 정상이었다. 이 결함은 **전투 중 상태 전환**이
> 겹쳐야 나오므로, 걸어 다니다 죽이는 조건을 그대로 복제해야 재현된다.
> 증상(시체가 뜬다)에서 원인(접지)을 짐작했다면 또 헛짚었을 것이다 —
> 실패한 순간의 **클립 가중치를 통째로 찍은 것**이 답을 줬다.

**아래는 위 결함을 고치기 전의 기록이다 (해결됨).**

- 통제된 조건에서는 **78회 전부 정상**이다: 층별 20 + 옥상 12 + 본체4 x 사망클립3
  조합 40 + 옥상 지점별 6. 12조합의 몸높이가 0.18~0.50 으로 **결정적이라**
  본체·클립 탓이 아니다. 낮은 뼈는 언제나 0.06 이다.
- 그런데 배포본을 걸어 다니며 찍는 검수에서 **60여 회 중 2회** 몸높이 0.99 / 1.55 가
  나왔다(`live_walk.mjs`). 두 번 다 순찰 경로 끝에서 스폰한 개체다.
- **재현을 못 했으므로 원인을 적지 않는다.** 소품 위·단 위를 의심해 옥상의
  그 자리에서 6회 다시 죽여 봤지만 전부 0.06 이었다(아래로 쏜 레이가 잰
  0.24~0.39m 는 데크가 아니라 바닥에 흩어진 잔해였다).
- 재활용 가설도 **확인해서 지웠다**: `ZombiePool.spawn` 이 빈 자리가 없으면 가장
  오래된 시체를 재사용하지만, `Zombie.spawn` → `_reset()` 이 `_settled` 와
  `_corpseY` 를 되돌린다.
- **남은 것: 재현 조건을 먼저 찾아야 한다.** 두 사례는 `live_walk.mjs` 가 순찰 경로
  끝에서 스폰했을 때만 나왔으므로, 그 자리의 좌표를 그대로 박아 놓고 수십 회
  돌리는 것부터 한다. 조건을 못 잡은 채로 `_groundOffset` 을 또 만지지 않는다 —
  이 증상으로 이미 세 번(재질·자기그림자·노멀) 헛짚었다.

### 구역 — **1F → B1 → 2F → 3F → 옥상, 전부 연결됨**
- [x] **구역 A (1F 격리병동)** — 복도 + 병실. 카드키 → 잠긴 계단실 문
- [x] **구역 B (B1 영안실·기계실)** — 완전 암흑. 전원 레버 3개 → 점등 → 대규모 웨이브.
      **소품을 영안실답게 다시 깔았다**: 시신 안치 캐비닛 5개(전에는 사물함 21개를 쌓아 둔
      창고처럼 보였다) · 시신백 8구(그중 2구는 열린 채 비어 있고 끌린 핏자국이 이어진다) ·
      부검대 위의 시신 · 복도의 시신백 4구 · 시야를 끊는 커튼 2장
- [x] **구역 C (2F 병동)** — 병실 8개, 커튼으로 시야 차단, 점멸 조명.
      **사건: 무전기 4대를 켜야 계단실이 열린다** (켤 때마다 소음 → 웨이브)
- [x] **구역 D (3F 수술부·중환자실)** — 수술실 2개 + 커튼 늘어선 중환자실. 좁음이 공포.
      **사건: 무영등 2개에 전원을 넣어야 봉쇄문이 풀린다**
- [x] **구역 E (옥상)** — 천장 없는 야외. 설비 사이를 돌며 헬리패드로.
      **사건: 신호탄 → 90초 버티기 → 헬기 도착 → EXTRACTED.** 출구는 그전까지 열리지 않는다
- [x] **옥상 피날레 연출** (2026-08-09) — 쏘고 0.9초 뒤 머리 위에서 터지며 화면이
      흔들리고 옥상이 주황으로 물든다(14초). 헬기는 도착 45·25·10초 전부터 헬리패드
      쪽에서 점점 크게 들리고, 도착하면 화면이 밝아진다(먹통 8.9%→4.1%)

### 08-09 마지막 날에 들어간 것
- [x] **충돌 박스에 높이** — 무릎 높이 소품(넘어진 자판기 0.9m·벤치 0.5m)이 총알을
      막던 것을 고쳤다. 전 층 34개가 그런 상태였다. **높이를 보는 것은 총알뿐**이고
      이동·좀비 경로·소리 차폐는 예전 그대로다 (근접은 일부러 2D)
- [x] **관리자 무적이 물림 피해로 샜다** — `godMode` 를 피격 무적시간과 분리해
      세 경로(직접 피해·물림 지속·물림 실패) 전부에 걸었다
- [x] **관리자 Alt+K** — 이 구역의 사건을 즉시 해결. 한 번 더 누르면 출구를 연다
      (옥상 90초 건너뛰기). 촬영용이라 화면에 `[DEV]` 문구를 남기지 않는다
- [x] **옥상 디테일** — 자판기를 눕히고 잔해를 뿌렸다. 벤치·환기구·이동침대·링거대·
      휠체어·의자열·문짝·소화기. 눕힌 것들의 접지 높이는 전부 지오메트리로 재서 맞췄다

### 에셋
- [x] PBR 텍스처(ambientCG CC0) · 핏자국 데칼 3종 · 의료폐기물 산포
- [x] **Sketchfab 소품 24종 반입 완료** — 병상·휠체어·링거대·커튼·수술대·시신 등
- [x] **무기 GLB 3종** — 쇠파이프 · 9mm 권총 · 쇠지렛대(신규)
- [x] **좀비 본체 GLB 4종** — 기본(4.45MB) · 방호복(4.27MB) · 외과의(0.69MB) · **경찰(0.92MB)**.
      뒤의 둘은 클립을 방호복에서 빌린다(`clipsFrom`). 클립은 총 **30개**
- [x] 변환 파이프라인 `tools/import_props.py` (Blender 헤드리스, 미리보기 렌더 포함)

---

## 제출 완료 (2026-08-09) — 여기서 한 단락이 끝났다

사전 과제 5항목을 전부 냈다. 아래 넷이 제출한 것이고, 다섯 번째(팀 소개 문서)는
1인 개발이라 해당 없음이다.

| 제출 항목 | 무엇 |
|---|---|
| 빌드 + 전체 소스 | https://yunseok-map.github.io/zombie_games/ · 이 저장소 (공개, 커밋 기록 유지) |
| 플레이 동영상 | https://www.youtube.com/watch?v=TH71shPSNqs — **60초** (규정 30~60초), 공개 |
| 게임 소개 및 설명 | `제출/QUARANTINE_No3_게임소개및설명문서.pdf` (A4 8쪽) |
| AI 활용 기술 문서 | `제출/QUARANTINE_No3_AI활용기술문서.pdf` (A4 16쪽) |

**PDF 는 `제출/` 한 곳에서만 만들어진다.** 원본은 `docs/submission_*.html` 이고
`npm run pdf` 로 다시 뽑는다. 전에 `docs/` 에도 같은 PDF 를 두었다가 한쪽만 갱신되는
사고 직전까지 갔다 — 한 곳으로 합쳤다. `tools/check_pdf_layout.mjs` 도 같은 경로를 본다.

> **`docs/notes/TODO_MORNING.md` 는 이제 역사다.** 08-05 새벽에 쓴 인계 메모이고,
> 맨 위 상태표만 현재로 갱신해 두었다. 새 작업의 출발점으로 쓰지 마라.

---

## 이제부터 — 확장할 때 알아야 하는 것

**CLAUDE.md §1-8(마감 D-2 이후 구조 동결)은 해제됐다.** 제출이 끝났으므로 구조를
바꿔도 된다. 나머지 규칙(§1-1 수치는 config 에만, §1-7 대규모 재작성 금지 등)은 그대로다.

### 확장 지점이 이미 준비돼 있다

아래 셋은 **본선 48시간을 염두에 두고** 08-08 에 정비해 둔 것이다. 자세한 것은
이 문서의 「확장성 정비」 절을 본다.

| 무엇을 늘릴 때 | 어디를 만지는가 |
|---|---|
| 새 구역 | `src/world/stages/` 에 파일 하나. `room()` · `wallWithDoors()` 로 골격이 몇 줄에 선다 |
| 새 무기 | `src/config/weapons.js` 에 항목 하나 (데이터 드리븐). `slot` 1근접·2총·3투척 |
| 새 좀비 | `src/config/balance/zombie.js` 의 `ZOMBIE` 에 항목 + 구역 `typeWeights` |
| 밸런스 | `src/config/balance/` 다섯 파일. **코드에 숫자를 쓰지 마라** |

새 에셋을 넣으면 **`index.html` 의 크레딧 화면도 같이 고친다.** CC BY 는 표기가
의무라 ASSETS.md 만 고치면 라이선스 위반이 된다. 지금 22명이 들어 있다.

### 먼저 손볼 만한 것 (우선순위 순)

1. **좀비 시야·소리 차폐를 높이 인식으로.** 08-09 에 충돌 박스에 높이를 넣었지만
   **그 높이를 보는 것은 총알 하나뿐**이다. 좀비는 여전히 2D 로 보므로 무릎 높이
   엄폐물 뒤에 숨어도 들킨다. `Collision.segmentBlocked` 에 `y` 를 넘기는 곳을
   `Zombie` 의 시야 판정과 `AudioManager.occlusionTest` 로 넓히면 된다.
   **난이도가 쉬워지는 방향이라 밸런스를 다시 재야 한다** — 그래서 제출 전에 안 했다.
2. **엄폐 플레이를 만들 수 있다.** 위가 되면 "낮은 것 뒤에 앉아 숨는다"가 성립한다.
   지금 웅크리기(Ctrl)는 노출도만 줄이지 시야를 못 끊는다.
3. **1F 가 너무 어둡다** — 화면의 62.8%가 완전한 검정이다(실측). **앰비언트로는 못 고친다**
   (0.06→0.16 으로 올려도 47.1%, 평균 휘도는 그대로). 원인은 포그 밀도와 손전등 사거리다.
   손대려면 `hospital_a` 의 `mood.fogDensity` 와 `FLASHLIGHT` 를 같이 재면서 움직여야 한다.

### 알면서 안 고친 것 (다음 사람이 또 발견하지 않도록)

| 증상 | 왜 남겼나 |
|---|---|
| 옥상에서 빗나간 총알이 **3.1m 상공에 콘크리트 탄흔**을 남긴다 | `WALL_IMPACT.ceilingHeight` 가 구역과 무관한 전역 상수이고, 옥상만 천장이 없다. 빗맞혔을 때만 나오는 0.35초짜리 회색 입자라 미뤘다 |
| **19cm 짜리 물건이 플레이어를 멈춰 세운다** | 넘어가는 수단이 게임에 없다. 넘기게 하려면 이동에 높이 개념이 필요한데 그건 별개 작업이다 |
| 근접 공격은 **일부러 2D** 다 | 무릎 높이 잔해에 파이프가 부딪히는 것은 맞는 동작이다. 총알과 달리 고칠 것이 아니다 |
| **NavMesh 가 없다** | 1주 스코프. 좀비는 격자 + 조향으로 움직인다. 구역이 평면이라 지금까지는 충분했다 |
| **자동 플레이스루 하네스는 실패했다** | 탐욕 조향도 격자 BFS 도 벽에 붙어 멈췄다. 도달 가능성은 `qa_stages` 의 홍수 채우기가 이미 증명한다. 스크립트는 `scratchpad/playthrough.mjs` |
| **ElevenLabs 무료 플랜 = 비상업 한정** | 이 제출물은 수익화가 없어 조건 안이다. **상업 배포하면 유료 플랜에서 54종을 다시 뽑아야 한다** — 라이선스는 생성 시점 플랜을 따르므로 사후 업그레이드로 소급되지 않는다 |

### 저장소에 없는 것 (있는 줄 알고 찾지 마라)

| 없는 것 | 되살리는 법 |
|---|---|
| `tools/source_models/` — Sketchfab 소품 **원본** 109MB | 게임도 빌드도 안 읽는다. 출처 URL 19개와 변환 배율이 `ASSETS.md §4` 에 있다. 다시 받아 `blender --background --python tools/import_props.py -- [이름]` |
| `tools/preview/` — 변환 결과 미리보기 PNG | `import_props.py` 가 변환할 때마다 다시 그린다 |
| `fbx_src/` — Mixamo FBX 원본 | 처음부터 `.gitignore` 다. `ASSETS.md §6` 에 다운로드 설정이 있다 |

**커밋 기록은 지우지 않았다** — 규정이 요구한다. 그래서 위 파일들을 뺐어도
`git clone` 용량은 줄지 않는다. 줄어든 것은 파일 목록이다.

---

## 관리자 모드 — 영상 촬영·검수용 (2026-08-08)

**URL 뒤에 `?dev=1` 을 붙였을 때만** 켜진다. 심사자가 받는 링크에는 없으므로
실수로 밟을 일이 없다. 그냥 키를 열어 두면 심사 도중에 눌려서 게임이 망가진다.

```
http://localhost:5180/?dev=1              개발 서버
https://yunseok-map.github.io/zombie_games/?dev=1    배포본
```

| 키 | 하는 일 |
|---|---|
| **Alt + G** | 무기 전부 + 탄약 가득 + 카드키 지급 (무기 2종 → 9종) |
| **Alt + M** | 다음 구역으로 |
| **Alt + I** | 무적 켜기/끄기 — 촬영 중에 죽으면 테이크가 날아간다 |
| **Alt + K** | 이 구역의 사건을 즉시 해결. 한 번 더 누르면 출구를 연다 |

`Alt+K` 는 **레버까지 걸어가는 시간만** 없앤다 (2026-08-09 추가). 다섯 구역이
전부 "어두운 층에서 레버를 찾아 작동시켜라"라서, 보여 주고 싶은 장면에 닿기 전에
테이크가 길어졌다. 특히 **옥상 신호탄**은 마지막 구역이라 `Alt+M` 으로도 못 건넌다.

- 첫 번째 누름 → 안 쓴 사건 레버를 전부 작동시킨다 (B1 발전기 3 · 2F 무전기 4 ·
  3F 무영등 2 · 옥상 신호탄 1). **거리 무시**, 어디서 눌러도 된다.
- 두 번째 누름 → 출구를 연다. 옥상의 **헬기 대기 90초**를 건너뛸 때 쓴다.
- **웨이브·타이머는 평소와 똑같이 걸린다.** 레버를 실제로 당긴 것과 같은 경로
  (`Interaction.use`)를 타기 때문이다. 사건을 건너뛰는 게 아니라 이동만 건너뛴다.
- 고를 때 `event` 표시만 본다. 수납장·문도 `noisy` 라 그걸로 고르면 온 층의
  서랍이 다 열리고 전리품이 바닥에 쏟아진다.

**Alt 조합인 이유** — `?dev=1` 링크로 남에게 보여 줄 때도 안전해야 한다. 맨 G·M·I 는
플레이 중에 손이 미끄러지면 그냥 눌린다.

조합을 고를 때 걸린 것 —
- **`Ctrl+N` 은 못 쓴다.** 크롬이 새 창을 열고 페이지에 키를 넘기지 않는다
  (`preventDefault` 로도 못 막는 예약 조합: N·T·W). 그래서 다음 구역이 **M** 이다.
- **Ctrl 을 쓰면 안 된다.** Ctrl 은 **웅크리기**라 조합을 누를 때마다 화면이 내려앉았다.
  그래서 Alt 로 옮겼다.
- **`Alt+F`·`Alt+E`·`Alt+D` 도 못 쓴다.** F·E 는 크롬 메뉴, D 는 주소창이다.
  `preventDefault` 보다 브라우저 가속키가 먼저 먹는다. 그래서 사건 해결이 **K** 다.

**`Alt+M` 으로 구역을 넘기면 카드키가 사라진다** — 구역마다 열쇠가 따로인 설계라
그렇다. 촬영 중에는 구역을 넘길 때마다 `Alt+G` 를 다시 누른다.

네 가지를 실측으로 확인했다 (`scratchpad/dev_check.mjs`) —
`dev` 없으면 Ctrl 조합도 안 먹고 · `dev=1` 이어도 맨 G/M 은 안 먹고 ·
`dev=1`+Alt+G 는 2종→9종+카드키 · `dev=1`+Alt+M 은 구역 0→1.

> **영상은 이 모드로 장비만 갖추고 시작한 뒤 사람이 실제로 플레이해서 찍는다.**
> 규정이 "실제 플레이 화면 그대로 · AI 합성·편집 조작 불가"를 요구한다. 자동 조작으로
> 찍은 영상은 화면만 진짜고 플레이는 사람이 한 것이 아니라, 규정 해석에서 다툴
> 여지를 남긴다. 게다가 봇 플레이는 눈에 띄게 어색해서 작품에 손해다.

---

## 옥상 달빛 (2026-08-08)

다섯 구역 내내 손전등에만 의존해서, 옥상에 올라온 해방감이 없었다.
**여기만 완전한 어둠을 푼다.** 손전등을 꺼도 지형이 읽힌다.

먹통 = 휘도 0.02 미만 픽셀 = "비춰도 안 보인다"의 실체. 데크(0, 26)에서 잰 값:

| | 먹통 % | 평균 휘도 | 탄 픽셀 |
|---|---|---|---|
| 이전 (달빛 없음) | 42.4 | 0.0447 | 0% |
| **지금** | **5.7** | **0.1408** | 0% |

실내 4구역은 그대로다 (1F 63.1 · B1 55.3 · 2F 47.0 · 3F 47.0) — 앰비언트 색과
달빛 세기를 **명시 안 하면 기본으로 되돌리게** 해서 옥상의 푸른 기가 안 새어 나간다.

값은 네 단계를 찍어 비교하고 골랐다 (없음 63.7 / 1단계 51.1 / **2단계 25.4** / 3단계 16.7,
같은 시점·같은 화면). 3단계는 바닥이 회색으로 떠서 밤이 아니게 된다.

**달빛 광원은 실내에서도 씬에 남아 있다** (세기 0). 구역마다 넣었다 뺐다 하면
three 가 광원 개수로 셰이더를 다시 굽는다 — 옥상에 올라갈 때마다 화면이 멎는다.

---

## 화염병 — 컷했다가 되살렸다 (2026-08-08)

**컷한 이유**: 정의·뷰모델은 있었는데 던지면 **9m 앞에 즉시 보이지 않는 광역 피해**가
생길 뿐이었다. 병이 날아가지도, 벽에 막히지도, 불이 보이지도 않았다.
그때 남긴 조건이 "투사체·불꽃·화상 셋을 다 넣어라"였고, 셋 다 넣어서 되살렸다.

새 파일 하나 — `src/weapons/Throwables.js`. 라디오도 같은 투사체를 타므로 같이 고쳐졌다.

| 확인 항목 | 실측 |
|---|---|
| 날아간다 | 벽 없는 사거리 **10.83m** · 최고 2.28m 포물선 |
| 벽을 안 뚫는다 | 벽 5.2m → 병이 깨진 곳 **5.12m** |
| 불이 붙는다 | 입자 144개 · 불빛 2.9 |
| 불 안의 좀비가 닳는다 | 2초에 **-52** (dps 26 × tick 0.4 × 5회) |
| 불 밖은 안 닳는다 | **-0** |
| 꺼진다 | 8초 뒤 웅덩이 비활성 · 불빛 합 **0** |
| 광원 개수 | 12개 → **12개** (안 변한다) |

**플레이어도 탄다** (초당 14). 안 그러면 통로에 던져 놓고 뒤에 서 있는 무적 버튼이 된다.

### 여기서 밟은 것

- **`hit()` 을 그대로 쓰면 안 된다.** 0.4초마다 넉백·핏방울·둔기 소리가 터져서
  좀비가 영원히 비틀거린다 — 화염병이 즉승 버튼이 된다. `ZombieCombat.burn()` 을
  따로 뒀다 (체력·비명·죽음만).
- **처음 사거리가 3.6m 였다.** 불 반경이 2.6m 라 불 가장자리가 발밑 1m — 던지자마자
  자기가 탔다. 중력 17 → 14, 위로 얹는 비율 0.22 → 0.30.
- **불이 "떠 있는 주황색 구슬"로 보였다.** 입자가 안 겹치면 낱개가 점으로 읽힌다.
  수·크기를 크게 올리고 개당 밝기를 0.15 로 낮췄다 — 겹친 곳만 밝아진다.
- **`uniform float uAlpha;` 선언을 빼먹으니 화면 전체가 새까맣게** 나왔다.
  셰이더 컴파일 실패는 그 물체만 사라지는 게 아니라 렌더 자체를 중단시킨다.
  스크린샷 도구는 반드시 콘솔 에러를 같이 수집해야 한다.
- **헤드리스 스크린샷은 가끔 빈 프레임을 준다.** 같은 코드로 두 번 돌려 확인할 것
  (평균 휘도 0.0646 vs 0.000). 게임 결함으로 오해하기 쉽다.

### 디테일 보강 (2026-08-09)

**던지기에 동작이 아예 없었다.** 근접 스윙과 같은 잣대로 재니 진폭이 이랬다 —

| | 위치 Z | 회전 X | 회전 Z | 빈손 프레임 |
|---|---|---|---|---|
| 쇠파이프 스윙 | 0.249m | 1.657 rad | 1.460 rad | — |
| 화염병 (이전) | **0.000** | **0.000** | **0.000** | 0 |
| 화염병 (지금) | 0.197m | 0.709 rad | 0.347 rad | **13** |

`_throw()` 가 `_swing = 1` 을 넣긴 했는데 그 값은 매 프레임 `_swing = arc`(=0) 로
덮어써지고 있었다 — **죽은 코드였다.** 병이 손에 붙박인 채 소리만 나고 3m 앞에
투사체가 생겼다. 이제 뒤로 당겼다가 뿌리고, **손을 떠난 0.22초 동안 빈손**이 보인다.

같이 고친 것 —
- **병은 손을 떠나는 순간(동작의 44%, 15프레임째)에 생긴다.** 클릭 프레임에 만들면
  병을 든 채로 병이 앞에 나타난다. 근접이 접촉 시점에 판정하는 것과 같은 구조다.
- **마지막 하나를 던지면 근접으로 돌아간다.** 그전에는 개수 0 인데 손에는 병이 있었다.
  무기 교체는 **동작이 끝난 뒤에** 한다 — 방출 순간에 바꾸면 파이프가 툭 나타난다.
- **던지는 도중 무기를 바꾸면 예약된 병을 취소한다.** 안 그러면 이미 손에 없는 무기의
  병이 날아가고, 새로 만든 뷰모델이 "빈손" 구간에 걸려 숨은 채로 남는다.
- **불이 좀비 몸에 옮겨붙는다.** 좀비 재질은 개체끼리 공유해서(ZombieModel, 메모리)
  한 마리만 물들일 수가 없다 — 대신 그 자리에 불꽃을 계속 뿜는다. 웅덩이를 벗어나도
  2.6초 더 타고 그동안 초당 10 씩 닳는다(실측 1.5초에 16). 이게 없으면 불 밖으로
  한 발 나오는 순간 멀쩡해져서 불이 **바닥에 그려진 금** 처럼 보인다.
  **풀에서 돌려쓰는 개체라 `spawn()` 에서 반드시 꺼야 한다** — 안 그러면 새로 나온
  좀비가 아무것도 없는 자리에서 타오른다.
- **물린 순간 화면이 반응한다.** `GRAB_START` 는 그동안 아무도 듣지 않는 죽은
  이벤트였다. 물려도 즉시 피해가 없으니 `PLAYER_DAMAGED` 가 안 걸려서, 게임에서 가장
  무서운 순간에 후처리만 멀쩡했다 (소리·흔들림·몸부림 게이지는 이미 있었다).

검증 12항목 전부 통과 (`scratchpad/detail_check.mjs`).

새 소리 4종 — `throw_whoosh` · `molotov_break` · `fire_loop` · `radio_static`.
불 소리는 8초 동안 그 자리에서 나야 해서 `AudioManager.loop()` 를 새로 뒀다
(`play()` 는 한 번 울리고 끝이라 위치가 안 따라간다). 라디오가 좀비를 끄는 것은
**소리를 내기 때문인데 그 소리가 없었다** — 이번에 같이 넣었다.

---

## 확장성 정비 (2026-08-08 저녁) — 본선 48시간을 대비한 것

게임 동작은 **한 군데도 안 바뀌었다.** 전부 리팩터링이고, 매 단계 증명하며 진행했다.

### 검사를 사람 없이 돌릴 수 있게 됐다 — 이게 제일 크다
그동안 `qa_stages.js` · `qa_motion.js` 는 **사람이 브라우저 콘솔에 붙여넣어야만** 도는
물건이었다. 그래서 "5층 40회 결함 0건" 같은 검증을 돌린 하네스가 임시 폴더에서
태어났다 사라졌고, **같은 검증을 다시 할 수가 없었다.** 이제 `node tools/qa_run.mjs`
한 줄이다. 첫 실행에서 기존 결과를 그대로 재현했다.

**배포 워크플로가 이 검사를 통과해야 넘어간다.** 전에는 빌드만 성공하면 그대로
나갔다 — 게임이 깨져도 깨진 채로 심사자에게 갔다는 뜻이다. CI 환경 문제로 검사가
막힐 때를 위해 수동 실행에 `skip_qa` 스위치를 뒀다.

### 파일 크기 (CLAUDE.md §1-7 계약은 250줄)

| 파일 | 전 | 후 | 갈라져 나간 곳 |
|---|---|---|---|
| `config/balance.js` | 1,291 | **24** (배럴) | `balance/` 아래 player·zombie·combat·render·world 다섯 |
| `enemies/Zombie.js` | 931 | **496** | `ZombieAnim` 285 · `ZombieCombat` 234 |
| `weapons/WeaponSystem.js` | 801 | **338** | `WeaponAttack` 329 · `WeaponViewModel` 228 |

- **임포트 경로는 하나도 안 바뀌었다.** balance.js 가 전부 다시 내보내고,
  클래스에는 같은 이름의 한 줄 위임이 남아 있다. 부르는 쪽은 고칠 게 없다.
- 클래스 쪽은 메서드 본문을 **인스턴스를 첫 인자로 받는 함수**로 옮겼다
  (`_updateAnim(dt)` → `ZombieAnim._updateAnim(z, dt)`). 프로토타입에 몰래 끼우는
  방식(mixin)을 안 쓴 이유는 **클래스만 봐도 메서드 목록이 보여야** 하기 때문이다.
- 아직 250줄을 넘는 파일이 남아 있다. 더 쪼개면 조각이 너무 잘게 나뉜다고 판단했다.

### 구역은 데이터로 바꾸지 않았다 — 재 보고 그만뒀다
평평한 데이터로 펴려다 측정하고 접었다. 헬퍼 호출 451번이 1,295줄 안에 있는데
대부분 **보일러플레이트가 아니라 루프로 압축된 절차적 생성**이라, 데이터로 펴면
오히려 길어진다. 진짜 반복은 두 가지뿐이었고 그것만 빌더로 만들었다.

```js
room(cx, cz, w, d, { open: 's', t: WALL_T })   // 바닥+천장+벽4. open 에 트인 면
wallWithDoors('z', x, z0, z1, [10, 22])        // 문 구멍 뚫린 직선 벽
```

구역 C 의 계단실 둘·복도 벽, 구역 B 의 복도 벽에 적용했다. **새 층을 추가할 때
이 둘을 쓰면 골격이 몇 줄로 선다.**

### 이 리팩터링을 어떻게 증명했는가
- `balance.js` — 쪼개기 전후로 **값 덤프를 바이트 단위 비교**. export 37개 그대로,
  원본 1,200줄이 주석까지 전부 살아 있음을 확인.
- 구역 빌더 — **지오메트리 지문** 비교. 충돌 박스 327개와 메시 283개의 좌표가 전부 동일.
- 매 단계 `qa_run.mjs` — 125/125 · 1482프레임 문제 0 · 잡이슈 0.

> **하네스가 실제로 결함을 잡았다.** 무기 뷰모델을 옮길 때 임포트 두 개가 빠졌는데,
> 구역 검사 125항목과 모션 1482프레임은 **전부 통과하는 상태**였고 콘솔 수집이 잡았다.
> 검사 항목만 봤으면 못 봤을 결함이다 — 자동 검사에 "잡이슈"를 같이 세는 이유다.

---

## 실제로 플레이해 보고 나온 것들 (2026-08-09)

검사는 전부 통과하는데 손으로 해 보면 나오는 종류였다. **자동 검사가 못 보는 것이
무엇인지**를 보여주는 목록이라 그대로 남긴다.

### 1. 때리면 누워 있던 좀비가 일어섰다
`ZombieAnim._animKey` 가 **피격 클립을 포복 분기보다 먼저** 봤다. 기어다니는 개체는
선 자세 클립밖에 없어서 맞는 순간 벌떡 일어선다. 같은 실수를 포효(`scream`)에서
이미 한 번 고쳤는데 — 주석에도 적혀 있다 — `hit` 은 더 위에 있어서 안 고쳐졌다.
예외를 하나 더 다는 대신 **포복 분기를 `death` 바로 다음으로 올렸다.**

반응이 아예 없으면 안 맞은 것처럼 보이므로 맞는 동안 기는 배속을 늦춘다.
처음 0.12 로 넣었더니 `qa_motion` 이 **71프레임을 "늘어짐"으로 잡았다**(하한 0.40).
슬로모션 사고를 막으려고 둔 검사라, 검사를 고치지 않고 값을 0.45 로 올렸다.

### 2. 핏자국이 63cm 공중에 떠 있었다
1F 병실. 높이가 `0.635` 로 박혀 있었는데 그건 **매트리스 속**이고(윗면 0.725),
침대 변형 3 은 매트리스가 아예 **바닥(0.145)** 에 떨어져 있다. 게다가 옆으로 밀 때
`sx` 를 곱해서 왼쪽 방에서는 **매트리스 반대쪽 허공**에 찍혔다.

| | 전 | 후 |
|---|---|---|
| 자국 높이 | 0.635 × 9개 | 0.728(매트리스) · 0.148(바닥 매트리스) · 1.012(검사대) |

**같은 실수를 검사대에서 이미 한 번 고쳤다**(그 주석이 바로 위에 있다). 그래서
이번엔 숫자를 스테이지에 두지 않고 `Props.BED` 가 침대 지오메트리에서 직접
내보내게 했다 — 침대 모양을 고치면 자국 높이가 같이 따라온다.

### 3. 라디오가 뭐 하는 물건인지 알 수 없었다
던지면 반경 30m 좀비를 12초간 끌어모으는 미끼인데, 주우면 "라디오 획득"만 떴다.
근접·총기는 써 보면 알지만 **던지는 무기는 던져도 눈에 보이는 일이 안 난다.**
`weapons.js` 에 `usage` 를 넣고 획득할 때 한 번 설명한다.

### 4. 2F 무전기 — 버그가 아니라 안내의 문제였다
무전기 4개가 다 있고 4개 다 작동한다(검사 17번이 "무전 4개 작동"으로 확인). 문제는
**병실이 8개인데 무전기는 4개**라 좌우 번갈아 있고, 어두워서 어느 방을 봤는지
기억이 안 난다는 것이다. 배치를 바꾸는 대신 **안 켠 무전기가 5.5초마다 잡음을 낸다.**
라디오니까 자연스럽고, 벽 뒤면 먹먹하게 들려서(`occlusionTest`) 소리로 좁혀진다.
화살표를 띄우지 않은 이유는 이 프로젝트가 소리로 안내하기로 했기 때문이다(§5).

### 5. 관리자 조합이 Ctrl → **Alt**
`Ctrl` 은 웅크리기다. 조합을 누를 때마다 화면이 내려앉았다. 처음엔 "한 프레임이라
괜찮다"고 주석에 적어 뒀는데 실제로 해 보니 아니었다.

### 6. 지하에서 전원을 켜도 2층은 다시 칠흑이었다
구역마다 자기 `mood` 만 적용해서, 방금 한 일이 없던 일이 됐다. 그렇다고 B1 만큼
(앰비언트 1.15) 밝히면 손전등이 필요 없어져 이 게임이 무너진다. **부분 전원**으로 했다 —
`EV.POWER_RESTORED` 를 `StageLoader` 가 받아 두고, 위층은 각자 정한 `poweredMood` 만큼만 밝아진다.

| | 앰비언트 | 포그 | 비상등 | 먹통 % | 평균 휘도 |
|---|---|---|---|---|---|
| 2F | 0.035 → **0.70** | 0.055 → 0.048 | ×1.35 | 52.8 → **40.1** | 0.0362 → 0.0431 |
| 3F | 0.045 → **0.60** | 0.050 → 0.044 | ×1.30 | 48.7 → **42.2** | 0.0544 → 0.0590 |
| 옥상 | 달빛이라 그대로 | | | | |

> **처음 넣은 값(0.10 / 0.09)은 화면에서 아무 차이가 안 났다.** 먹통 52.7 → 48.3,
> 평균 휘도 +4%, 3F 는 0.7%p 로 사실상 변화 없음. "부분 전원이니까 조금만" 이라고
> 생각해서 고른 숫자였는데, B1 의 `litAmbient: 1.15` 주석에 **"0.4 대로는 화면에서
> '켜졌다'가 안 읽힌다"** 고 이미 적혀 있었다. 한 자릿수 아래를 넣고 있었던 것이다.
> 3지점 실측으로 곡선을 그려 다시 골랐다 —
>
> | 2F 앰비언트 | 0.035(꺼짐) | 0.25 | 0.45 | **0.70** | 1.15(B1급) |
> |---|---|---|---|---|---|
> | 먹통 % | 52.6 | 46.2 | 43.2 | **39.6** | 33.3 |
>
> B1급(1.15)으로 올려도 2F 는 여전히 33% 가 먹통이다 — **손전등은 계속 필요하다.**
> 어둠을 잃지 않는 선에서 가장 크게 읽히는 0.70 을 골랐다.
>
> 처음엔 위층 등을 전부 `flicker` 로 덮기도 했다. 그러면 층마다 정해 둔 steady/pulse
> 조합이 날아가서 **전기가 온 게 아니라 조명이 고장 난 것처럼** 보인다. 모드는 두고
> 밝기만 올린다. 새 판을 시작하면 전원은 꺼진 상태로 되돌아간다
> (체크포인트로 2F 이상에서 재개하면 켠 채로).

> **여기서 배운 것** — "값을 넣었다"와 "화면이 달라졌다"는 다른 말이다.
> 조명 값은 반드시 `qa_brightness` 로 전/후를 재고 넣는다. 이번에도 안 쟀으면
> 아무 효과 없는 기능을 넣어 놓고 고쳤다고 보고할 뻔했다.

### 자동으로 플레이시키려던 시도는 실패했다 — 기록해 둔다
게임 루프를 그대로 돌리고(렌더만 끔) 자동조종으로 5개 구역을 주파시키려 했다.
탐욕적 조향으로는 **1F 에서 163m 를 걷고도 목표에 2.9m 이내로 못 갔다**(방에 들어갔다
나오는 구조라 막다른 곳에서 진동한다). 격자 BFS 길찾기를 넣었더니 이번엔 경로를
벽에 붙여 뽑아서 긁히며 멈췄다.

**여기서 멈췄다.** 도달 가능성은 `qa_stages` 의 홍수 채우기가 이미 증명하고 있다 —
"12 상호작용 도달가능 · 19 시작→출구 경로 · 21 스폰이 갇히지 않음 · 22 상호작용
도달영역 안"이 5개 구역 전부 통과다. 자동조종을 더 다듬어도 **이미 아는 것을 더
비싸게 다시 알 뿐**이라 판단했다. (스크립트는 `scratchpad/playthrough.mjs` 에 있다)

### 무적(`?dev=1` + Alt+I)이 절반만 무적이었다

`Player._invuln` 하나로 두 가지를 겸하고 있었다 — **피격 후 짧은 무적시간**과
**관리자 무적**이다. 관리자 무적을 켜면 `_invuln` 에 큰 수를 넣는 방식이라,
`damage()` 를 **거치지 않는** 경로가 전부 새어 나갔다:

- 물렸을 때의 지속 피해(`GRAB.dps`)가 `this.hp` 를 직접 깎는다 → 물리면 그대로 죽는다
- 물림에서 못 빠져나왔을 때의 `GRAB.breakDamage` 도 마찬가지

`godMode` 를 **별도 필드로 분리**하고 세 경로 전부에 걸었다. 검증은 HP 가 줄어드는
길을 하나씩 다 밟아 봤다 — 직접 피해 · 15초 강제 물림 · 물림 타임아웃 · 불 속 5초.
넷 다 HP 100 유지, 끄면 다시 깎인다 (`scratchpad/god.mjs`).

### 마지막 장면이 게임에서 가장 조용했다 — 옥상 피날레

다섯 구역을 지나 도달하는 사건인데, 신호탄을 쏘면 **손전등 클릭음**(`flashlight`)이
나고 조명 모드가 바뀌는 게 전부였다. 헬기는 소리 파일이 아예 없어서 90초가 지나면
목표 문구만 바뀌었다. 소리가 이 게임의 절반이라고 써 놓고(CLAUDE.md §5-1)
가장 큰 순간이 무음이었다.

**소리 4종을 새로 만들었다** (ElevenLabs, `tools/gen_sfx.py`) —
`flare_launch` · `flare_boom` · `heli_distant` · `heli_arrive`.

- **옥상용 톤을 따로 뒀다** (`TONE_ROOF`). 기본 톤에 "cold concrete interior" 가
  들어 있어서 그대로 붙이면 뻥 뚫린 하늘에서 나야 할 소리가 복도에서 난다.
- **발사와 폭발을 두 파일로 나눴다.** 한 파일이면 터지는 시점이 고정되어
  화면흔들림·조명과 못 맞춘다. 쏘고 0.9초 뒤에 위에서 터진다 — 그 간격이 있어야
  "쏘아 올렸다"로 읽힌다. 한 번에 터뜨리면 그냥 큰 총소리다.
- 헬기는 **도착 45초 전부터** 멀리서 들린다 (45/25/10초 전, 볼륨 0.5→0.75→1.0,
  헬리패드 위치에서). 도착음만 있으면 90초가 그냥 견디는 시간이다.

**AI 가 준 첫 폭음은 쓸 수 없었다.** 프롬프트에 "bright sharp crack" 을 앞세웠더니
200Hz 아래 에너지가 **13.2%** — 권총(26%)보다 얇았고 전체 에너지의 98%가 앞 1.2초에
몰려서 '탕' 이었다. 고음 파열을 빼고 저역과 긴 꼬리만 주문해 다시 뽑았다:
**저역 89.2% · 6초 · 최대 0.699(클리핑 없음)**. 파형을 안 쟀으면 그냥 넣었을 것이다
(`scratchpad/sfxcheck.mjs` — 길이·최대·저역비율·포락선 박동을 잰다.
헬기 도착음은 **12.5Hz 로터 박동이 자기상관 0.95** 로 잡힌다).

**같이 드러난 것 — 클라이맥스에서 화면이 오히려 어두워졌다.** 헬기 도착 코드가
`setMood({ ambientIntensity: 0.4, fogDensity: 0.018 })` 만 넘겼는데,
`applyStageMood` 는 **안 넘긴 값을 기본으로 되돌린다.** 그래서 옥상의 정체성인
달빛(`moonIntensity` 0.95)과 푸른 기가 마지막 순간에 꺼졌다. 값을 전부 명시했다 —
지금은 앰비언트 0.85 · 달빛 1.25 · 포그 0.015 로 **밝아진다** (먹통 8.9% → 4.1%).

**신호탄 색은 한 번 틀렸다.** 물드는 색에 `fogColor` 를 같이 넘겼는데 그건
`scene.background` 이기도 해서 **하늘 전체가 자홍색**이 됐다 — 조명탄이 아니라
렌더링이 깨진 것으로 보였다. 하늘은 밤 그대로 두고 앰비언트만 주황으로 올린다.
신호탄은 바닥과 물건을 비추는 것이지 하늘을 칠하는 것이 아니다.

### 멈춘 포복체가 슬로모션으로 기었다

배회하다 잠깐 멈춘 포복체의 클립 배속이 `0.35 × 지터(0.85~1.15)` = **0.30~0.40** 이라
하한(`ANIM.moveMinSpeed` 0.40)을 거의 항상 밑돌았다. 지터가 개체마다 굴러서
`qa_motion` 에 걸렸다 안 걸렸다 했고, 이번 판에 90프레임이 걸렸다.

멈춘 동안과 맞는 동안은 **지터를 곱하지 않는다**(배속 자체가 하한 바로 위라
±15% 가 그대로 하한을 깬다). 그 위에 걷기/달리기와 같은 clamp 도 걸었다 —
**이 검사를 두 번 우회당했으므로**, 값이 아니라 구조로 막는다.

---

## 제출 전 코드 정비 (2026-08-09) — 심사자가 저장소를 열었을 때

게임 동작은 **한 군데도 안 바뀌었다.** 심사자가 소스를 열어 볼 것을 전제로,
"돌아가는 코드"를 "읽히는 저장소"로 만드는 작업만 했다.

### 중복 제거 — 같은 난수 생성기가 여섯 벌 있었다
`Scatter.js` 와 구역 파일 다섯 개가 **각자 똑같은 LCG 를 복사해 갖고 있었다.**
`world/rng.js` 하나로 합쳤다. 시드 기반 절차 생성이라 여기서 한 글자만 틀어져도
맵 배치가 통째로 바뀌는데, **지오메트리 지문을 바이트 비교해서 동일함을 확인**했다.

`WeaponViewModel` 의 이징 함수 두 개는 **매 프레임 호출되는 함수 안에서 매번 다시
정의되고 있었다.** 모듈 스코프로 올렸다. `WeaponSystem` 의 한 줄 위임 메서드마다
붙어 있던 같은 내용의 주석 블록도 위쪽 설명 하나로 합쳤다.

### 빌드 결과물을 아무도 안 보고 있었다 — 이게 제일 위험했다
CI 는 굽기만 하고 **구운 것이 열리는지는 안 봤다.** 그런데 자동 검사는 전부 개발
서버에 붙는다. 에셋 경로(`base`)·청크 로드 순서·번들에서만 깨지는 임포트는
**개발 서버에서 절대 안 나는 사고**다. `tools/qa_dist.mjs` 를 만들어 배포 직전에
`dist/` 를 실제로 띄우고, 404 · 콘솔 오류 · **`PLAYING` 상태 진입**까지 확인한다.

> 처음 만든 판은 `stages.length > 0` 을 기다렸는데 **그건 정적 게터라 타이틀
> 화면에서도 참**이었다. 통과는 했지만 아무것도 증명하지 못하는 검사였다.
> 상태가 `TITLE` 로 찍히는 걸 보고 잡았다.

### three 를 별도 청크로 갈랐다

| | 전 | 후 |
|---|---|---|
| 번들 | 787.0 kB (단일) | 게임 233.7 kB + three 554.0 kB |
| gzip | 220.3 kB | 80.4 kB + 140.3 kB |

게임 코드는 마감까지 계속 바뀌니 배포마다 해시가 갈리는데, 한 덩어리면 **안 바뀐
three 까지 매번 다시 받는다.** 지연 로딩은 안 했다 — 첫 화면부터 3D 라 쪼개도 결국
같은 순간에 다 받아야 하고 요청만 는다. 경고선은 600 kB 로 올렸다. 3D 엔진 554 kB 는
줄일 방법이 없고, **매 빌드 경고를 띄우면 진짜 경고를 가린다.**

### 저장소 첫인상
- **`README.md`** — 플레이 링크 · 실행 · 조작 · 설계 원칙 · `src/` 구조 · 검사 도구 ·
  에셋 라이선스. 적힌 수치는 전부 코드에서 확인하고 썼다.
- **`package.json`** — 버전·설명·라이선스·`engines` 를 채우고, 흩어져 있던 검사 명령을
  `lint` / `qa` / `qa:dist` / `verify` 로 정리했다. CI 도 날 `npx` 대신 이 스크립트를 부른다.
- `TODO_MORNING.md` · `MIGRATION_CHECKLIST.md` 는 **개인 메모라 `docs/notes/` 로** 내렸다
  (참조하던 두 문서도 같이 고쳤다). 루트에는 제출물에 해당하는 문서만 남겼다.
- `main.js` 의 `window.game` 은 디버그 잔재로 보이지만 **하네스의 계약**이다.
  지우면 CI 가 멈춘다는 사실을 주석으로 박아 뒀다.

---

## 예산 현황 (2026-08-05 실측, 구역별)

| 구역 | 드로우콜 | 삼각형 | +좀비14 | 스폰 | 판정 |
|---|---|---|---|---|---|
| 1F 격리병동 | 87 | 83,268 | 167k | 17 | ✅ |
| B1 영안실 | 29 | 45,103 | 129k | 10 | ✅ |
| 2F 병동 | 47 | 116,480 | 200k | 12 | ✅ |
| 3F 수술부 | 45 | 75,508 | 160k | 9 | ✅ |
| 옥상 | 26 | 37,258 | 121k | 9 | ✅ |

예산: 드로우콜 ≤300 · 삼각형 ≤350k (CLAUDE.md §3).
빌드 산출물 **15.9MB**, 로컬 로딩 5.3초(예산 8초). 배포 환경 실측은 위 "배포" 항목 참고.

---

## 성능 (2026-08-06 실측 · Radeon 780M 내장그래픽 · 60Hz)

**증상은 "프레임이 살짝 끊긴다" 였지만 실제로는 24fps 였다.** 원인은 후처리가 무거워서가
아니라 **적응형 해상도가 통째로 무효였던 것**이다.

three.js `EffectComposer` 는 만들어질 때의 `pixelRatio` 를 내부(`_pixelRatio`)에 붙잡아 두고
`setSize()` 에서 그 값을 곱한다. `Game._adaptResolution` 은 `renderer.setPixelRatio()` 만
바꾸고 컴포저에는 알려 주지 않았다. 그래서 배율을 0.75 로 낮춰도 씬과 후처리는 계속
1.5 배 해상도로 그려졌다. **약한 PC 를 위한 안전장치가 이름만 남아 있었다.**
→ `PostFX.setSize` 가 `composer.setPixelRatio()` 를 같이 갱신하도록 고쳤다.

| | 렌더 비용 | 60Hz 에서 보이는 fps |
|---|---|---|
| 이전 (2379x1125 로 고정 렌더) | 44.3ms | **20~24fps · 판정이 흔들림** |
| 이후 (적응형이 실제로 동작) | 13~16.8ms | **60fps 고정** |

### 품질 항목별 비용 (dpr 1.0 · 1600x900 · 좀비 8 · vsync 끄고 측정)

기준 12.8ms. **vsync 를 끄지 않으면 16.7ms 아래가 전부 16.7 로 읽혀 비교가 안 된다.**

| 항목 | 추가 비용 | 채택 |
|---|---|---|
| 이방성 8 → 16 | +0.4ms | ✅ 복도를 보며 걷는 게임이라 화면 대부분이 비스듬한 바닥 |
| 블룸 0.5 → 1.0 배 | +0.5ms | ✅ 사실상 공짜. 절반이면 손전등 핫스팟 둘레가 계단진다 |
| 그림자 2048 → 4096 | +1.2ms | ✅ 손전등 원뿔이 좁아 올린 만큼 그대로 좋아진다 |
| MSAA 0 → 2 | +3.3ms | ❌ MSAA 4 도 같은 값인데 **가장자리만** 좋아진다 |
| 해상도 1.0 → 1.25 배 | +7.9ms | 적응형에 맡긴다 (같은 예산으로 화면 **전체**가 좋아진다) |
| 그림자 전체 | 1.4ms | 병목 아님 |
| 좀비 14 마리 | 3.6ms | 병목 아님 |

**결론: 이 게임에서 비싼 것은 오직 해상도다.** 그래서 MSAA 대신 해상도 천장을 1.75 까지
열고(수퍼샘플링 허용) 적응형이 알아서 고르게 했다. 강한 PC 는 올라가고 약한 PC 는 내려간다.

### 적응형은 이제 수렴한다

60Hz vsync 에서 프레임 간격은 16.7 아니면 33.3 으로만 튄다 — 중간이 없다. 그래서
조금만 잘못 잡아도 두 값을 오가고, 배율이 바뀔 때마다 렌더 버퍼를 다시 잡느라 끊긴다.

- 예산을 **vsync 기준**으로 다시 잡았다 (16.5 → 20.0). 예전 값은 "여유 있음"을 영원히
  못 보게 만들어서, 한 번 내려간 해상도가 다시 안 올라왔다.
- 내릴 땐 초과 폭에 따라 크게/작게, 올릴 땐 연속 4회 확인 후 한 칸씩 (비대칭).
- **한 번 못 버틴 배율은 기억해 두고 다시 올라가지 않는다** → 한 값에 정착한다.
- 결과: 배율 변경 8회 → 3회, 33ms 초과 프레임 4% → 1.5%.

> **fps 상한(`PERF.fpsCap`)은 0(끔)이 기본이다.** vsync 가 이미 주사율로 묶어 주므로
> 그 위에 상한을 또 걸면 이득 없이 지터만 는다(측정: 663 중 2 → 9). 분위기를 위해
> 30 으로 낮추고 싶다면 **주사율의 약수만** 쓸 것. 다만 이 게임은 모션블러가 없어서
> 30 은 필름 룩이 아니라 끊김으로 읽히기 쉽고, 1인칭 마우스 조준이라 지연도 커진다.

---

## 배포 (2026-08-07)

> ### ✅ 2026-08-08 배포본이 로컬과 같다 (`53ef193`)
>
> Actions 성공, 배포 페이지에서 **직접 확인**했다 — 번들 해시 비교만으로는
> 게임이 뜬다는 보장이 없으므로 매번 실제로 열어 본다
> (`scratchpad/qa/live_check.mjs` · `live_prop.mjs`).
>
> | 확인한 것 | 배포본 실측 |
> |---|---|
> | `FX.exposure` | 1.5 (새 값) |
> | 먹통 픽셀 | 42.1% |
> | 근접 접촉 시점 | 0.22s / 스윙 0.478s = **0.46** (설계값 그대로) |
> | 서 있는 시신 밝기 | **105.3** (고치기 전 24.4) |
> | 네트워크 실패 | 0 |
>
> **배포본에서만 나는 결함이 실재한다.** `swing_curves.json` 404 가 그랬다.
> 로컬 QA 는 개발 서버밖에 못 보므로, 푸시한 뒤에는 반드시 배포 URL 을 직접 연다.

**https://yunseok-map.github.io/zombie_games/** — master 에 푸시하면 GitHub Actions 가
자동으로 다시 굽는다(`.github/workflows/deploy.yml`). `dist/` 는 커밋하지 않는다.
커밋된 dist 가 소스보다 낡아서 심사자만 옛날 게임을 보는 사고를 원천 차단한다.

> Pages 를 **처음 켜는 것**만 저장소 설정에서 사람이 한다
> (Settings > Pages > Source > `GitHub Actions`). 워크플로 토큰 권한 밖이다.

### 왜 마감 전에 미리 배포했는가

로컬 로딩 5.3초는 **아무것도 보장하지 않는다.** 심사자는 HTTPS 로 받는다.
올려 놓고 회선을 제한해 재 보니 이랬다.

| 회선 | 처음 배포 (전송 29MB) | 지금 (전송 13.4MB, 본체 3종 포함) |
|---|---|---|
| 50Mbps | 5.3초 | **2.9~3.1초** |
| **10Mbps** | **23.8초** | **10.6~11.0초** (2회 재현) |

10Mbps 는 호텔·사무실 와이파이에서 흔하다. **검은 화면 24초면 게임을 보기 전에 닫는다.**
마감 당일에 알았으면 손쓸 시간이 없었다. 이것이 배포를 미루면 안 되는 유일한 이유다
(PDF·영상은 늦게 써도 품질이 안 떨어지지만, 배포는 늦으면 **문제를 발견할 기회가 사라진다**).

**10Mbps 의 10.6초는 사실상 바닥이다** — 13.4MB 를 10Mbps 로 받는 이론 최소값이 10.7초다.
여기서 더 빨라지려면 **처음에 받는 양 자체**를 줄여야 한다. 남은 수단은 하나뿐이다:

- **구역별 지연 로딩.** 지금은 `Game.start()` 에서 5구역 소품을 전부 받는다(StageLoader 의
  조립이 동기라서 그렇게 돼 있다). 1구역 것만 받고 나머지는 진행하며 받으면 첫 진입이
  절반 이하로 떨어진다. 다만 **구조 변경**이다 (CLAUDE.md §1-8).

애니메이션 중복은 이미 처리했다 — 아래 "좀비 본체 3종" 참고. 남은 중복은 기본 메시와
방호복이 각각 들고 있는 클립 한 벌씩인데, **이 둘은 뼈 구성이 서로 달라 공유할 수 없다.**

> **빠른 회선 수치는 이 기계에서 믿을 게 못 된다.** 전송이 순식간이라 기계 쪽 잡음이
> 그대로 드러나서 같은 빌드가 2.9초와 9.1초를 오간다. 반면 **10Mbps 는 두 번 재서
> 10.6 / 11.0 초로 재현됐다** — 전송이 지배적이라 잡음에 묻히지 않는다.
> 판단은 **느린 회선 쪽 숫자**로 한다.

> **로딩 시간 측정은 기계 상태에 심하게 오염된다.** 한 번 재고 판단하면 오진한다.
> 실제로 "에셋을 줄였는데 3.3초 → 10.9초로 느려졌다"는 결과가 나와 회귀로 오해했는데,
> 같은 조건에서 옛/새 에셋을 번갈아 재 보니 **새 쪽이 빨랐다**(중앙값 2.42 vs 3.08초).
> 회차마다 9초대 이상치가 하나씩 섞여 평균을 망가뜨리고 있었다.
> 그리고 **제한을 건 쪽이 더 빠르게 나오면 그 회차는 버려라** — 첫 브라우저 실행이
> 콜드스타트 비용을 뒤집어쓴 것이다(제한없음 9.9초 vs 50Mbps 2.9초).
> 판단은 **중앙값**과 **같은 조건의 A/B** 로만 한다.

### 무엇이 컸는가 — 애니메이션이 아니라 텍스처였다

`tools/glb_breakdown` 류로 GLB 내부를 갈라 보니:

| 파일 | 이전 | 이후 | 정체 |
|---|---|---|---|
| `zombie_nurse.glb` | 7.01MB | **3.78MB** | 4096 노멀·광택맵이 안 줄어든 채 들어감 (아래 함정) |
| 소품·무기 27종 | 8.65MB | **3.98MB** | 1024 텍스처 → 512 (파이프라인 설정은 이미 512였는데 결과물이 낡아 있었다) |
| `zombie_shambler.glb` | 4.36MB | 4.05MB | 2048 → 1024 |
| `zombie_surgeon.glb` | — | +0.69MB | 새로 추가한 세 번째 본체 (클립을 빌려 써서 이 크기다) |
| 합계 `dist/` | 24.1MB | **16.6MB** | 본체가 하나 늘었는데도 24.1 → 16.6 |

소품 텍스처는 **512 로 줄여도 눈에 안 띈다** — 녹·긁힘·노멀 디테일이 그대로 남는다
(2배 확대해 옛 텍스처와 나란히 놓고 확인했다). 손전등 원뿔 안에서만 보이는 물건에
1024 는 낭비였다.

> 조명 아래 스크린샷으로 "흐려졌는가"를 판단하려 하지 마라. 손전등이 흰 소품을
> 통째로 날려서 아무것도 못 가린다. **텍스처를 직접 꺼내 나란히 놓고 봐야 한다.**

---

## 타격·물림 (2026-08-07)

### 시계가 두 개였다

공격 클립은 ATTACK 에 들어갈 때 한 번 재생되고 **그 뒤로는 루프**였고, 데미지는
그것과 무관한 쿨다운 타이머로 들어갔다. 스윙마다 되감지 않으니 두 번째 공격부터
위상이 어긋나서 **팔이 회수 중인데 체력이 깎이거나, 팔이 관통해도 아무 일이 없었다.**

지금은 스윙 하나(`_startSwing`)가 클립과 데미지를 같이 지배한다. 데미지는
**팔·다리가 가장 뻗는 프레임**에 들어간다. 그 지점은 `tools/measure_contact.js` 로
네 팔다리의 전방 도달 거리를 프레임 단위로 재서 넣었다:

| 클립 | 접촉 지점 | 타격 부위 |
|---|---|---|
| attack_01 | 0.742 | 오른손 |
| attack_02 | 0.326 | 왼손 |
| attack_03 | 0.416 | 오른손 |
| attack_04 | 0.202 | 왼손 |
| attack_05 | 0.360 | 왼손 |
| kicking | 0.360 | 오른발 (도달 0.83m 로 가장 길다) |

**0.20~0.74 로 3.7배 벌어진다 — 고정값 하나로는 절대 못 맞춘다.**
오른손만 재면 안 된다: 6개 중 3개가 왼손이고 kicking 은 발이다.

같이 넣은 것 — 닿는 순간 **좀비 쪽 히트스톱**(0.06초), 접촉까지 파고드는 **런지**
(루트 이동을 지워 제자리에서만 휘두르던 것의 보정), 맞은 쪽 반대로 밀리는
**방향 있는 카메라 충격**(흔들림과 달리 방향이 있다).

### 물림(GRAB)

공격이 **닿았을 때** 34% 확률로 좀비가 달라붙는다. 시야가 좀비 얼굴로 끌리고,
이동이 묶이고, 초당 8 씩 깎이며, `Space` 연타로 뿌리쳐야 한다.

억울하지 않게 만든 장치들 — 뿌리친 뒤 8초 쿨다운(연속으로 안 물린다),
체력 22% 아래면 아예 안 문다, 3.4초 안에 못 풀면 강제 해제(무한히 갇히지 않는다),
가만히 있으면 절대 안 풀린다(감쇠 0.5 > 획득 0.155).

> **놓아주는 판단은 전부 Player 가 한다.** 좀비가 죽거나 반납되거나 **총에 맞아
> 물기를 놓친** 경우까지 한 곳에서 봐야 어긋날 여지가 없다. 이 검사가 없으면
> 물고 있던 좀비를 쏴 죽였을 때 **영원히 갇힌다.**
> 검증: 스크래치패드의 `grab_test.mjs` 12항목 (QA 하네스는 이 영역을 안 건드린다).

---

## 걸음이 미끄러지던 이유 (2026-08-07)

배속을 `실제속도 / 설계속도(speedWander)` 로 잡고 있었다. 이건 **설계속도가 클립의
원래 속도와 같을 때만** 맞는 식이다. 실제로 재 보니 제각각이었다:

| 클립 | 길이 | 원래 속도 | 필요 배속(설계 1.1) | 판정 |
|---|---|---|---|---|
| walk_01 | **29.87초** | 0.06 m/s | **18.4×** | ❌ 걷기 클립이 아니다 |
| walk_02 | 4.07초 | 0.46 | 2.4× | ✅ |
| walk_03 | 3.03초 | 0.40 | 2.7× | ✅ |
| walk_04 | 4.97초 | 0.30 | 3.7× | ❌ 아래 참고 |

배속 상한이 2.2 였다. **모든 걷기가 상한에 걸려 다리가 못 따라가고 그대로 미끄러졌다.**

고친 것 — 기준을 **클립의 원래 속도**(`ANIM.clipSpeed`, 실측값)로 바꾸고,
배회 속도를 1.1 → 0.8 로 낮췄다(어슬렁거리다 갑자기 달려드는 낙차가 이 게임의 리듬이다).

> **미끄러짐이 더 큰 결함을 가리고 있었다.** `walk_04` 는 클립 중간(t=0.49)에
> **가장 낮은 뼈가 바닥에서 0.89m** 뜬다 — 몸이 통째로 난다. 배속 상한에 막혀
> 클립 앞부분만 재생되던 탓에 QA 창(1.5초) 안에 그 구간까지 가지도 못했다.
> 제대로 재생되기 시작하자 바로 드러났다. `walk_01` 과 함께 후보에서 뺐다.

### 걷기 변형이 둘뿐인데도 다르게 보이게 하기 (2026-08-07)

쓸 수 있는 걷기 클립이 `walk_02`, `walk_03` 둘뿐이라 무리가 **같은 동작을 같은
박자로** 했다. 클립을 더 받아 넣는 게 정공법이지만(아래 "다음 할 일"), 클립 수를
안 늘리고도 다르게 보이게 하는 값이 세 개 있다. 개체를 만들 때 한 번만 뽑아
평생 유지한다(`ZombieModel.makeInstance` → `Zombie._attachModel`).

| 변형 | 범위 | 무엇이 달라 보이는가 |
|---|---|---|
| **위상**(`phase`) | 0~1 | 클립 **시작 지점**을 개체마다 옮긴다. 같은 클립이어도 **발이 동시에 안 나간다.** 가장 효과가 크다 |
| **기울기**(`lean`) | ±0.055 rad | 몸통을 좌우로 살짝 기울인다. 실루엣이 달라진다 |
| **크기**(`sizeMul`) | 0.96~1.04 | 체격 차이. 멀리서도 무리가 균일해 보이지 않는다 |

> **위상은 클립을 갈아탈 때마다가 아니라 개체마다 고정이어야 한다.** 매번 새로
> 뽑으면 걷기↔달리기 전환에서 다리가 순간이동한다. `_updateAnim` 에서
> `next.time = this._phase * 클립길이` 로 **한 값을 계속 쓴다.**

- 여전히 **클립 자체를 늘리는 게 최선이다** — Mixamo "Zombie Walk" 계열을 2~3개 더
  받아 `fbx_src/walk_05.fbx` 식으로 넣으면, 원래 속도만 재서 `ANIM.clipSpeed` 에
  추가하면 끝난다(`tools/measure_contact.js` 의 `measureStride()`).

### 그 밖

- **넉백을 줄였다** (0.34 → 0.18m, 젖힘 0.26 → 0.15rad). 맞을 때마다 훌쩍 밀리니
  무게가 없어 보이고 연사하면 좀비가 뒤로 떠내려갔다. 타격감은 히트스톱·흔들림·피가
  만든다 — **밀림은 거들 뿐인데 키워 놓으면 그것만 눈에 띈다.** 젖힘을 줄이면
  "발이 바닥을 4~5cm 뚫는다"는 알려진 함정도 같이 작아진다.
- **시야를 확 돌릴 때 멈추는 증상** — three.js 는 재질이 **처음 화면에 들어오는
  프레임**에 셰이더를 컴파일한다. 그래서 새 물체가 시야에 들어오는 순간만 수십 ms
  튄다. 프레임 평균은 멀쩡해서 성능 문제로 오해하기 쉽다. 구역을 연 직후
  `renderer.compile()` 로 한 번에 굽는다(`Game._precompile`). **풀에 든 좀비는
  숨어 있어 `traverseVisible` 에 안 잡히므로 잠깐 보이게 해서 같이 굽는다** —
  안 그러면 첫 좀비가 나타나는 순간 똑같이 튄다.

---

## 기어다니는 좀비가 안 맞던 이유 (2026-08-07)

사격 판정이 **개체당 구 하나**였다. 서 있는 좀비는 그걸로 대충 맞지만,
기는 좀비는 **바닥에 길게 누워 있다.** 구 하나로는 어디를 노려도 어긋난다.

- 구가 몸을 덮을 만큼 크면 → **옆으로 빗나간 총알까지 맞는다**
- 몸에 맞을 만큼 작으면 → **머리나 다리를 정확히 쏴도 안 맞는다**

판정을 **캡슐**(선분 + 반지름)로 바꿨다(`WeaponSystem.capsuleOf` / `rayVsSegment`).
서 있으면 세로 캡슐, 기고 있으면 **바라보는 방향으로 누운 가로 캡슐**이다.
캡슐 길이·굵기·높이는 `balance.js` 의 `bodyLength / hitRadius / hitY` 에 있다.

덤으로 **헤드샷 판정이 자세를 따라간다.** 서 있으면 "키의 78% 위",
기고 있으면 **"캡슐 앞끝 26% 구간"** — 기는 좀비도 머리를 노릴 수 있다.

| 조준 지점 | 맞음 | 피해 |
|---|---|---|
| 기는 좀비 · 머리(앞끝) | ✅ | 22 (헤드샷) |
| 기는 좀비 · 몸통 | ✅ | 10 |
| 기는 좀비 · 다리(뒤끝) | ✅ | 10 |
| 기는 좀비 · 옆으로 0.5m | ❌ | 0 |
| 기는 좀비 · 머리 위 1m | ❌ | 0 |
| 서 있는 좀비 · 머리 | ✅ | 22 (헤드샷) |
| 서 있는 좀비 · 몸통 | ✅ | 10 |
| 옆으로 2m 빗맞힘 | ❌ | 0 |

---

## "좀비가 제대로 안 죽는다" 의 정체 — 근접이 각도에 죽고 있었다 (2026-08-07)

증상은 *"가끔 좀비가 제대로 안 죽는다"* 였다. 총은 멀쩡했다. **근접이었다.**

`WeaponSystem._swingMelee` 는 바닥에 눕힌 부채꼴로 판정한다(위에서 본 각도).
그런데 비교 대상인 `fwd` 가 카메라의 **3D** 전방이었다. 3D 전방은 고개를 숙이면
수평 길이가 `cos(고개각)` 으로 줄어든다. 그래서 **정면으로 딱 겨눠도 내적이 문턱을 못 넘는다.**
각도가 좁은 무기부터 먼저 죽는다:

| 무기 | 부채꼴 | 이 각도보다 숙이면 **무조건 빗나감** |
|---|---|---|
| 소방도끼 | 55° | **27.5°** |
| 쇠지렛대 | 60° | 30° |
| 쇠파이프 | 70° | 35° |
| 소화기 | 90° | 45° |

눈높이 1.55m 에서 **1.2m 앞의 기는 좀비는 이미 46° 아래**다.
즉 **기는 좀비는 근접으로 아예 죽일 수 없었다.** 어제 히트박스를 캡슐로 고친 건
레이(총) 쪽이라 이 문제를 못 건드렸다.

고침 — 고개각은 부채꼴 판정에 들어가면 안 된다. `fwd` 의 **수평 성분만 뽑아 정규화**한다.
발밑에 딱 붙은 개체(거리 0)는 방향이 무의미하므로 무조건 맞은 것으로 친다.

검증: 파이프·도끼 × 고개각 0/-17/-29/-40/-52/-63° × 기는 좀비·서 있는 좀비 = **18/18 명중**
(`scratchpad/qa/fix_check.mjs`).

---

## "1F 카드키가 가끔 안 나온다" 의 정체 — 안 나오는 게 아니라 안 보였다 (2026-08-07)

배치는 **매 판 완전히 똑같다.** 난수는 시드 고정(`_s = 4021`)이고, 카드키는 항상
복도 왼쪽 i=2 병실(검사대 위, `-5, 0.98, 15.5`)에 있으며 그 방의 문은 항상 `ajar`(열림)다.
스폰 실패가 아니었다.

진짜 원인은 **크기**다. 카드는 실물 크기 그대로 **8.5cm × 5.4cm × 5mm** 다.
자체발광을 켜 뒀지만 발광은 **면적에 비례**하므로, 면적이 작으면 밝기를 올려도
픽셀 수가 안 늘어난다. 3.6m 짜리 검사대 위의 몇 픽셀이라 손전등을 정확히 그 지점에
비추지 않으면 그냥 지나친다.

고친 것 둘 —
1. **후광**(`KEY_ITEM_GLOW`). 64px 캔버스에 그린 방사형 그라디언트 스프라이트를
   카드의 **자식**으로 붙였다. 자식이라 주우면 같이 사라진다(따로 지울 필요가 없다).
   깊이 판정을 그대로 쓰므로 **벽 뒤로는 안 비친다** — 방에 들어와야 보인다.
   0.5Hz 로 밝기가 숨쉰다. 가만히 있는 빛보다 흔들리는 빛을 눈이 먼저 잡는다.
2. **잠긴 문이 어디를 뒤져야 하는지 말한다** — "카드키가 필요하다 — 복도 중간, 왼쪽 병실 검사대".
   여기서 방향을 안 주면 복도를 몇 바퀴 돌다 게임이 막힌다.

---

## 좀비 본체 3종 (2026-08-07)

피드백은 "가운 입은 간호사 좀비가 너무 적다" 였다. **재 보니 비율 문제가 아니었다** —
가운 변형은 이미 75% 였다. 문제는 **실루엣**이었다. 기본 메시(ZombieGirl)는 크롭탑 +
반바지라, 텍스처를 아무리 희게 칠해도 "짧은 흰 상의 + 맨다리"로 읽힌다. 옷을 바꾼 게
아니라 **몸매를 흰색으로 칠한 것**이었다.

→ 옷을 **별도 메시**로 분리했다. `ZombieModel` 이 개체마다 본체를 골라 쓴다.

| 본체 | 정점 | 비율 | 파일 |
|---|---|---|---|
| 방호복 (후드·고글·니트릴 장갑) | 1,343 | **80%** | `zombie_nurse.glb` 3.78MB |
| 수술복 외과의 (수술모·마스크·찢긴 등) | 3,079 | 10% | `zombie_surgeon.glb` **0.69MB** |
| 기본 (ZombieGirl, 수술복 텍스처 / 원본) | 3,479 | 10% | `zombie_shambler.glb` 4.05MB |

실측(풀 20마리): 15 / 2 / 3. 요청한 8:2 가 유지된다.

### 세 번째 본체는 **이미 갖고 있었다**

`fbx_src` 의 With Skin 파일 안을 훑어 보니 서로 다른 캐릭터가 들어 있었다.
FBX 는 바이너리지만 오브젝트·재질 이름은 평문이라 Blender 없이 확인된다.

| 파일 | 들어 있는 몸 |
|---|---|
| `idle_03.fbx` | `ZombieGirl_Body` — 기본 메시 |
| `idle_04.fbx` | `Ch11` — **방호복과 같은 캐릭터.** 60MB 를 애니메이션 용도로만 쓰고 있었다 |
| `idle_05.fbx` | `Ch16_Body` — **수술복 외과의. 한 번도 안 쓰였다** |
| `idle_01.fbx` | `Beta_Joints` — Mixamo 기본 마네킹 |

**새 모델을 받기 전에 이미 받아 둔 것부터 열어 봐라.**

### 클립은 한 벌만 싣는다

본체를 늘릴 때마다 같은 24클립이 통째로 한 벌씩 더 실린다 — 본체당 **3.1MB** 다.
Mixamo 리그는 뼈 이름이 같으므로 three.js 가 이름으로 붙여 준다(PropertyBinding).
그래서 수술복은 **애니메이션을 아예 빼고 굽고** 방호복 것을 빌려 쓴다.

- `zombie_surgeon.glb` 3.78MB → **0.69MB**
- 앞으로 본체를 추가할 때도 한 종당 3.9MB 가 아니라 **약 0.7MB** 만 든다

> **빌려 쓰기 전에 뼈 이름 집합을 반드시 비교해라.** 방호복과 수술복은 뼈 65개가
> 완전히 일치해서 안전하다. **기본 메시만 혼자 다르다** — 눈뼈 2개가 더 있고
> 손가락 뼈 4개가 없다. 어긋난 채로 빌려 쓰면 three.js 가 `no target node found` 를
> **console.warn** 으로 뱉고, 이 프로젝트의 QA 는 그걸 실패로 센다.

### 근거리 노출

Mixamo 원본은 알베도가 밝아서 손전등을 가까이서 받으면 완전히 날아간다.
공격 사거리(1.9m)에서 찍어 보니 방호복은 **몸통 주름이 통째로 사라진 흰 덩어리**였다.
3.6m 에서는 멀쩡해서 놓치기 쉬운데, 정작 플레이어가 가장 자주 보는 거리가 1.9m 다.
알베도를 0.56 배로 낮추고 거칠기를 0.94 로 올렸다(`balance.js` 의 `ZOMBIE_LOOK`,
적용 대상은 `MODEL_FILES` 의 `tune` 표시로 정한다). 주름·솔기·후드가 되살아났다.

---

## 네 번째 본체와 새 클립 — Sketchfab 좀비 (2026-08-07 저녁)

사용자가 Sketchfab 좀비 4개를 받아 왔다(전부 CC BY 4.0, 제작자 표기 의무 → `ASSETS.md §4-C`).
FBX 안의 뼈 이름을 먼저 확인한 것이 전부를 갈랐다 — **Blender 없이 확인된다.**
FBX 는 바이너리지만 뼈 이름은 평문으로 박혀 있다.

| 원본 | 제작자 | 리그 | 결론 |
|---|---|---|---|
| Zombie Walk | OSCAR CREATIVO | `mixamorig:` 66개 | 클립 `walk_05` 로 바로 합침 |
| Zombie Crawl | Beer Game Maker | `mixamorig:` 66개 | 클립 `crawl_02` 로 바로 합침 |
| Animated Zombie Cop | LasquetiSpice | **접두어만 빠진** Mixamo 65개 | 이름 붙여서 **네 번째 본체** |
| Sexy Zombie Girl | doublesob | **뼈 없음**(정적 메시 150k tri) | 좀비 불가 → **소품**으로 |

### 경찰 좀비 — 접두어 하나가 전부였다

원본 뼈는 `Hips` `LeftUpLeg` `RightForeArm` … Mixamo 이름 그대로인데 `mixamorig:`
네임스페이스만 빠져 있었다. three.js 는 클립을 **노드 이름**으로 붙이므로 한 글자만
달라도 T포즈로 선다. 그래서 `fbx_to_glb.py` 에 `normalize_bone_names()` 를 넣어
접두어를 붙였다. **버텍스 그룹도 같이 바꿔야 한다** — 안 바꾸면 스키닝이 끊겨
메시가 원점에 뭉친다.

결과: 뼈 65개가 방호복과 **하나도 안 틀리고** 일치. 클립 27개를 그대로 빌려 쓰고
(`clipsFrom: 'nurse'`) 파일은 **0.92MB** 다. `no target node found` 경고 0건.
흰 가운 보정(`tune`)은 안 건다 — 어두운 제복이라 더 누르면 검은 덩어리가 된다.

무리 비율: 방호복 80% · **경찰 8%** · 외과의 8% · 기본 4% · 수술복 4%.

### 새 걷기 클립은 재기 전에 넣지 마라

`walk_01`(보폭 0.06m/s) 과 `walk_04`(중간에 0.89m 공중부양) 를 뺐던 이유가 그대로 살아 있다.
`walk_05` 도 같은 잣대로 쟀다 — 4.07초 / 보폭 0.87m / **원래 속도 0.43m/s** ·
클립 전체에서 가장 낮은 뼈 **0.057m**(기존 walk_02 는 0.066, walk_03 은 0.088).
기존 것보다 오히려 낫다. `ANIM.clipSpeed.walk_05 = 0.43` 으로 등록.
`crawl_02` 는 최저 뼈 0.665m 로 `crawl`(0.603)보다 6cm 높지만, 포복 동작 자체가
한 주기에 0.60→0.84m 를 오르내리므로 그 범위 안이라 뜬 것처럼 안 보인다.

### 서 있는 시신 (`prop_standing_body`)

뼈가 없어 좀비로는 못 쓰는 모델을 **첫 병실(오른쪽 i=0) 구석에 벽 보고 세웠다.**
150,000 → 5,000 삼각형, 0.31MB. 그 방은 조명이 깜빡이는 방이라 그림자가 흔들려서
살아 있는 것처럼 보인다. **쏴 봐도 아무 일도 안 일어나는 침묵**이 이 연출의 전부다.

> `tools/import_props.py` 는 이제 **FBX 도 받는다**(`import_glb` 가 확장자로 분기).

---

## 자동 QA (2026-08-06 기준 / 2026-08-07 재확인)

`SETUP.md §4` 의 콘솔 검사 두 종을 **헤드리스 크롬으로 반복 실행**해서 간헐 결함을 잡는다.
한 번만 돌리면 통과하는데 30번 돌리면 2~4번 실패하는 것들이 실제로 있었다.

한 회차에서 보는 것: 구역 배치 125항목 · 좀비 모션 1482프레임 · **해상도연동 회귀** ·
콘솔 에러 0 · 네트워크 실패 0.

**최종 30회 전부 통과 (30/30).** 세션 시작 시점은 20회 중 18회였고, 실패 2회는
게임이 아니라 도구 오탐이었다. 회당 약 40초(헤드리스 소프트웨어 렌더 기준).

**2026-08-07 저녁 · 경찰 본체 + 새 클립 2개 반영 후 8/8 통과.**
구역 배치 125/125 · 모션 1482프레임 전부 정상 · `no target node found` 0건 · 네트워크 실패 0.

> **로딩 시간은 반드시 `npm run build` + `vite preview` 로 재라.** 개발 서버(vite dev)로 재면
> 모듈 변환 때문에 8~10초가 나와서 **예산(8초)을 넘긴 것처럼 보인다.** 배포 빌드로 재면
> **4.67초**(4회 평균, 캐시 끄고 20.1MB 전송)로 여유가 있다. 도구: `scratchpad/qa/load_time.mjs`.

**2026-08-07 방호복 메시 + 영안실 소품 반영 후 20/20 통과.** 로딩 평균 5.32초
(GLB 7MB 가 늘었는데도 예산 8초 안). 모션 1482프레임 전부 정상이라
방호복 메시의 접지·오프셋도 문제없다.

> **하네스가 `console.warn` 을 놓치고 있었다.** 퍼펫티어가 주는 타입은 `'warning'` 이
> 아니라 **`'warn'`** 이다. 이 프로젝트는 에셋 로드 실패를 전부 `console.warn` 으로
> 처리하는(폴백 설계) 데다, vite 개발 서버는 없는 에셋에 404 대신 index.html 을 **200**
> 으로 준다. 둘이 겹쳐서 **에셋이 통째로 빠져도 QA 가 통과했다.** 필터를 고쳤다 —
> 이제 방호복 GLB 를 지우면 QA 가 즉시 실패한다.

이번 세션에서 **QA 가 잡아낸 것** (전부 수정 완료):

| 무엇 | 정체 |
|---|---|
| 콘솔 에러 2건/로드 | `weapon_axe.glb`·`weapon_molotov.glb` 가 저장소에 없는 아틀라스 `Textures/colormap.png` 를 참조. GLB 안의 텍스처 참조를 제거했다 (색은 원래도 안 붙었고 `colorMul` 이 그 상태 기준이라 화면은 그대로) |
| favicon 404 | `index.html` 에 인라인 SVG 로 넣었다. 이제 콘솔이 완전히 비어야 정상 |
| 청각체가 슬로모션으로 걷는다 | `Zombie.js` 에서 `_jitter` 를 `clamp` **바깥**에서 곱해, 하한 0.4 를 걸어 놓고 0.92 를 곱해 **0.37 배속**이 나왔다. 공격 클립도 같은 방식으로 상한 2.0 을 넘었다. 지터를 clamp 안으로 넣고 수치는 `ANIM.moveMinSpeed/moveMaxSpeed` 로 옮겼다 |
| 화염병이 게임에 없다 | `weapons.js` 에 정의만 있고 어느 스테이지에도 배치가 없었다. 게다가 `_throw()` 에 불꽃·화상(dot)·투사체가 없어 "보이지 않는 광역 피해"였다. **컷했다** (되살리려면 연출부터) |

**도구 자체의 오탐 2건도 고쳤다** — 이쪽이 더 무섭다. 멀쩡한 것을 결함이라고 외치면
진짜 결함을 못 믿게 된다.
- 포복체가 비명 클립에 들어가면 허용치가 0.952 → 0.616 으로 **오히려 좁아졌다**
  (삼항으로 하나만 골라서). 둘 다 "더 떠도 정상"인 사유이므로 느슨한 쪽을 쓰게 고쳤다.
- 피격 중 몸이 젖혀지면 발이 내려가는데(위 함정 참고) 고정 허용치로 재고 있었다.
  **그 순간의 실제 기울기에서 허용치를 유도**하도록 고쳤다 (상수를 늘린 것이 아니다).

> **QA 를 돌리는 동안 소스를 편집하지 마라.** Vite 가 핫리로드로 페이지를 갈아치워서
> `Execution context was destroyed` 로 회차가 통째로 날아간다. 한 번 밟았다.

---

## 배포본 40회 × 5층 순회 검증 (2026-08-08)

로컬이 아니라 **실제로 올라가 있는 사이트**(`be11562`)에 붙어서 돌렸다.
`scratchpad/qa/sweep_live.mjs`. 배포본에는 `node_modules` 도 `src/config/*.js` 도 없으므로
두 가지를 바꿨다 — THREE 는 로컬 파일을 **Blob 모듈로 주입**하고, 설정값은 import 하지 않고
**실측한다**(배터리는 손전등을 켜고 1초 돌려서 잰다). 번들된 값을 직접 재는 쪽이 오히려 맞다.

| 층 | 도달 칸 | 최소통로 | 드로우콜 | 삼각형 | 시체 | 배터리 실측 |
|---|---|---|---|---|---|---|
| 1F 격리병동 | 55817 | 1.15m | 109/300 | 213k/350k | 40구 불량 0 | 0.75/초 → 133초 |
| B1 영안실 | 51456 | 2.45m | 81/300 | 149k/350k | 40구 불량 0 | 동일 |
| 2F 중환자실 | 60934 | 1.45m | 109/300 | 266k/350k | 40구 불량 0 | 동일 |
| 3F 수술부 | 45405 | 2.95m | 103/300 | 163k/350k | 40구 불량 0 | 동일 |
| 옥상 | 133926 | 4.05m | 55/300 | 76k/350k | 40구 불량 0 | 동일 |

콘솔 에러·요청 실패·4xx **0건**. 시체 200구 전부 정상.
로컬 수치와 자리수까지 같다 — 배포된 월드가 소스와 같다는 확인도 된다.

### 진짜 결함 1건 — 멈춘 포복체의 무릎 아래가 바닥에 잠겼다

5회 검출(1F 2 · B1 1 · 2F 2). 가장 낮은 뼈 **-0.13m**, 클립 `crawl_idle`, 상태 WANDER.

`crawl_idle` 은 **무릎 꿇고 고개 숙인 자세**다. 엎드린 `crawl_02` 와 골반 높이는 같은데
다리가 20cm 아래로 내려간다. `modelYOffset(-0.62)` 은 엎드린 자세에 맞춘 값이라
멈추는 순간 무릎 아래가 통째로 바닥 밑으로 들어갔다 — **24프레임 전부 잠김**
(`scratchpad/qa/crawl_idle_detail.mjs`). 옆에서 찍어 보니 다리가 없는 몸통이었다.

> 오프셋을 올려서 못 고친다. `crawl_idle` 을 살리면 `crawl_02` 가 뜬다.
> **두 클립을 한 값으로 맞출 수 없다.** 게다가 자세가 아예 달라서, 높이를 맞춰도
> 멈출 때마다 엎드림 → 무릎 꿇기로 튄다.

그래서 **자세를 하나로 통일**했다. 멈춘 포복체도 `crawl` 을 쓰고,
`ANIM.crawlerIdleSpeed`(0.35)로 느리게 돌려 제자리에서 꿈틀거리게 한다.
검증: 10마리 × 300프레임 실제 상태머신 → 가장 낮은 뼈 **+0.09m**, 뚫은 프레임 **0/2800**
(`scratchpad/qa/crawl_verify.mjs`). 자동 QA 6/6 통과.

### 오탐 25건 — "사거리 안인데 데미지가 안 들어간다"

전부 하네스 탓이었다. 이 스윕은 좀비를 **무조건 플레이어 +z 1.5m** 에 세우는데,
그 자리가 벽 반대편일 때가 있다. `WeaponSystem._swingMelee` 는 `segmentBlocked` 로
벽 너머 타격을 막는다 — **설계대로 동작한 것이다.**

원인을 넷으로 쪼개서 세었다 (`scratchpad/qa/melee_why.mjs`):
**벽 7 · 부채꼴 0 · 사거리 0 · 스윙예약실패 0.** 하나도 게임 결함이 아니다.
스윕에 `벽너머` 카운터를 넣어 이제 결함이 아니라 통계로 뺀다.

> 하네스가 "실패"라고 말할 때 **왜 실패했는지를 코드 경로로 쪼개서** 세어야 한다.
> 숫자만 보면 25건짜리 대형 결함으로 보인다.

### 고친 뒤 배포본 재검증 (`4600ef6`) — 5층 전부 결함 0

같은 40회 × 5층을 새 배포본에 다시 돌렸다.

```
1F  시체 40구(불량 0) · 타격 21회(무효 0) · 결함 0건
B1  시체 40구(불량 0) · 타격 25회(무효 0) · 결함 0건
2F  시체 40구(불량 0) · 타격 18회(무효 0) · 결함 0건
3F  시체 40구(불량 0) · 타격 24회(무효 0) · 결함 0건
옥상 시체 40구(불량 0) · 타격 26회(무효 0) · 결함 0건
총 결함 0건 · 잡이슈(콘솔·네트워크) 0건
```

포복체 바닥 잠김 5건 → **0건**. 벽 너머를 뺀 유효 타격 **114회 전부 명중**
(무효 0). 드로우콜 55~109/300 · 삼각형 76k~264k/350k · 배터리 실측 0.75/초.

---

## 알려진 함정

### 성능·렌더 (2026-08-06 추가)
- **후처리를 쓰면 `renderer.setPixelRatio()` 만으로는 해상도가 안 바뀐다.**
  `EffectComposer` 가 자기 `_pixelRatio` 를 따로 들고 있다. `PostFX.setSize` 가
  `composer.setPixelRatio()` 를 같이 부르게 해 뒀다. **후처리 체인을 손대면 이걸 깨기 쉽다** —
  화면으로는 절대 안 보이고 프레임만 조용히 무너진다. `tools/qa_stages.js` 가 아니라
  QA 하네스의 "해상도연동" 검사가 이걸 지킨다.
- **프레임을 잴 때는 vsync 를 꺼라.** 안 끄면 16.7ms 아래가 전부 16.7 로 읽혀서
  무엇이 비싼지 못 가린다. `--disable-gpu-vsync --disable-frame-rate-limit`.
- **적응형 예산은 vsync 기준으로 읽어야 한다.** 12ms 에 그려도 60Hz 에서는 간격이
  16.7ms 로 측정된다. 예산을 16.5 로 잡으면 "항상 초과"가 되어 해상도가 바닥까지 내려간다.
- 피격 중 발이 바닥을 **4~5cm 뚫는다.** `KNOCK.bend`(0.26rad)로 몸을 발치 기준으로
  젖히는 연출의 기하학적 결과다(sin(0.26) x 0.19m). 어두운 바닥이라 눈에 잘 안 띄어
  그대로 뒀다. 거슬리면 젖히는 동안 group 을 그만큼 들어 올리면 되지만, 그러면
  맞을 때마다 살짝 뛰어오르는 것처럼 보인다. **트레이드오프를 보고 정할 것.**
  (2026-08-08: 반동의 대부분을 group 회전에서 **뼈**로 옮겼다 — `KNOCK.groupBend`
  0.03 / `boneBend` 0.34. 발이 딸려 내려가는 통로가 group 회전 하나뿐이라 이걸로
  기하학적 원인이 거의 사라졌다. 위 함정은 `groupBend` 를 다시 키울 때만 돌아온다.)

- **스폰 직후 발이 바닥을 최대 10cm 뚫던 것** (2026-08-08 해결, `ANIM.spawnFade: 0`).
  원인이 걷기 클립도 `_phase` 도 아니었다 — **첫 클립의 페이드인**이다.
  three 는 액션 가중치 합이 1 미만이면 나머지를 **바인드 포즈(T포즈)** 로 채운다.
  믹서가 빈 상태(스폰 직후)에서 0.22초 페이드인을 걸면 그동안 화면에 나오는 것은
  걷기가 아니라 "T포즈와 걷기를 섞은 자세" 이고, `_phase` 가 어디 떨어지느냐에 따라
  그 중간 자세의 발이 바닥 아래로 내려간다. 개체마다 `_phase` 가 달라서 **20개체 중
  2~4개만** 그랬고, QA 가 어느 개체를 뽑느냐에 따라 회차마다 나타났다 사라졌다 했다.
  → 섞을 이전 자세가 없을 때(`curAnim` 이 없을 때)는 페이드 없이 전체 가중치로 튼다.
  **교훈: "회차마다 다르다"는 난수가 아니라 개체별 상태일 수 있다.** 풀 전체를 하나씩
  훑어보면 바로 드러난다 (`scratchpad/qa/floor_probe.mjs`, 4건 → 0건).

### 성능·렌더 (2026-08-08 추가)
- **three 의 셰이더 프로그램 캐시 키에 광원 개수가 들어간다** (`numPointLights`,
  three.module.js 의 `getProgramCacheKey`). 구역마다 비상등이 12 → 6 → 14 → 9 → 5 로
  바뀌던 탓에 **구역을 옮길 때마다 씬의 재질이 통째로 재컴파일**됐다 —
  프로그램 누적 68 → 104 → 138 → 177 → 200. `Atmosphere` 를 고정 슬롯
  (`FX.emergencyLightSlots`, 6개)으로 바꿔 개수를 붙박으니 66 → 74 → 78 → 82 → 82 가 됐다.
  **광원을 씬에 직접 add 하는 코드를 다시 넣으면 이게 그대로 돌아온다.**
- 안 쓰는 슬롯은 `visible = false` 가 아니라 **intensity 0** 이어야 한다.
  three 는 `visible` 로 광원을 세므로 끄면 개수가 다시 흔들린다.
- **전방 렌더라 셰이더가 픽셀마다 광원 전부를 돈다.** 2F 병동은 광원이 14개인데
  한 지점에서 사거리(7m) 안에 실제로 드는 것은 **최대 4개**였다(전 구역 격자 표본).
  나머지는 매 픽셀 계산되고 0 을 더한다. 14 → 5 로 줄이면 GPU 프레임의 15% 가 빠졌다.
- **화면 비교를 할 때는 필름 그레인을 반드시 꺼라.** `rand(uv * fract(uTime))` 이라
  프레임마다 픽셀이 바뀐다. 안 끄고 재면 슬롯 4개와 8개가 **같은 차이값**으로 나온다 —
  차이의 전부가 그레인이기 때문이다. 실제로 그렇게 나와서 한 번 헛짚었다.
- **A/B 는 같은 페이지 안에서 해라.** HEAD 와 새 코드를 각각 띄워 밝기를 비교했더니
  B1 이 0.064 vs 2.594(40배)로 나왔다. 최적화가 화면을 40배 밝게 만들 수는 없다 —
  두 실행 사이에서 깜빡임 난수·컴파일 시점이 달라진 것이었다. 슬롯 배열만 늘렸다
  줄이는 방식으로 바꾸니 평균차 0.002 가 나왔다 (`scratchpad/qa/slot_ab.mjs`).

### 충돌 박스에 높이가 없어서 무릎 높이 소품이 총알을 막았다 (2026-08-09 해결)

`Collision.addBox` 는 `{minX,maxX,minZ,maxZ}` 만 저장했다. **높이 개념이 아예 없었다.**
그래서 병상·벤치·넘어진 자판기처럼 넘어다 볼 수 있는 물건이 전부 **천장까지 닿는
보이지 않는 벽**이었다. 눈에는 그 너머 좀비가 훤히 보이는데 쏘면 안 맞는다
(`_hitscan` 이 XZ 선분 하나로 시야를 판정한다).

옥상에 소품을 눕히자 이게 노골적으로 드러났다 — 0.9m 짜리 넘어진 자판기 뒤가
사각지대가 됐다. 원래 "낮은 소품에는 충돌을 걸지 마라"로 회피하고 있었는데,
그러면 이번엔 눕힌 물건을 **뚫고 지나간다.** 회피가 안 되는 지점에 왔다.

**박스에 윗면 높이를 넣었다.** `addBox(cx, cz, w, d, h = Infinity)` —
기본값이 무한이라 **안 넘기면 예전과 완전히 같다.** 높이를 보는 건 총알 하나뿐이다:

- `isBlocked(x, z, r, y)` · `segmentBlocked(..., y)` 에 선택 인자 `y` 를 뒀다.
  `y` 보다 낮은 박스는 무시한다. 안 주면 전부 막는다 → **플레이어 이동·좀비 경로·
  소리 차폐는 한 줄도 안 바뀐다** (넘어갈 수단이 없으니 이동은 계속 막혀야 맞다).
- `_hitscan` 은 **사선의 가장 낮은 지점**(총구 y 와 명중점 y 중 낮은 쪽)을 넘긴다.
  낮은 쪽을 쓰는 게 안전한 방향이다 — 막혀야 하는 사격을 통과시키지 않는다.
- **근접은 일부러 2D 로 뒀다.** 무릎 높이 잔해에 파이프가 부딪히는 건 맞는 동작이다.

높이는 **손으로 안 적는다.** 소품을 만들면서 잰다 —
`addProp3D` 는 회전·이동을 다 적용한 지오메트리의 `boundingBox.max.y` 를,
`addPropGLB` 는 `PropModels.topY(name)`(프로토 파트 행렬까지 반영, 캐시)를 쓴다.
눕히면서 중심이 틀어지는 경우를 위해 `collideAt [dx,dz]`, 예외용 `collideH` 도 있다.

실측 (`scratchpad/shootover.mjs` — 실제 `_hitscan` 을 부르고 좀비 HP 변화로 판정):

| 사이에 놓인 것 | 표적 | 기대 | 결과 |
|---|---|---|---|
| 넘어진 자판기 0.9m | 서 있는 좀비 | 명중 | 명중 |
| 벤치 0.5m | 서 있는 좀비 | 명중 | 명중 |
| 세운 자판기 1.85m | 서 있는 좀비 | 막힘 | 막힘 |
| 벽(무한) | 서 있는 좀비 | 막힘 | 막힘 |
| 넘어진 자판기 0.9m | **바닥의 포복체** | 막힘 | 막힘 |

전 층에서 **총알이 넘어갈 수 있게 된 박스 34개** (1F 6 · B1 7 · 2F 5 · 3F 10 · 옥상 6,
가장 낮은 것 0.19m — `scratchpad/boxcensus.mjs`).

**아직 남은 것**: 옥상은 천장이 없는데 `WALL_IMPACT.ceilingHeight` 가 전역 상수라
3.1m 상공에 콘크리트 탄흔이 생긴다. 빗맞혔을 때만 나오는 0.35초짜리 회색 입자라
마감 뒤로 미뤘다. 좀비 시야·소리 차폐도 여전히 2D 다 — 낮은 소품 뒤에 숨어도
좀비가 본다. 이건 **난이도상 오히려 안전한 방향**이라 건드리지 않았다.

### 게임
- 브라우저는 **사용자 클릭 전에 소리를 못 낸다.** 시작 화면 클릭 시점에 오디오를 초기화해야 한다.
- 포인터락은 `Esc` 로 풀린다. 풀리면 일시정지로 들어가게 되어 있다.
- 좀비 렌더가 프레임의 대부분을 먹는다. 14마리가 하드캡이다.
- 좀비 추격은 NavMesh 없이 **직선 + 3방향 레이 회피**다. 15초 이상 접근 못 하면 자동 반납.
- **스폰 지점은 플레이어에게서 12m 이상 떨어져 있어야 쓰인다**(`DIRECTOR.spawnMinDistance`).
  옥상 헬리패드 둘레 3곳은 중심에서 6~7m 라 **한 번도 안 쓰였고**, 패드에 서면 남쪽 2곳에서만
  일렬로 들어왔다. 둘레 13~15m 에 링을 둘러 사방에서 오게 고쳤다(방향별 유효 지점 2 → 5~6).
  **새 구역에 스폰을 놓을 때는 "플레이어가 오래 머무는 자리에서 12~34m 인가"를 먼저 재라.**
  재는 법: `game.director.spawnPoints` 와 `game.collision.segmentBlocked/isBlocked` 로 직접 계산.
- **이벤트 웨이브는 예정 수보다 훨씬 많이 나온다.** `triggerWave` 가 phase 를 PEAK 로 돌리므로
  그동안 Director 가 목표치(12)까지 따로 채운다. 옥상은 예정 35 → 실제 45~65.
  사람 없이 재려면 가짜 pool(`{activeCount, spawn, broadcastNoise}`)로 Director 를 돌린다.
- `Director.triggerWave(n)` 은 타입을 안 주면 **구역 typeWeights** 를 따른다.
  예전에는 'shambler' 로 고정돼 있어서 청각체·포복체를 등록해도 이벤트 웨이브가 전부 배회체였다.
- 포복체는 **몸집이 작은 게 아니라 엎드린 것**이다. `height` 로 줄이면 미니어처가 된다 →
  `modelScale`(몸집)과 `height`(피격 판정)를 따로 둔다. 루트 수직 이동을 지운 탓에
  엎드린 클립을 쓰면 뜨므로 `modelYOffset`(-0.62)로 내린다.
- `Collision.segmentBlocked` 의 5번째 인자는 반지름이 아니라 **샘플 간격**이다.
- **`Zombie._syncMesh(dt)` 에 dt 를 안 주면 회전이 즉시 맞춰진다.** 스폰 직후에는 그게 맞고
  (도는 모습이 보이면 안 된다), 매 프레임 호출에서는 반드시 dt 를 넘겨야 한다.
- 좀비 모션을 손으로 확인하려면 `z.facing` 을 반대로 준 뒤 `z._syncMesh(1/60)` 을 여러 번
  부르고 `z.group.rotation.y` 를 찍는다. 공격 선딜은 `z._startChase()` 로 붙여 놓고
  `pool.update` 를 돌리며 hp 가 떨어지는 시점을 본다 — **플레이어 무적시간(0.45초)은
  `player.update` 를 같이 안 돌리면 안 풀려서 두 번째 타격이 영영 안 온다.**
- 수치를 바꾸려면 `src/config/balance.js` 와 `src/config/weapons.js` **만** 열면 된다.
- **GLB 변환 시 본체 파일(`idle_03.fbx`)을 반드시 인자로 준다.** 생략하거나 다른 파일을 주면
  Mixamo 기본 마네킹(`Beta_Joints`)이 몸이 되어 **좀비가 통째로 회색 인형으로 바뀐다.**
  클립 수·용량이 정상이라 GLB 를 열어 보기 전까지 티가 안 난다. 확인:
  `python -c "...json...['meshes']"` 로 메시 이름이 `ZombieGirl_Body` 인지 본다. (ASSETS.md §6-3-D)
- **`img.has_data` 로 텍스처를 거르면 대부분이 조용히 안 줄어든다.** 팩된(FBX 내장)
  이미지는 **한 번 건드리기 전까지 False** 다. `fbx_to_glb.py` 가 이걸로 필터하고 있어서
  방호복 GLB 에 **4096 노멀맵·광택맵이 그대로 실려 3MB 를 더 먹었다.** 로그에는
  "축소 3장"만 찍혀서 9장 중 6장이 빠진 걸 못 알아봤다 — 같은 함정을 `import_props.py`
  는 이미 겪고 고쳐 놨었는데 좀비 변환기만 옛날 코드였다. 지금은 `img.pixels[0]` 로
  강제 로드하고 **몇 장 줄였는지 총계를 찍는다.** 조용히 건너뛰는 것이 이 함수의
  유일한 실패 방식이므로 총계가 없으면 또 못 잡는다.
- **데시메이트를 모디파이어로 남겨 두면 변환이 안 끝난다.** glTF 익스포터는 애니메이션을
  구울 때 **프레임마다 depsgraph 를 다시 평가**한다. 데시메이트가 모디파이어로 걸려 있으면
  그때마다 원본(5만 삼각형)을 다시 깎는다 — 클립 24개면 수천 번이다. 실제로 방호복 변환이
  **66분을 넘겨도 안 끝났고**, 익스포트 직전에 확정 적용(`modifier_apply`)하도록 고치니
  **1분 30초**에 끝났다. 기본 메시는 삼각형이 적어 데시메이트가 아예 안 걸렸기 때문에
  이 함정이 지금까지 드러나지 않았다. (`tools/fbx_to_glb.py`)
- **본체가 아닌 With Skin FBX 는 변환에서 빼라.** `idle_03/04/05` 는 With Skin 으로
  받아져서(§6-2 위반) 합계 127MB 다. 애니메이션만 필요한데 4096² 텍스처까지 끌고 들어와
  임포트 단계를 몇 배로 늘린다. 빼도 idle 변형이 5종 → 2종으로 줄 뿐이다
  (`ZombieModel.pick()` 이 **있는 클립만 고르므로** 부분 집합이어도 안전하다).
- **오래 걸리는 변환을 `Select-Object -Last N` 에 파이프하지 마라.** 출력이 파이프에
  갇혀 끝날 때까지 한 줄도 안 보인다. 진행 중인지 멈춘 건지 구분이 안 돼서
  66분짜리 실패를 66분 동안 못 알아봤다. 로그 파일로 리다이렉트하고 tail 로 본다.
- **Mixamo 팩(Pack)으로 받으면 안 된다.** 변환기는 FBX 하나당 애니메이션 하나를 전제하고
  파일명을 클립 이름으로 쓴다. 팩은 내부 take 이름이 전부 `mixamo.com` 이라 쪼개도 못 살린다.
- **`addSign(slot, ...)` 의 slot 은 아틀라스 칸 번호다 — 뜻이 있는 번호다.**
  칸 목록은 `tools/gen_signage.py` 의 `CELLS` 배열이 원본이다. 번호를 잘못 주면
  **2F 병실 문마다 "영안실", 3F 수술실에 "기계실 B1"** 이 붙는다(실제로 그랬다).
  0~2 일반병실 · 3 처치실 · 4 격리A · 5 간호사실 · 6 물품보관 · 7 소독실 ·
  8 검역경고 · 9 예방수칙 · 10 층안내도 · 11 방향표지 · 12 비상구 · 13 찢어진공고 ·
  14 영안실 · 15 기계실B1.
  `roll`(기울임)·`pitch`(눕힘) 인자로 끊어진·떨어진 표지판을 만든다. `glow` 를 끄면 죽은 비상구 등.
- **에셋을 AudioManager 에 등록만 해도 게임에 없는 것이다.** `zombie_notice.mp3` 는 등록만 되고
  두 세션 동안 한 번도 재생되지 않았다. `weapons.js` 의 무기 6종도 같은 상태였다.
  새 에셋을 넣었으면 **호출자가 있는지 grep 으로 확인**한다.
- 화면 촬영 시 **스크린샷 도구는 뷰포트 전체를 안 잡는다**(898px 중 상단 773px). 하단 HUD 를
  확인하려면 `getBoundingClientRect()` 로 좌표를 재거나, 잠깐 위로 올려서 찍는다.
- **`weapons.js` 에 정의만 하면 게임에 없는 것이다.** 스테이지에서 `addWeapon(x,z,id,label)`
  으로 놓아야 플레이어가 얻는다. 쇠지렛대는 GLB 까지 만들어 놓고 두 세션 동안 죽어 있었다.
- 스테이지 파일은 ctx 를 **구조분해**해서 쓴다. 새 헬퍼를 StageLoader 에 추가하면
  쓰는 스테이지 파일 맨 위 구조분해 목록에도 **같이 넣어야 한다** (안 넣으면 `is not defined`).

### 소품 GLB 파이프라인 (`tools/import_props.py`) — 같은 함정을 또 밟지 마라
- **부모(RootNode 등 빈 오브젝트)를 지우기 전에 `parent_clear(KEEP_TRANSFORM)` 을 먼저 부른다.**
  Sketchfab GLB 는 축 변환과 스케일이 부모에 걸려 있어서, 순서를 어기면 모델이 눕거나
  엉뚱한 크기가 된다. 원본은 멀쩡한데 결과만 이상해지므로 원인을 찾기 어렵다.
- **1바이트/픽셀(팔레트·그레이) 이미지는 WebP 로 내보내기가 조용히 실패한다.**
  텍스처 항목만 남고 이미지가 빠져서 three.js 가 파일 전체를 못 읽는다
  (`Cannot read properties of undefined (reading 'uri')`). 그래서 내보내기 전에
  모든 이미지를 RGBA 로 다시 만든다(`normalize_images`). `img.depth` 는 믿을 수 없다.
- **`img.has_data` 는 이미지를 한 번 건드리기 전까지 False 다.** 이걸로 거르면 전부 건너뛴다.
- `bpy.ops.object.transform_apply(rotation=True)` 는 백그라운드에서 조용히 안 먹을 때가 있다.
  메시 데이터를 직접 돌린 뒤 `bpy.context.view_layer.update()` 로 `dimensions` 를 갱신한다.
- **미리보기 렌더는 모델당 프로세스를 새로 띄운다.** 한 프로세스에서 여러 개를 렌더하면
  오프스크린 버퍼가 오염돼 화면이 노이즈로 깨진다.
- 소품 GLB 는 스테이지 조립이 동기라서 **`Game.start()` 에서 전부 프리로드**한다.
  그래서 시작 무기는 Game 생성 시점에 GLB 가 없다 → `weapons.refreshViewModel()` 로 다시 만든다.

### 시각 품질
- **얇은 소품에 텍스처를 넣을 때는 UV 를 면 방향별로 잡아야 한다.** 가장 긴 변 하나로
  U·V 를 같이 스케일하면 짧은 쪽 면이 4cm 마다 반복돼 물결무늬가 어른거린다.
- **환경맵 없이 금속도를 올리면 새까맣게 죽는다.** 반사할 것이 없기 때문. 반대로
  환경맵을 세게 넣으면 어둠이 통째로 걷힌다 — 0.16 정도가 경계다.
- **후처리 AO(GTAO)는 이 게임에 못 쓴다.** 14.5ms → 30.2ms. 해상도를 절반으로 낮춰도
  29.5ms 라 거의 안 줄어든다 — 비용이 해상도가 아니라 깊이·노멀 프리패스(씬을 한 번 더
  그린다)에 있고, 스킨 메시 14개가 두 번 렌더되는 순간 예산이 깨진다. `POST.ao` 로 남겨 뒀다.
- **프레임 측정의 첫 값은 버려야 한다.** 구역을 갈아치운 직후에는 텍스처·셰이더를
  GPU 에 다시 올리는 시간이 섞여 몇 배로 튄다(62.5ms → 재측정 16.3ms). `tools/bench.js`
  가 내부에서 한 번 버리고 두 번째를 돌려준다. 벽시계 대신 GPU 타이머를 쓰면 편차 2%.
- **프레임 측정은 백그라운드 탭에서 심하게 흔들린다.** 한 번 재고 판단하면 오진한다
  (18.2ms 로 나왔던 것이 재측정 시 13.1ms). 워밍업 40프레임 뒤 중앙값을 본다.
- **사망 클립은 루트 수직 이동이 지워져 있어 시체가 공중에 뜬다.** 쓰러진 뒤
  가장 낮은 뼈의 월드 Y 를 재서 바닥에 붙인다(`Zombie._groundOffset`). Box3.setFromObject
  는 스킨 메시의 실제 자세를 반영하지 않아 못 쓴다. 1.2초 만에 사라지던 때는 안 보였다.
- **원뿔 메시로 빛기둥을 그리면 안 된다.** 안에서 보면 원뿔 벽면이 통째로 빛나서
  즉시 가짜로 보인다(시선이 벽면과 나란할수록 밝아진다). 입자를 쓴다.

### 타이틀 화면 (index.html)
- **`::before` 는 생성 순서상 첫 자식이라, 뒤에 오는 절대배치 형제(`.art`)가 그 위에 그려진다.**
  스크림이 안 먹는데 원인을 못 찾으면 이것이다. `#title` 의 겹은 z-index 로 못 박아 뒀다
  (art 0 · grade 1 · 스크림 2 · 활자 3 · 그레인 4 · 암전 5).
- **한 요소에 transform 을 쓰는 애니메이션을 두 개 걸면 뒤엣것이 앞엣것을 덮는다.**
  그래서 시차(.artwrap) · 달리인(.art) · 빛무리(.lamp)를 각각 다른 겹에 두었다.
  filter 도 통째로 교체되므로 깜빡임 키프레임마다 `var(--g)`(그레이딩)를 다시 써야 한다.
- 키아트는 **원본을 그대로 쓰지 않는다.** 맑은 실사 사진이라 그냥 깔면 포스터가 아니라
  "다른 게임의 스크린샷"으로 읽힌다. 채도·대비·미세흐림+인쇄 그레인은 전부 CSS 에 있다.

### 검증
- **브라우저 탭이 백그라운드면 `requestAnimationFrame` 이 안 돈다.** 게임 루프가 멈추므로
  좀비가 안 나오고 상태가 안 변한다. 게임 버그로 오해하기 쉽다.
  루프 없이 확인하려면 `director.update(1/60)` 을 직접 여러 번 부른다.
- 스크린샷 절차: `btn-start` 클릭 → **`game.state='PAUSED'`** → `.screen` 숨김 →
  카메라 직접 세팅(`rotation.order='YXZ'`) → `flashlight.update(dt)` → `post.render(dt)`.

---

## 도구

| 명령 | 하는 일 |
|---|---|
| `npm run dev` | 개발 서버 (5180) |
| `python tools/glb_inspect.py --sizes <glb...>` | Blender 없이 노드·삼각형·실측 크기 조사 |
| `blender --background --python tools/import_props.py -- [이름]` | 소품 GLB 변환 (이름 생략 시 전부) |
| `blender --background --python tools/fbx_to_glb.py -- <fbx폴더> <출력.glb> <본체.fbx>` | 좀비 본체+애니 → GLB. **본체 파일을 반드시 준다** |
| `python tools/glb_shrink_textures.py --max 512 --dry-run <glb...>` | GLB 안 텍스처만 줄인다(Blender 불필요). 지오메트리·배치는 안 건드린다. `--dry-run` 을 빼면 실제로 쓴다 |
| `python tools/glb_strip_animations.py <glb...>` | 클립을 통째로 들어낸다. **뼈 이름이 같은 다른 본체에서 빌려 쓸 때만** 쓴다 (본체당 약 3.1MB 절감) |
| 콘솔: `(await import('/tools/measure_contact.js')).measureContact(game)` | 공격 클립의 **닿는 프레임**을 실측 → `ATTACK.contact` |
| 콘솔: `(await import('/tools/measure_contact.js')).measureStride(game)` | 걷기·달리기 클립의 **원래 속도**를 실측 → `ANIM.clipSpeed` |
| **`node tools/qa_run.mjs`** | **자동 검사 전부를 사람 없이.** 개발 서버를 띄우고 크롬을 몰아 구역 125항목 + 모션 1482프레임을 돌리고, 콘솔 에러·죽은 요청까지 모은다. `--stages` / `--motion` 으로 한쪽만, `--head` 로 창을 띄워서. **바꾼 게 있으면 이걸 먼저 돌린다** |
| `node tools/qa_geometry.mjs <저장.json> [비교.json]` | 구역의 **지오메트리 지문**(충돌 박스·메시 좌표 전부). 조립 코드만 바꾸는 리팩터링이 벽을 1cm도 안 움직였는지 증명한다. `qa_run` 은 성질을 보지 좌표를 안 본다 |
| `node tools/make_submission_pdf.mjs` | **제출용 PDF 2종 생성 + 검사.** 손으로 Ctrl+P 하지 마라 — "배경 그래픽"을 빠뜨리면 표 머리와 스크린샷이 사라진다. 깨진 이미지·가로 넘침·로드 실패를 같이 잰다 |
| `node tools/check_pdf_layout.mjs [쪽...]` | 뽑은 PDF 의 **쪽 하단 여백**을 전 쪽 측정. 쪽번호를 주면 `docs/pdf_pages/` 에 PNG 로도 저장 |
| `node tools/qa_swing.mjs [저장.json] [비교.json]` | 근접 스윙을 프레임 단위로 측정 — 회전 진폭·**최대 각속도**·접촉 시점의 자세·**카메라 진폭**. "밋밋하다"를 감이 아니라 수치로 본다 |
| `node tools/check_audio.mjs` | 소리의 **등록·파일·호출** 세 집합을 서로 뺀다. 등록만 하고 안 부르는 것(= 게임에 없는 것)을 잡는다 |
| `npm run lint` | **임포트 누락**(no-undef). 빌드는 이걸 안 잡아 준다 — JS 는 그 줄을 실행할 때까지 모른다 |
| **`npm run verify`** | lint → 사운드 배선 → 자동 검사. **커밋 전에 이거 하나만 돌리면 된다** |
| `npm run qa:dist` | 빌드하고 **`dist/` 를 실제로 띄운다.** 404·번들 파손·시작 실패는 개발 서버에서 안 난다 — 이건 배포본만 잡는다 |
| `node tools/qa_brightness.mjs` | 구역별 **먹통 %**(휘도 0.02 미만) · 평균 휘도 · 탄 픽셀. "너무 어둡다"를 감이 아니라 수치로 |
| `python tools/preview_sheet.py` | 변환 결과 미리보기를 한 장으로 모음 |
| `python tools/gen_surfaces.py [px]` | 벽·바닥·천장 텍스처 재인코딩 (기본 1024, `512` 로 되돌림) |
| `blender --background --python tools/extract_swing.py -- fbx_src/person public/assets/models/swing_curves.json` | 사람 모션 → 무기 스윙 궤적 |
| 브라우저 콘솔에서 아래 2줄 | **구역별 자동 QA 25항목 × 5구역 = 125** |

```js
const { runQA, summarize } = await import('/tools/qa_stages.js');
console.log(summarize(runQA(window.game)));   // 실패 목록이 좌표까지 찍힌다

// 모션을 프레임 단위로 — 바닥뚫림·공중부양·배속이상(경련/늘어짐)을 잡는다
const { runMotionQA, summarize: mSum } = await import('/tools/qa_motion.js');
console.log(mSum(await runMotionQA(window.game)));

const { benchAll } = await import('/tools/bench.js');
console.table(await benchAll(window.game));   // 구역별 GPU 프레임 시간
```

> 배치를 바꿨으면 이걸 먼저 돌린다. 어두운 화면에서는 눈으로 못 찾는 것이 많다 —
> 벽에 박힌 소품, 갇힌 스폰, 도달 못 하는 상호작용, 예산 초과를 한 번에 잡는다.

---

## 커밋 로그 요약

- `초기 뼈대` — Vite + Three.js, 1인칭 이동, 손전등, 좀비, Director, 무기, HUD
- `전면 개편` — 구역 A·B, 사운드, 좀비 GLB, 부상, 수색
- `Sketchfab 소품 24종 반입` — 변환 파이프라인 + InstancedMesh 배치
- `무기 GLB 3종 + 쇠지렛대`
- `구역 C·D·옥상 추가` — 전 구역 골격 완성 + QA 18항목
