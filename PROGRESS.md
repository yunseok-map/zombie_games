# PROGRESS — 현재 상태

> **세션 시작 시 CLAUDE.md 다음으로 이 파일을 읽는다.**
> 세션 종료 시 이 파일을 갱신한다. 다음 세션이 이것만 읽고 이어받을 수 있어야 한다.

최종 갱신: 2026-08-07 / **D-3 (접수 마감 2026-08-10)**

> **이건 NHN GAME X AI HACKATHON(NAN 2026) 사전 과제다.** 제출물이 5종이고
> **하나라도 빠지면 심사 대상에서 제외된다.** 요건과 남은 항목은 `TODO_MORNING.md` 맨 위에 있다.
> 규정상 **.exe 제출 불가 · 웹 또는 APK 만 허용** — 이 게임은 이미 웹이라 조건을 통과한 상태다.

---

## 지금 되는 것

### 시스템
- [x] 프로젝트 뼈대 (Vite + Three.js) · 1인칭 이동 · AABB 벽 충돌
- [x] 손전등(F) — 배터리 소모, 잔량 낮으면 깜빡임
- [x] 어둠·포그·톤매핑 · 후처리(블룸·비네트·그레인·색수차)
- [x] **빛줄기 속 먼지** — 손전등 원뿔 안 입자만 밝게 (Points 1400, 드로우콜 1)
- [x] **환경맵** — 금속·유리가 형태를 읽히게 (FX.envIntensity 0.16 으로 아주 약하게)
- [x] **타이틀 화면** — AI 키아트(Higgsfield, 59KB)를 **CSS 로 포스터처럼 가공한** 배경 +
      왼쪽 아래 활자 락업. 형광등이 두 번 실패한 뒤 켜지며 활자가 드러나는 등장 연출,
      마우스 시차, 비상등 호흡, 초당 9번 움직이는 필름 그레인.
      조작법은 ESC 일시정지 화면으로 옮겼다. 이미지가 없으면 뒤의 3D 복도가 비친다(폴백)
- [x] **총구 화염** — 칠흑 속 발사 순간 주변이 전부 드러난다 (0.065초)
- [x] **방향성 넉백** — 맞은 방향으로 밀리고 젖혀진다. 뒤에서 맞으면 앞으로 고꾸라진다
- [x] **좀비가 몸을 돌려서 온다** — 회전 속도 제한(종류별). 전에는 방향이 바뀔 때마다 홱 돌았다
- [x] **공격 선딜** — 사거리에 들어와도 팔을 드는 시간이 있다. 전에는 닿는 즉시 깎였다
- [x] **이동감** — 달릴 때 시야각이 넓어지고, 옆걸음에 몸이 기울고, 헤드밥이 8자를 그린다
- [x] **계기판형 HUD** — 라벨+숫자+눈금, 위험 시 맥동, 저탄약 경고색. 콘덴스드 활자
- [x] **시체가 남는다** — 30초 유지 후 가라앉으며 사라짐. 죽은 자리에 핏자국
- [x] **피격 시 피가 튄다** — 맞은 방향으로, 헤드샷이면 더 많이 (재사용 풀, 드로우콜 1)
- [x] **로딩 화면** — 진행 막대 + 지금 무엇을 받는지
- [x] **표면 텍스처 1024** (색상만. 노멀·러프는 512) — AO 를 색에 55% 구워 넣음
- [x] 좀비 상태머신 + 풀링 + Director 긴장도 스폰 (BUILDUP / PEAK / RELIEF)
- [x] **좀비 3종** — 배회체 · 청각체(눈이 없다, 소리에만) · **포복체(신규, 엎드려 기어온다)**
- [x] 무기 시스템 (데이터 드리븐) · HUD · 게임 루프(시작→플레이→사망→재시작)
- [x] **구역 체크포인트** — 죽으면 "이 구역부터" / "처음부터". 전에는 무조건 1구역부터였다
- [x] **상시 목표 표시** — 좌상단에 "지금 할 일". 진행에 따라 갱신(무전기 2/4 등)
- [x] **무기를 구역에서 줍는다** — B1 쇠지렛대 · 2F 라디오(유인) · 3F 소방도끼
- [x] **타격감** — 카메라 흔들림(총·근접명중·피격) · 히트스톱 0.055초(닿았을 때만)
- [x] **좀비 발소리** — 보이기 전에 들린다. 16m 안쪽만, 포복체는 더 낮은 피치
- [x] **피격 방향 표시** — 맞은 쪽만 붉게 물든다
- [x] **적응형 해상도** — 프레임이 예산을 넘으면 스스로 낮춘다 (심사자 PC 를 고를 수 없다).
      **2026-08-06: 이게 그동안 동작하지 않고 있었다 — 아래 "성능" 항목 참고.**
- [x] **스텔스 피드백** — 노출도 계기(앉기 2m → 달리기+손전등 22m) + 감지 상태(경계/발각됨)
      + 좀비가 처음 경계할 때 나는 소리. 손전등을 켜는 대가가 처음으로 화면에 보인다
- [x] 상호작용(E) · 수색/전리품 · 부상 시스템 · 거리 기반 3D 오디오
- [x] **수색에서 9mm 탄약이 나온다** — 한 판 전체 +50발(48 → 98). 전에는 보급이 아예 없었다
- [x] 사운드 34종 (ElevenLabs) · 좀비 GLB + 애니 **27클립** (Mixamo)
- [x] **좀비 본체 3종 — 감염된 의료진이 무리의 90%다.** 후드·마스크·고글·니트릴 장갑의
      **전신 방호복**(80%), 수술모·마스크에 등이 찢긴 **수술복 외과의**(10%), 나머지가
      기본 메시. 이 병원이 왜 격리됐는지가 옷으로 읽힌다.
      **텍스처가 아니라 메시를 바꾼 이유**는 아래 "좀비 본체 3종" 항목 참고
- [x] **무기 스윙이 사람 모션 궤적을 따른다** — 사람 근접공격에서 오른손 궤적만 뽑아
      뷰모델에 입혔다. 절차적 사인 곡선과 달리 예비동작에서 뜸을 들이고 타격에서 확 빠진다
- [x] **모션 프레임 단위 점검 도구** `tools/qa_motion.js` — 바닥뚫림·공중부양·배속이상을
      1482프레임에서 자동으로 잡는다

### 구역 — **1F → B1 → 2F → 3F → 옥상, 전부 연결됨**
- [x] **구역 A (1F 격리병동)** — 복도 + 병실. 카드키 → 잠긴 계단실 문
- [x] **구역 B (B1 영안실·기계실)** — 완전 암흑. 전원 레버 3개 → 점등 → 대규모 웨이브.
      **소품을 영안실답게 다시 깔았다**: 시신 안치 캐비닛 5개(전에는 사물함 21개를 쌓아 둔
      창고처럼 보였다) · 시신백 8구(그중 2구는 열린 채 비어 있고 끌린 핏자국이 이어진다) ·
      부검대 위의 시신 · 복도의 시신백 4구 · 시야를 끊는 커튼 2장
- [x] **구역 C (2F 병동)** — 병실 8개, 커튼으로 시야 차단, 점멸 조명.
      **사건: 무전기 4대를 켜야 계단실이 열린다** (켤 때마다 소음 → 웨이브)
- [x] **구역 D (3F 수술부·중환자실)** — 수술실 2개 + 커튼 늘어선 중환자실. 좁음이 공포.
      **사건: 무영등 2개에 전원을 넣어야 봉쇄문이 풀린다**
- [x] **구역 E (옥상)** — 천장 없는 야외. 설비 사이를 돌며 헬리패드로.
      **사건: 신호탄 → 90초 버티기 → 헬기 도착 → EXTRACTED.** 출구는 그전까지 열리지 않는다

### 에셋
- [x] PBR 텍스처(ambientCG CC0) · 핏자국 데칼 3종 · 의료폐기물 산포
- [x] **Sketchfab 소품 24종 반입 완료** — 병상·휠체어·링거대·커튼·수술대·시신 등
- [x] **무기 GLB 3종** — 쇠파이프 · 9mm 권총 · 쇠지렛대(신규)
- [x] **좀비 본체 GLB 2종** — 기본 메시(4.4MB) + 방호복 메시(7.0MB). 클립 이름이 같아
      `ZombieModel` 이 어느 쪽이든 같은 24클립을 물린다
- [x] 변환 파이프라인 `tools/import_props.py` (Blender 헤드리스, 미리보기 렌더 포함)

---

## 아직 안 되는 것 / 다음 할 일 (우선순위 순 3개)

1. **옥상 90초 체감 결정 — 이건 사람이 플레이해야 한다.** 숫자로 잴 수 있는 것은 다 쟀다(아래).
   남은 판단은 하나: *90초가 "겨우 버텼다"인가.* 고칠 값은 `balance.js` 의 `EVENTS.roof`
   (`holdSeconds` · `waveSize` · `finalWave`) 뿐이다.
   - **실측**: 예정 웨이브는 35마리지만 **실제로는 45~65마리가 나온다.** 웨이브가 올 때마다
     PEAK 로 되돌아가서 Director 가 목표치(12)까지 알아서 채우기 때문. 처치가 빠를수록 더 나온다.
   - 필요 피해량 2,300~3,300 vs 탄약 전량 98발 = 3,332. **총만으로는 못 버티고, 근접이 절반을
     책임져야 한다.** 탄약 보급이 없던 어제까지는 1,632 라 애초에 불가능했다(그래서 고쳤다).
   - 판단 재료: 2F 무전기 4대(웨이브 3씩) · 3F 무영등 2개(웨이브 5씩)는 총량이 옥상의 1/4 이라
     지금도 가볍다. 옥상만 만지면 된다.
2. **좀비 3종 체감 확인** — 배회체·청각체·포복체가 구역별 가중치대로 섞여 나온다.
   실제로 싸워 보고 청각체(빠름·소리만 반응)와 포복체(낮아서 늦게 보임)의 비율을 맞춘다.
   *완료 기준: 포복체를 밟기 전에 발견하게 되는가.*
3. **제출물 4종 — 이게 지금 가장 급하다.** 게임은 이미 5구역 + 엔딩까지 있다.
   빠진 것은 **배포 URL · 30~60초 영상 · 설명 PDF · AI 활용 기술 PDF** 다.
   전부 `TODO_MORNING.md` 에 있고, PDF 초안은 AI 가 쓸 수 있다.
   *CLAUDE.md §1-8: D-2 이후에는 구조를 건드리지 않는다. 지금이 D-5다.*

> **사용자가 직접 해야 하는 일은 `TODO_MORNING.md` 에 따로 모아 뒀다.**

---

## 예산 현황 (2026-08-05 실측, 구역별)

| 구역 | 드로우콜 | 삼각형 | +좀비14 | 스폰 | 판정 |
|---|---|---|---|---|---|
| 1F 격리병동 | 87 | 83,268 | 167k | 17 | ✅ |
| B1 영안실 | 29 | 45,103 | 129k | 10 | ✅ |
| 2F 병동 | 47 | 116,480 | 200k | 12 | ✅ |
| 3F 수술부 | 45 | 75,508 | 160k | 9 | ✅ |
| 옥상 | 26 | 37,258 | 121k | 9 | ✅ |

예산: 드로우콜 ≤300 · 삼각형 ≤350k (CLAUDE.md §3).
빌드 산출물 **15.9MB**, 로컬 로딩 5.3초(예산 8초). 배포 환경 실측은 위 "배포" 항목 참고.

---

## 성능 (2026-08-06 실측 · Radeon 780M 내장그래픽 · 60Hz)

**증상은 "프레임이 살짝 끊긴다" 였지만 실제로는 24fps 였다.** 원인은 후처리가 무거워서가
아니라 **적응형 해상도가 통째로 무효였던 것**이다.

three.js `EffectComposer` 는 만들어질 때의 `pixelRatio` 를 내부(`_pixelRatio`)에 붙잡아 두고
`setSize()` 에서 그 값을 곱한다. `Game._adaptResolution` 은 `renderer.setPixelRatio()` 만
바꾸고 컴포저에는 알려 주지 않았다. 그래서 배율을 0.75 로 낮춰도 씬과 후처리는 계속
1.5 배 해상도로 그려졌다. **약한 PC 를 위한 안전장치가 이름만 남아 있었다.**
→ `PostFX.setSize` 가 `composer.setPixelRatio()` 를 같이 갱신하도록 고쳤다.

| | 렌더 비용 | 60Hz 에서 보이는 fps |
|---|---|---|
| 이전 (2379x1125 로 고정 렌더) | 44.3ms | **20~24fps · 판정이 흔들림** |
| 이후 (적응형이 실제로 동작) | 13~16.8ms | **60fps 고정** |

### 품질 항목별 비용 (dpr 1.0 · 1600x900 · 좀비 8 · vsync 끄고 측정)

기준 12.8ms. **vsync 를 끄지 않으면 16.7ms 아래가 전부 16.7 로 읽혀 비교가 안 된다.**

| 항목 | 추가 비용 | 채택 |
|---|---|---|
| 이방성 8 → 16 | +0.4ms | ✅ 복도를 보며 걷는 게임이라 화면 대부분이 비스듬한 바닥 |
| 블룸 0.5 → 1.0 배 | +0.5ms | ✅ 사실상 공짜. 절반이면 손전등 핫스팟 둘레가 계단진다 |
| 그림자 2048 → 4096 | +1.2ms | ✅ 손전등 원뿔이 좁아 올린 만큼 그대로 좋아진다 |
| MSAA 0 → 2 | +3.3ms | ❌ MSAA 4 도 같은 값인데 **가장자리만** 좋아진다 |
| 해상도 1.0 → 1.25 배 | +7.9ms | 적응형에 맡긴다 (같은 예산으로 화면 **전체**가 좋아진다) |
| 그림자 전체 | 1.4ms | 병목 아님 |
| 좀비 14 마리 | 3.6ms | 병목 아님 |

**결론: 이 게임에서 비싼 것은 오직 해상도다.** 그래서 MSAA 대신 해상도 천장을 1.75 까지
열고(수퍼샘플링 허용) 적응형이 알아서 고르게 했다. 강한 PC 는 올라가고 약한 PC 는 내려간다.

### 적응형은 이제 수렴한다

60Hz vsync 에서 프레임 간격은 16.7 아니면 33.3 으로만 튄다 — 중간이 없다. 그래서
조금만 잘못 잡아도 두 값을 오가고, 배율이 바뀔 때마다 렌더 버퍼를 다시 잡느라 끊긴다.

- 예산을 **vsync 기준**으로 다시 잡았다 (16.5 → 20.0). 예전 값은 "여유 있음"을 영원히
  못 보게 만들어서, 한 번 내려간 해상도가 다시 안 올라왔다.
- 내릴 땐 초과 폭에 따라 크게/작게, 올릴 땐 연속 4회 확인 후 한 칸씩 (비대칭).
- **한 번 못 버틴 배율은 기억해 두고 다시 올라가지 않는다** → 한 값에 정착한다.
- 결과: 배율 변경 8회 → 3회, 33ms 초과 프레임 4% → 1.5%.

> **fps 상한(`PERF.fpsCap`)은 0(끔)이 기본이다.** vsync 가 이미 주사율로 묶어 주므로
> 그 위에 상한을 또 걸면 이득 없이 지터만 는다(측정: 663 중 2 → 9). 분위기를 위해
> 30 으로 낮추고 싶다면 **주사율의 약수만** 쓸 것. 다만 이 게임은 모션블러가 없어서
> 30 은 필름 룩이 아니라 끊김으로 읽히기 쉽고, 1인칭 마우스 조준이라 지연도 커진다.

---

## 배포 (2026-08-07)

**https://yunseok-map.github.io/zombie_games/** — master 에 푸시하면 GitHub Actions 가
자동으로 다시 굽는다(`.github/workflows/deploy.yml`). `dist/` 는 커밋하지 않는다.
커밋된 dist 가 소스보다 낡아서 심사자만 옛날 게임을 보는 사고를 원천 차단한다.

> Pages 를 **처음 켜는 것**만 저장소 설정에서 사람이 한다
> (Settings > Pages > Source > `GitHub Actions`). 워크플로 토큰 권한 밖이다.

### 왜 마감 전에 미리 배포했는가

로컬 로딩 5.3초는 **아무것도 보장하지 않는다.** 심사자는 HTTPS 로 받는다.
올려 놓고 회선을 제한해 재 보니 이랬다.

| 회선 | 처음 배포 (전송 29MB) | 지금 (전송 13.4MB, 본체 3종 포함) |
|---|---|---|
| 50Mbps | 5.3초 | **2.9~3.1초** |
| **10Mbps** | **23.8초** | **10.6~11.0초** (2회 재현) |

10Mbps 는 호텔·사무실 와이파이에서 흔하다. **검은 화면 24초면 게임을 보기 전에 닫는다.**
마감 당일에 알았으면 손쓸 시간이 없었다. 이것이 배포를 미루면 안 되는 유일한 이유다
(PDF·영상은 늦게 써도 품질이 안 떨어지지만, 배포는 늦으면 **문제를 발견할 기회가 사라진다**).

**10Mbps 의 10.6초는 사실상 바닥이다** — 13.4MB 를 10Mbps 로 받는 이론 최소값이 10.7초다.
여기서 더 빨라지려면 **처음에 받는 양 자체**를 줄여야 한다. 남은 수단은 하나뿐이다:

- **구역별 지연 로딩.** 지금은 `Game.start()` 에서 5구역 소품을 전부 받는다(StageLoader 의
  조립이 동기라서 그렇게 돼 있다). 1구역 것만 받고 나머지는 진행하며 받으면 첫 진입이
  절반 이하로 떨어진다. 다만 **구조 변경**이다 (CLAUDE.md §1-8).

애니메이션 중복은 이미 처리했다 — 아래 "좀비 본체 3종" 참고. 남은 중복은 기본 메시와
방호복이 각각 들고 있는 클립 한 벌씩인데, **이 둘은 뼈 구성이 서로 달라 공유할 수 없다.**

> **빠른 회선 수치는 이 기계에서 믿을 게 못 된다.** 전송이 순식간이라 기계 쪽 잡음이
> 그대로 드러나서 같은 빌드가 2.9초와 9.1초를 오간다. 반면 **10Mbps 는 두 번 재서
> 10.6 / 11.0 초로 재현됐다** — 전송이 지배적이라 잡음에 묻히지 않는다.
> 판단은 **느린 회선 쪽 숫자**로 한다.

> **로딩 시간 측정은 기계 상태에 심하게 오염된다.** 한 번 재고 판단하면 오진한다.
> 실제로 "에셋을 줄였는데 3.3초 → 10.9초로 느려졌다"는 결과가 나와 회귀로 오해했는데,
> 같은 조건에서 옛/새 에셋을 번갈아 재 보니 **새 쪽이 빨랐다**(중앙값 2.42 vs 3.08초).
> 회차마다 9초대 이상치가 하나씩 섞여 평균을 망가뜨리고 있었다.
> 그리고 **제한을 건 쪽이 더 빠르게 나오면 그 회차는 버려라** — 첫 브라우저 실행이
> 콜드스타트 비용을 뒤집어쓴 것이다(제한없음 9.9초 vs 50Mbps 2.9초).
> 판단은 **중앙값**과 **같은 조건의 A/B** 로만 한다.

### 무엇이 컸는가 — 애니메이션이 아니라 텍스처였다

`tools/glb_breakdown` 류로 GLB 내부를 갈라 보니:

| 파일 | 이전 | 이후 | 정체 |
|---|---|---|---|
| `zombie_nurse.glb` | 7.01MB | **3.78MB** | 4096 노멀·광택맵이 안 줄어든 채 들어감 (아래 함정) |
| 소품·무기 27종 | 8.65MB | **3.98MB** | 1024 텍스처 → 512 (파이프라인 설정은 이미 512였는데 결과물이 낡아 있었다) |
| `zombie_shambler.glb` | 4.36MB | 4.05MB | 2048 → 1024 |
| `zombie_surgeon.glb` | — | +0.69MB | 새로 추가한 세 번째 본체 (클립을 빌려 써서 이 크기다) |
| 합계 `dist/` | 24.1MB | **16.6MB** | 본체가 하나 늘었는데도 24.1 → 16.6 |

소품 텍스처는 **512 로 줄여도 눈에 안 띈다** — 녹·긁힘·노멀 디테일이 그대로 남는다
(2배 확대해 옛 텍스처와 나란히 놓고 확인했다). 손전등 원뿔 안에서만 보이는 물건에
1024 는 낭비였다.

> 조명 아래 스크린샷으로 "흐려졌는가"를 판단하려 하지 마라. 손전등이 흰 소품을
> 통째로 날려서 아무것도 못 가린다. **텍스처를 직접 꺼내 나란히 놓고 봐야 한다.**

---

## 좀비 본체 3종 (2026-08-07)

피드백은 "가운 입은 간호사 좀비가 너무 적다" 였다. **재 보니 비율 문제가 아니었다** —
가운 변형은 이미 75% 였다. 문제는 **실루엣**이었다. 기본 메시(ZombieGirl)는 크롭탑 +
반바지라, 텍스처를 아무리 희게 칠해도 "짧은 흰 상의 + 맨다리"로 읽힌다. 옷을 바꾼 게
아니라 **몸매를 흰색으로 칠한 것**이었다.

→ 옷을 **별도 메시**로 분리했다. `ZombieModel` 이 개체마다 본체를 골라 쓴다.

| 본체 | 정점 | 비율 | 파일 |
|---|---|---|---|
| 방호복 (후드·고글·니트릴 장갑) | 1,343 | **80%** | `zombie_nurse.glb` 3.78MB |
| 수술복 외과의 (수술모·마스크·찢긴 등) | 3,079 | 10% | `zombie_surgeon.glb` **0.69MB** |
| 기본 (ZombieGirl, 수술복 텍스처 / 원본) | 3,479 | 10% | `zombie_shambler.glb` 4.05MB |

실측(풀 20마리): 15 / 2 / 3. 요청한 8:2 가 유지된다.

### 세 번째 본체는 **이미 갖고 있었다**

`fbx_src` 의 With Skin 파일 안을 훑어 보니 서로 다른 캐릭터가 들어 있었다.
FBX 는 바이너리지만 오브젝트·재질 이름은 평문이라 Blender 없이 확인된다.

| 파일 | 들어 있는 몸 |
|---|---|
| `idle_03.fbx` | `ZombieGirl_Body` — 기본 메시 |
| `idle_04.fbx` | `Ch11` — **방호복과 같은 캐릭터.** 60MB 를 애니메이션 용도로만 쓰고 있었다 |
| `idle_05.fbx` | `Ch16_Body` — **수술복 외과의. 한 번도 안 쓰였다** |
| `idle_01.fbx` | `Beta_Joints` — Mixamo 기본 마네킹 |

**새 모델을 받기 전에 이미 받아 둔 것부터 열어 봐라.**

### 클립은 한 벌만 싣는다

본체를 늘릴 때마다 같은 24클립이 통째로 한 벌씩 더 실린다 — 본체당 **3.1MB** 다.
Mixamo 리그는 뼈 이름이 같으므로 three.js 가 이름으로 붙여 준다(PropertyBinding).
그래서 수술복은 **애니메이션을 아예 빼고 굽고** 방호복 것을 빌려 쓴다.

- `zombie_surgeon.glb` 3.78MB → **0.69MB**
- 앞으로 본체를 추가할 때도 한 종당 3.9MB 가 아니라 **약 0.7MB** 만 든다

> **빌려 쓰기 전에 뼈 이름 집합을 반드시 비교해라.** 방호복과 수술복은 뼈 65개가
> 완전히 일치해서 안전하다. **기본 메시만 혼자 다르다** — 눈뼈 2개가 더 있고
> 손가락 뼈 4개가 없다. 어긋난 채로 빌려 쓰면 three.js 가 `no target node found` 를
> **console.warn** 으로 뱉고, 이 프로젝트의 QA 는 그걸 실패로 센다.

### 근거리 노출

Mixamo 원본은 알베도가 밝아서 손전등을 가까이서 받으면 완전히 날아간다.
공격 사거리(1.9m)에서 찍어 보니 방호복은 **몸통 주름이 통째로 사라진 흰 덩어리**였다.
3.6m 에서는 멀쩡해서 놓치기 쉬운데, 정작 플레이어가 가장 자주 보는 거리가 1.9m 다.
알베도를 0.56 배로 낮추고 거칠기를 0.94 로 올렸다(`balance.js` 의 `ZOMBIE_LOOK`,
적용 대상은 `MODEL_FILES` 의 `tune` 표시로 정한다). 주름·솔기·후드가 되살아났다.

---

## 자동 QA (2026-08-06 기준 / 2026-08-07 재확인)

`SETUP.md §4` 의 콘솔 검사 두 종을 **헤드리스 크롬으로 반복 실행**해서 간헐 결함을 잡는다.
한 번만 돌리면 통과하는데 30번 돌리면 2~4번 실패하는 것들이 실제로 있었다.

한 회차에서 보는 것: 구역 배치 125항목 · 좀비 모션 1482프레임 · **해상도연동 회귀** ·
콘솔 에러 0 · 네트워크 실패 0.

**최종 30회 전부 통과 (30/30).** 세션 시작 시점은 20회 중 18회였고, 실패 2회는
게임이 아니라 도구 오탐이었다. 회당 약 40초(헤드리스 소프트웨어 렌더 기준).

**2026-08-07 방호복 메시 + 영안실 소품 반영 후 20/20 통과.** 로딩 평균 5.32초
(GLB 7MB 가 늘었는데도 예산 8초 안). 모션 1482프레임 전부 정상이라
방호복 메시의 접지·오프셋도 문제없다.

> **하네스가 `console.warn` 을 놓치고 있었다.** 퍼펫티어가 주는 타입은 `'warning'` 이
> 아니라 **`'warn'`** 이다. 이 프로젝트는 에셋 로드 실패를 전부 `console.warn` 으로
> 처리하는(폴백 설계) 데다, vite 개발 서버는 없는 에셋에 404 대신 index.html 을 **200**
> 으로 준다. 둘이 겹쳐서 **에셋이 통째로 빠져도 QA 가 통과했다.** 필터를 고쳤다 —
> 이제 방호복 GLB 를 지우면 QA 가 즉시 실패한다.

이번 세션에서 **QA 가 잡아낸 것** (전부 수정 완료):

| 무엇 | 정체 |
|---|---|
| 콘솔 에러 2건/로드 | `weapon_axe.glb`·`weapon_molotov.glb` 가 저장소에 없는 아틀라스 `Textures/colormap.png` 를 참조. GLB 안의 텍스처 참조를 제거했다 (색은 원래도 안 붙었고 `colorMul` 이 그 상태 기준이라 화면은 그대로) |
| favicon 404 | `index.html` 에 인라인 SVG 로 넣었다. 이제 콘솔이 완전히 비어야 정상 |
| 청각체가 슬로모션으로 걷는다 | `Zombie.js` 에서 `_jitter` 를 `clamp` **바깥**에서 곱해, 하한 0.4 를 걸어 놓고 0.92 를 곱해 **0.37 배속**이 나왔다. 공격 클립도 같은 방식으로 상한 2.0 을 넘었다. 지터를 clamp 안으로 넣고 수치는 `ANIM.moveMinSpeed/moveMaxSpeed` 로 옮겼다 |
| 화염병이 게임에 없다 | `weapons.js` 에 정의만 있고 어느 스테이지에도 배치가 없었다. 게다가 `_throw()` 에 불꽃·화상(dot)·투사체가 없어 "보이지 않는 광역 피해"였다. **컷했다** (되살리려면 연출부터) |

**도구 자체의 오탐 2건도 고쳤다** — 이쪽이 더 무섭다. 멀쩡한 것을 결함이라고 외치면
진짜 결함을 못 믿게 된다.
- 포복체가 비명 클립에 들어가면 허용치가 0.952 → 0.616 으로 **오히려 좁아졌다**
  (삼항으로 하나만 골라서). 둘 다 "더 떠도 정상"인 사유이므로 느슨한 쪽을 쓰게 고쳤다.
- 피격 중 몸이 젖혀지면 발이 내려가는데(위 함정 참고) 고정 허용치로 재고 있었다.
  **그 순간의 실제 기울기에서 허용치를 유도**하도록 고쳤다 (상수를 늘린 것이 아니다).

> **QA 를 돌리는 동안 소스를 편집하지 마라.** Vite 가 핫리로드로 페이지를 갈아치워서
> `Execution context was destroyed` 로 회차가 통째로 날아간다. 한 번 밟았다.

---

## 알려진 함정

### 성능·렌더 (2026-08-06 추가)
- **후처리를 쓰면 `renderer.setPixelRatio()` 만으로는 해상도가 안 바뀐다.**
  `EffectComposer` 가 자기 `_pixelRatio` 를 따로 들고 있다. `PostFX.setSize` 가
  `composer.setPixelRatio()` 를 같이 부르게 해 뒀다. **후처리 체인을 손대면 이걸 깨기 쉽다** —
  화면으로는 절대 안 보이고 프레임만 조용히 무너진다. `tools/qa_stages.js` 가 아니라
  QA 하네스의 "해상도연동" 검사가 이걸 지킨다.
- **프레임을 잴 때는 vsync 를 꺼라.** 안 끄면 16.7ms 아래가 전부 16.7 로 읽혀서
  무엇이 비싼지 못 가린다. `--disable-gpu-vsync --disable-frame-rate-limit`.
- **적응형 예산은 vsync 기준으로 읽어야 한다.** 12ms 에 그려도 60Hz 에서는 간격이
  16.7ms 로 측정된다. 예산을 16.5 로 잡으면 "항상 초과"가 되어 해상도가 바닥까지 내려간다.
- 피격 중 발이 바닥을 **4~5cm 뚫는다.** `KNOCK.bend`(0.26rad)로 몸을 발치 기준으로
  젖히는 연출의 기하학적 결과다(sin(0.26) x 0.19m). 어두운 바닥이라 눈에 잘 안 띄어
  그대로 뒀다. 거슬리면 젖히는 동안 group 을 그만큼 들어 올리면 되지만, 그러면
  맞을 때마다 살짝 뛰어오르는 것처럼 보인다. **트레이드오프를 보고 정할 것.**

### 게임
- 브라우저는 **사용자 클릭 전에 소리를 못 낸다.** 시작 화면 클릭 시점에 오디오를 초기화해야 한다.
- 포인터락은 `Esc` 로 풀린다. 풀리면 일시정지로 들어가게 되어 있다.
- 좀비 렌더가 프레임의 대부분을 먹는다. 14마리가 하드캡이다.
- 좀비 추격은 NavMesh 없이 **직선 + 3방향 레이 회피**다. 15초 이상 접근 못 하면 자동 반납.
- **스폰 지점은 플레이어에게서 12m 이상 떨어져 있어야 쓰인다**(`DIRECTOR.spawnMinDistance`).
  옥상 헬리패드 둘레 3곳은 중심에서 6~7m 라 **한 번도 안 쓰였고**, 패드에 서면 남쪽 2곳에서만
  일렬로 들어왔다. 둘레 13~15m 에 링을 둘러 사방에서 오게 고쳤다(방향별 유효 지점 2 → 5~6).
  **새 구역에 스폰을 놓을 때는 "플레이어가 오래 머무는 자리에서 12~34m 인가"를 먼저 재라.**
  재는 법: `game.director.spawnPoints` 와 `game.collision.segmentBlocked/isBlocked` 로 직접 계산.
- **이벤트 웨이브는 예정 수보다 훨씬 많이 나온다.** `triggerWave` 가 phase 를 PEAK 로 돌리므로
  그동안 Director 가 목표치(12)까지 따로 채운다. 옥상은 예정 35 → 실제 45~65.
  사람 없이 재려면 가짜 pool(`{activeCount, spawn, broadcastNoise}`)로 Director 를 돌린다.
- `Director.triggerWave(n)` 은 타입을 안 주면 **구역 typeWeights** 를 따른다.
  예전에는 'shambler' 로 고정돼 있어서 청각체·포복체를 등록해도 이벤트 웨이브가 전부 배회체였다.
- 포복체는 **몸집이 작은 게 아니라 엎드린 것**이다. `height` 로 줄이면 미니어처가 된다 →
  `modelScale`(몸집)과 `height`(피격 판정)를 따로 둔다. 루트 수직 이동을 지운 탓에
  엎드린 클립을 쓰면 뜨므로 `modelYOffset`(-0.62)로 내린다.
- `Collision.segmentBlocked` 의 5번째 인자는 반지름이 아니라 **샘플 간격**이다.
- **`Zombie._syncMesh(dt)` 에 dt 를 안 주면 회전이 즉시 맞춰진다.** 스폰 직후에는 그게 맞고
  (도는 모습이 보이면 안 된다), 매 프레임 호출에서는 반드시 dt 를 넘겨야 한다.
- 좀비 모션을 손으로 확인하려면 `z.facing` 을 반대로 준 뒤 `z._syncMesh(1/60)` 을 여러 번
  부르고 `z.group.rotation.y` 를 찍는다. 공격 선딜은 `z._startChase()` 로 붙여 놓고
  `pool.update` 를 돌리며 hp 가 떨어지는 시점을 본다 — **플레이어 무적시간(0.45초)은
  `player.update` 를 같이 안 돌리면 안 풀려서 두 번째 타격이 영영 안 온다.**
- 수치를 바꾸려면 `src/config/balance.js` 와 `src/config/weapons.js` **만** 열면 된다.
- **GLB 변환 시 본체 파일(`idle_03.fbx`)을 반드시 인자로 준다.** 생략하거나 다른 파일을 주면
  Mixamo 기본 마네킹(`Beta_Joints`)이 몸이 되어 **좀비가 통째로 회색 인형으로 바뀐다.**
  클립 수·용량이 정상이라 GLB 를 열어 보기 전까지 티가 안 난다. 확인:
  `python -c "...json...['meshes']"` 로 메시 이름이 `ZombieGirl_Body` 인지 본다. (ASSETS.md §6-3-D)
- **`img.has_data` 로 텍스처를 거르면 대부분이 조용히 안 줄어든다.** 팩된(FBX 내장)
  이미지는 **한 번 건드리기 전까지 False** 다. `fbx_to_glb.py` 가 이걸로 필터하고 있어서
  방호복 GLB 에 **4096 노멀맵·광택맵이 그대로 실려 3MB 를 더 먹었다.** 로그에는
  "축소 3장"만 찍혀서 9장 중 6장이 빠진 걸 못 알아봤다 — 같은 함정을 `import_props.py`
  는 이미 겪고 고쳐 놨었는데 좀비 변환기만 옛날 코드였다. 지금은 `img.pixels[0]` 로
  강제 로드하고 **몇 장 줄였는지 총계를 찍는다.** 조용히 건너뛰는 것이 이 함수의
  유일한 실패 방식이므로 총계가 없으면 또 못 잡는다.
- **데시메이트를 모디파이어로 남겨 두면 변환이 안 끝난다.** glTF 익스포터는 애니메이션을
  구울 때 **프레임마다 depsgraph 를 다시 평가**한다. 데시메이트가 모디파이어로 걸려 있으면
  그때마다 원본(5만 삼각형)을 다시 깎는다 — 클립 24개면 수천 번이다. 실제로 방호복 변환이
  **66분을 넘겨도 안 끝났고**, 익스포트 직전에 확정 적용(`modifier_apply`)하도록 고치니
  **1분 30초**에 끝났다. 기본 메시는 삼각형이 적어 데시메이트가 아예 안 걸렸기 때문에
  이 함정이 지금까지 드러나지 않았다. (`tools/fbx_to_glb.py`)
- **본체가 아닌 With Skin FBX 는 변환에서 빼라.** `idle_03/04/05` 는 With Skin 으로
  받아져서(§6-2 위반) 합계 127MB 다. 애니메이션만 필요한데 4096² 텍스처까지 끌고 들어와
  임포트 단계를 몇 배로 늘린다. 빼도 idle 변형이 5종 → 2종으로 줄 뿐이다
  (`ZombieModel.pick()` 이 **있는 클립만 고르므로** 부분 집합이어도 안전하다).
- **오래 걸리는 변환을 `Select-Object -Last N` 에 파이프하지 마라.** 출력이 파이프에
  갇혀 끝날 때까지 한 줄도 안 보인다. 진행 중인지 멈춘 건지 구분이 안 돼서
  66분짜리 실패를 66분 동안 못 알아봤다. 로그 파일로 리다이렉트하고 tail 로 본다.
- **Mixamo 팩(Pack)으로 받으면 안 된다.** 변환기는 FBX 하나당 애니메이션 하나를 전제하고
  파일명을 클립 이름으로 쓴다. 팩은 내부 take 이름이 전부 `mixamo.com` 이라 쪼개도 못 살린다.
- **`addSign(slot, ...)` 의 slot 은 아틀라스 칸 번호다 — 뜻이 있는 번호다.**
  칸 목록은 `tools/gen_signage.py` 의 `CELLS` 배열이 원본이다. 번호를 잘못 주면
  **2F 병실 문마다 "영안실", 3F 수술실에 "기계실 B1"** 이 붙는다(실제로 그랬다).
  0~2 일반병실 · 3 처치실 · 4 격리A · 5 간호사실 · 6 물품보관 · 7 소독실 ·
  8 검역경고 · 9 예방수칙 · 10 층안내도 · 11 방향표지 · 12 비상구 · 13 찢어진공고 ·
  14 영안실 · 15 기계실B1.
  `roll`(기울임)·`pitch`(눕힘) 인자로 끊어진·떨어진 표지판을 만든다. `glow` 를 끄면 죽은 비상구 등.
- **에셋을 AudioManager 에 등록만 해도 게임에 없는 것이다.** `zombie_notice.mp3` 는 등록만 되고
  두 세션 동안 한 번도 재생되지 않았다. `weapons.js` 의 무기 6종도 같은 상태였다.
  새 에셋을 넣었으면 **호출자가 있는지 grep 으로 확인**한다.
- 화면 촬영 시 **스크린샷 도구는 뷰포트 전체를 안 잡는다**(898px 중 상단 773px). 하단 HUD 를
  확인하려면 `getBoundingClientRect()` 로 좌표를 재거나, 잠깐 위로 올려서 찍는다.
- **`weapons.js` 에 정의만 하면 게임에 없는 것이다.** 스테이지에서 `addWeapon(x,z,id,label)`
  으로 놓아야 플레이어가 얻는다. 쇠지렛대는 GLB 까지 만들어 놓고 두 세션 동안 죽어 있었다.
- 스테이지 파일은 ctx 를 **구조분해**해서 쓴다. 새 헬퍼를 StageLoader 에 추가하면
  쓰는 스테이지 파일 맨 위 구조분해 목록에도 **같이 넣어야 한다** (안 넣으면 `is not defined`).

### 소품 GLB 파이프라인 (`tools/import_props.py`) — 같은 함정을 또 밟지 마라
- **부모(RootNode 등 빈 오브젝트)를 지우기 전에 `parent_clear(KEEP_TRANSFORM)` 을 먼저 부른다.**
  Sketchfab GLB 는 축 변환과 스케일이 부모에 걸려 있어서, 순서를 어기면 모델이 눕거나
  엉뚱한 크기가 된다. 원본은 멀쩡한데 결과만 이상해지므로 원인을 찾기 어렵다.
- **1바이트/픽셀(팔레트·그레이) 이미지는 WebP 로 내보내기가 조용히 실패한다.**
  텍스처 항목만 남고 이미지가 빠져서 three.js 가 파일 전체를 못 읽는다
  (`Cannot read properties of undefined (reading 'uri')`). 그래서 내보내기 전에
  모든 이미지를 RGBA 로 다시 만든다(`normalize_images`). `img.depth` 는 믿을 수 없다.
- **`img.has_data` 는 이미지를 한 번 건드리기 전까지 False 다.** 이걸로 거르면 전부 건너뛴다.
- `bpy.ops.object.transform_apply(rotation=True)` 는 백그라운드에서 조용히 안 먹을 때가 있다.
  메시 데이터를 직접 돌린 뒤 `bpy.context.view_layer.update()` 로 `dimensions` 를 갱신한다.
- **미리보기 렌더는 모델당 프로세스를 새로 띄운다.** 한 프로세스에서 여러 개를 렌더하면
  오프스크린 버퍼가 오염돼 화면이 노이즈로 깨진다.
- 소품 GLB 는 스테이지 조립이 동기라서 **`Game.start()` 에서 전부 프리로드**한다.
  그래서 시작 무기는 Game 생성 시점에 GLB 가 없다 → `weapons.refreshViewModel()` 로 다시 만든다.

### 시각 품질
- **얇은 소품에 텍스처를 넣을 때는 UV 를 면 방향별로 잡아야 한다.** 가장 긴 변 하나로
  U·V 를 같이 스케일하면 짧은 쪽 면이 4cm 마다 반복돼 물결무늬가 어른거린다.
- **환경맵 없이 금속도를 올리면 새까맣게 죽는다.** 반사할 것이 없기 때문. 반대로
  환경맵을 세게 넣으면 어둠이 통째로 걷힌다 — 0.16 정도가 경계다.
- **후처리 AO(GTAO)는 이 게임에 못 쓴다.** 14.5ms → 30.2ms. 해상도를 절반으로 낮춰도
  29.5ms 라 거의 안 줄어든다 — 비용이 해상도가 아니라 깊이·노멀 프리패스(씬을 한 번 더
  그린다)에 있고, 스킨 메시 14개가 두 번 렌더되는 순간 예산이 깨진다. `POST.ao` 로 남겨 뒀다.
- **프레임 측정의 첫 값은 버려야 한다.** 구역을 갈아치운 직후에는 텍스처·셰이더를
  GPU 에 다시 올리는 시간이 섞여 몇 배로 튄다(62.5ms → 재측정 16.3ms). `tools/bench.js`
  가 내부에서 한 번 버리고 두 번째를 돌려준다. 벽시계 대신 GPU 타이머를 쓰면 편차 2%.
- **프레임 측정은 백그라운드 탭에서 심하게 흔들린다.** 한 번 재고 판단하면 오진한다
  (18.2ms 로 나왔던 것이 재측정 시 13.1ms). 워밍업 40프레임 뒤 중앙값을 본다.
- **사망 클립은 루트 수직 이동이 지워져 있어 시체가 공중에 뜬다.** 쓰러진 뒤
  가장 낮은 뼈의 월드 Y 를 재서 바닥에 붙인다(`Zombie._groundOffset`). Box3.setFromObject
  는 스킨 메시의 실제 자세를 반영하지 않아 못 쓴다. 1.2초 만에 사라지던 때는 안 보였다.
- **원뿔 메시로 빛기둥을 그리면 안 된다.** 안에서 보면 원뿔 벽면이 통째로 빛나서
  즉시 가짜로 보인다(시선이 벽면과 나란할수록 밝아진다). 입자를 쓴다.

### 타이틀 화면 (index.html)
- **`::before` 는 생성 순서상 첫 자식이라, 뒤에 오는 절대배치 형제(`.art`)가 그 위에 그려진다.**
  스크림이 안 먹는데 원인을 못 찾으면 이것이다. `#title` 의 겹은 z-index 로 못 박아 뒀다
  (art 0 · grade 1 · 스크림 2 · 활자 3 · 그레인 4 · 암전 5).
- **한 요소에 transform 을 쓰는 애니메이션을 두 개 걸면 뒤엣것이 앞엣것을 덮는다.**
  그래서 시차(.artwrap) · 달리인(.art) · 빛무리(.lamp)를 각각 다른 겹에 두었다.
  filter 도 통째로 교체되므로 깜빡임 키프레임마다 `var(--g)`(그레이딩)를 다시 써야 한다.
- 키아트는 **원본을 그대로 쓰지 않는다.** 맑은 실사 사진이라 그냥 깔면 포스터가 아니라
  "다른 게임의 스크린샷"으로 읽힌다. 채도·대비·미세흐림+인쇄 그레인은 전부 CSS 에 있다.

### 검증
- **브라우저 탭이 백그라운드면 `requestAnimationFrame` 이 안 돈다.** 게임 루프가 멈추므로
  좀비가 안 나오고 상태가 안 변한다. 게임 버그로 오해하기 쉽다.
  루프 없이 확인하려면 `director.update(1/60)` 을 직접 여러 번 부른다.
- 스크린샷 절차: `btn-start` 클릭 → **`game.state='PAUSED'`** → `.screen` 숨김 →
  카메라 직접 세팅(`rotation.order='YXZ'`) → `flashlight.update(dt)` → `post.render(dt)`.

---

## 도구

| 명령 | 하는 일 |
|---|---|
| `npm run dev` | 개발 서버 (5180) |
| `python tools/glb_inspect.py --sizes <glb...>` | Blender 없이 노드·삼각형·실측 크기 조사 |
| `blender --background --python tools/import_props.py -- [이름]` | 소품 GLB 변환 (이름 생략 시 전부) |
| `blender --background --python tools/fbx_to_glb.py -- <fbx폴더> <출력.glb> <본체.fbx>` | 좀비 본체+애니 → GLB. **본체 파일을 반드시 준다** |
| `python tools/glb_shrink_textures.py --max 512 --dry-run <glb...>` | GLB 안 텍스처만 줄인다(Blender 불필요). 지오메트리·배치는 안 건드린다. `--dry-run` 을 빼면 실제로 쓴다 |
| `python tools/glb_strip_animations.py <glb...>` | 클립을 통째로 들어낸다. **뼈 이름이 같은 다른 본체에서 빌려 쓸 때만** 쓴다 (본체당 약 3.1MB 절감) |
| `python tools/preview_sheet.py` | 변환 결과 미리보기를 한 장으로 모음 |
| `python tools/gen_surfaces.py [px]` | 벽·바닥·천장 텍스처 재인코딩 (기본 1024, `512` 로 되돌림) |
| `blender --background --python tools/extract_swing.py -- fbx_src/person public/assets/models/swing_curves.json` | 사람 모션 → 무기 스윙 궤적 |
| 브라우저 콘솔에서 아래 2줄 | **구역별 자동 QA 25항목 × 5구역 = 125** |

```js
const { runQA, summarize } = await import('/tools/qa_stages.js');
console.log(summarize(runQA(window.game)));   // 실패 목록이 좌표까지 찍힌다

// 모션을 프레임 단위로 — 바닥뚫림·공중부양·배속이상(경련/늘어짐)을 잡는다
const { runMotionQA, summarize: mSum } = await import('/tools/qa_motion.js');
console.log(mSum(await runMotionQA(window.game)));

const { benchAll } = await import('/tools/bench.js');
console.table(await benchAll(window.game));   // 구역별 GPU 프레임 시간
```

> 배치를 바꿨으면 이걸 먼저 돌린다. 어두운 화면에서는 눈으로 못 찾는 것이 많다 —
> 벽에 박힌 소품, 갇힌 스폰, 도달 못 하는 상호작용, 예산 초과를 한 번에 잡는다.

---

## 커밋 로그 요약

- `초기 뼈대` — Vite + Three.js, 1인칭 이동, 손전등, 좀비, Director, 무기, HUD
- `전면 개편` — 구역 A·B, 사운드, 좀비 GLB, 부상, 수색
- `Sketchfab 소품 24종 반입` — 변환 파이프라인 + InstancedMesh 배치
- `무기 GLB 3종 + 쇠지렛대`
- `구역 C·D·옥상 추가` — 전 구역 골격 완성 + QA 18항목
