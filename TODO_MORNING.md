# 아침에 직접 해야 할 일

> 2026-08-05 새벽 작업분 인계. **AI 가 대신 할 수 없는 것만** 모았다.
> 코드 작업은 전부 `PROGRESS.md` 의 "다음 할 일" 에 있다. 이 파일은 사람이 해야 하는 것.
> 위에서부터 순서대로 하면 된다. 각 항목은 15~40분 안에 끝나는 크기로 잘라 뒀다.

---

## 0. 먼저 5분: 밤새 뭐가 바뀌었는지 눈으로 확인

```
npm run dev
```
브라우저에서 시작 → **1F → B1 → 2F → 3F → 옥상** 순으로 이어진다.
빨리 확인하려면 개발자 콘솔(F12)에 아래를 붙여넣어 구역을 건너뛴다.

```js
game._nextStage()          // 다음 구역으로. 4번 부르면 옥상
```

바뀐 것 다섯 가지만 보면 된다.
1. **시작 화면**이 검은 배경이 아니라 실제 복도다. 손전등이 훑고 먼지가 떠다닌다
2. 손에 든 무기가 **텍스처 있는 쇠파이프**다 (전에는 절차적 원통)
3. 병실에 **진짜 병상·링거대·커튼**이 있고, 금속이 빛을 받아 형태가 읽힌다
4. 2F·3F·옥상이 생겼고 각 구역에 사건이 있다. 옥상에서 90초 버티면 **EXTRACTED**
5. **포복체**(기어다니는 좀비)가 섞여 나온다 — 낮아서 늦게 보인다

배치를 바꿨다면 브라우저 콘솔(F12)에 아래를 붙여넣어 자동 검사를 돌린다.
```js
const { runQA, summarize } = await import('/tools/qa_stages.js');
console.log(summarize(runQA(window.game)));
```
구역마다 25항목(총 125)을 검사한다. 지금은 전부 통과 상태다.

---

## 1. 라이선스 확인 2건 — **제출 전 필수** (약 20분)

`ASSETS.md §5` 표의 "상업 이용" 칸이 아직 "확인 필요"인 항목이 있다.
계정 로그인이 필요해서 대신 확인할 수 없다.

| 확인할 것 | 어디서 | 표의 어느 칸 |
|---|---|---|
| **ElevenLabs 무료 플랜** 의 상업적 이용 / 어트리뷰션 요구 여부 | elevenlabs.io 약관 | 사운드 34종 전부 |
| **Mixamo(Adobe)** 캐릭터·애니메이션 사용 조건 | Adobe 계정 → Mixamo 약관 | `zombie_shambler.glb` |

- 확인한 내용을 `ASSETS.md §5` 표에 그대로 적는다.
- **어트리뷰션이 필요하면** 게임 내 크레딧 화면(아래 3번)에 한 줄 추가하면 끝난다.
- Sketchfab 24종은 이미 확인 완료다 (전부 CC BY 4.0, `ASSETS.md §4-B`).

---

## 2. 실제로 플레이해서 밸런스 체감 (약 40분)

**이건 반드시 사람이 해야 한다.** 숫자로는 "어렵다"를 알 수 없다.
2~3판 돌려 보고 아래 표에 느낌을 적은 뒤, 값만 바꾸면 된다.
**`src/config/balance.js` 하나만 열면 된다. 다른 파일은 건드리지 않는다.**

| 느낌 | 고칠 값 |
|---|---|
| 좀비가 너무 빨리/느리게 몰린다 | `DIRECTOR` 의 스폰 간격·긴장도 |
| 배터리가 너무 빨리/늦게 닳는다 | `FLASHLIGHT.drainPerSec` |
| 근접이 너무 약하다/세다 | `weapons.js` 의 `damage` · `cooldown` |
| 좀비가 너무 질기다 | `ZOMBIE.shambler.hp` |
| 복도가 너무 어둡다/밝다 | 각 구역 파일의 `meta.mood.ambientIntensity` |

> **한 번에 한 값만 바꾸고 다시 플레이한다.** 두 개를 같이 바꾸면 뭐가 효과였는지 모른다.

체감 메모를 남겨 두면 다음 세션에서 AI 가 이어서 조정할 수 있다.

---

## 3. 구역별 "사건" — **구현해 뒀다. 플레이해 보고 판단만** (약 20분)

세 구역 모두 사건을 넣었다. 값은 전부 `balance.js` 의 `EVENTS` 에 있다.

| 구역 | 사건 | 판단할 것 |
|---|---|---|
| 2F 병동 | 병실 4곳의 **무전기**를 켜야 계단실이 열린다. 켤 때마다 소음 → 웨이브 | 4개가 너무 많은가? |
| 3F 수술부 | 수술실 2곳의 **무영등 전원**을 넣어야 봉쇄문이 풀린다. 켜면 방이 환해진다 | 환해지는 게 오히려 안 무서운가? |
| 옥상 | **신호탄**을 쏘고 헬기가 올 때까지 **90초 버틴다** (15초마다 웨이브, 마지막에 10마리) | 90초가 너무 길거나 짧은가? |

- 마음에 안 들면 `EVENTS` 의 숫자만 바꾸면 된다 (`radioCount` · `lampCount` · `holdSeconds` 등).
- 사건 자체를 바꾸고 싶으면 어떻게 바꿀지만 적어 두면 다음 세션에서 AI 가 교체한다.
- **옥상 90초가 이 게임의 클라이맥스다.** 여기 난이도만큼은 직접 플레이해서 정해야 한다.

---

## 4. 배포 (약 30분)

계정이 필요해서 대신 못 한다. 정적 사이트라 아무 데나 올라간다.

```
npm run build        # dist/ 생성 (16MB)
```

- **가장 쉬운 길**: Vercel 또는 Netlify 에 GitHub 저장소를 연결.
  빌드 명령 `npm run build`, 출력 디렉터리 `dist`.
- GitHub Pages 를 쓸 거면 `vite.config.js` 에 `base: '/저장소이름/'` 을 넣어야 한다.
  (안 넣으면 에셋 경로가 전부 깨진다 — 화면이 까맣게 나온다)
- 원격은 `github.com/yunseok-map/zombie_games` (master).

> 배포 URL 이 나오면 알려 달라. 발표자료·README 에 넣을 자리를 만들어 두겠다.

---

## 5. 데모 영상 (약 40분)

포인터락 때문에 자동 촬영이 안 된다. 직접 플레이하며 녹화해야 한다.

- 길이 **90초 안쪽**. 심사에서 끝까지 보는 시간은 그 정도다.
- 순서 제안: 어두운 복도 걷기(10초) → 좀비 조우·근접(15초) → B1 완전 암흑·레버(20초)
  → 2F 점멸 복도(15초) → 옥상 탈출·EXTRACTED(20초)
- **소리를 반드시 켜고 녹화한다.** 이 게임은 소리가 절반이다.
- 녹화는 Windows `Win+Alt+R` (Xbox Game Bar) 로 충분하다.

---

## 6. 발표자료 (약 40분)

넣을 내용은 이미 저장소에 다 있다. 옮겨 담기만 하면 된다.

| 슬라이드 | 출처 |
|---|---|
| 컨셉·핵심 감정 | `SPEC.md §1` |
| 구조(5개 구역) | `PROGRESS.md` 의 구역 목록 |
| 기술 스택·성능 예산 | `CLAUDE.md §3` + `PROGRESS.md` 예산 표 |
| AI 툴 활용 | `ASSETS.md §2` (Claude Code / Higgsfield / Mixamo / ElevenLabs) |
| 에셋 출처·라이선스 | `ASSETS.md §4-B`, `§5` |

> **심사 대응 포인트**: "AI 로 만들었다"보다 **"AI 로 무엇을 판단했는가"**가 평가된다.
> 예: 100MB 짜리 외부 모델 24종을 16MB 예산에 맞추려고 변환 파이프라인을 따로 만들었다는 것,
> 수술실 모델은 한 덩어리라 소품 분리가 불가능해서 **버리는 판단**을 했다는 것.

---

## 7. (선택) 옥상 설비 모델 — 시간 남으면

지금 옥상의 공조기는 **영안실 서랍벽 모델을 대용**으로 쓰고 있다. 멀리서는 티가 안 나지만
가까이 가면 서랍이 보인다. Sketchfab 에서 아래를 받아 `tools/source_models/` 에 넣고
`tools/import_props.py` 의 MANIFEST 에 한 줄 추가하면 교체된다.

- 검색어: `rooftop hvac unit` · `air conditioning unit` · `industrial roof vent`
- 조건: **CC Attribution 이하**, 삼각형 5천 이하
- 받은 뒤 알려 주면 변환·배치까지 AI 가 처리한다.

---

## 안 해도 되는 것 (이미 끝남)

- ~~Sketchfab 모델 반입~~ — 24종 변환·배치 완료
- ~~라이선스 종류 확인(Sketchfab)~~ — 전부 CC BY 4.0, 표에 기록됨
- ~~쇠지렛대 무기 추가~~ — `weapons.js` 에 정의 완료
- ~~2F·3F·옥상 만들기~~ — 골격 완료, 걸어서 통과됨
- ~~에셋 용량 줄이기~~ — 107MB → 16MB
