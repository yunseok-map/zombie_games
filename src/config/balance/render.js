/**
 * 연출·렌더 — 분위기·후처리·산포·HUD 연출·표면·성능 예산
 *
 * balance.js 에서 갈라져 나왔다. **직접 임포트하지 말고 balance.js 를 임포트한다** —
 * 그쪽이 전부 다시 내보내므로 어느 값이 어느 파일로 갔는지 몰라도 된다.
 * (CLAUDE.md §1-1: 수치는 config/ 안에만 존재한다)
 */


export const FX = {
  fogColor: 0x05070a,
  fogDensity: 0.055,       // 실내 기본 (구역별로 StageLoader 가 덮어씀)
  ambientIntensity: 0.055, // 거의 0 — 어둠이 기본이다

  /**
   * 톤매핑 노출. **0.95 → 1.5 (2026-08-08).**
   *
   * "손전등에만 의존하니 진행이 어렵다"는 보고를 재 봤더니, 손전등을 켠 1F 복도조차
   * **화면의 74%가 사실상 순수한 검정**(휘도 0.02 미만)이었다. 손전등이 비추는 자리마저
   * 어두워서, 비춰도 바닥인지 문인지 구분이 안 되는 상태였다.
   *
   * 먼저 의심한 ambient 는 **레버가 아니었다** — 0.06 → 0.36(6배)으로 올려도
   * 먹통 77.7% → 69.1%, 평균 밝기 0.0173 → 0.0193 으로 거의 안 움직인다.
   * 포그 색이 0x05070a(거의 검정)라 거리가 있는 픽셀이 전부 그 색으로 수렴하기 때문이다.
   * 포그 색·포그 밀도·비네트도 각각 5%p 안쪽이었다. 노출만 통째로 먹었다.
   *
   * | 노출 | 1F 먹통 비율 | 하얗게 탄 픽셀 |
   * |---|---|---|
   * | 0.95 (이전) | 74.4% | 0% |
   * | **1.5 (지금)** | **42.7%** | **0%** |
   * | 1.8 | 37.9% | 0% |
   *
   * 1.8 이 아니라 1.5 인 이유는 **어둠을 없애려는 게 아니기 때문**이다.
   * 노출은 화면 전체에 곱해지므로 손전등 원뿔과 바깥의 **대비는 그대로 유지된다** —
   * 원뿔 밖은 여전히 검정이고(CLAUDE.md §5-2), 다만 원뿔 안이 읽힌다.
   * 5구역 × 사건 전/후 10가지 상태에서 하얗게 타는 픽셀은 전부 0%다(옥상만 0.01%).
   * 그레인은 톤매핑 뒤에 더해지므로 노출을 올려도 노이즈가 같이 커지지 않는다.
   */
  exposure: 1.5,
  // 환경맵 세기. 금속·유리가 형태를 읽히게 하는 정도만 — 올리면 어둠이 걷힌다
  envIntensity: 0.16,
  emergencyLightColor: 0xff3b2e,
  emergencyLightIntensity: 0.9,
  emergencyLightRange: 7,
  emergencyLightDecay: 1.6,   // 예전에는 Atmosphere.js 에 박혀 있었다
  /**
   * **씬에 실제로 존재하는 비상등 개수** (fx/Atmosphere.js `_slots`).
   * 구역에 몇 개를 배치하든 이 수만 존재하고, 매 프레임 지금 화면에 영향을 주는
   * 것들이 슬롯에 실린다.
   *
   * 왜 고정인가 — three 의 프로그램 캐시 키에 광원 **개수**가 들어간다.
   * 구역마다 12 → 6 → 14 → 9 → 5 로 바뀌던 탓에 구역을 옮길 때마다 씬의 재질이
   * 통째로 재컴파일됐다(프로그램 누적 68 → 200). 개수를 붙박아 두면 사라진다.
   *
   * 왜 6인가 — 실측이다. 5개면 전부 켠 화면과 **픽셀 단위로 동일**했고
   * (세 구역 x 여섯 시점, 최대 차이 0), 4개는 1F 에서 눈에 띄게 어두워졌다.
   * 여유 한 칸을 더 둔 값이다. 배치를 크게 바꾸면 다시 재야 한다
   * (scratchpad 의 light_visual.mjs · light_reach.mjs).
   */
  emergencyLightSlots: 6,
};


/** 후처리 — 텍스처만큼이나 "게임처럼 보이는가"를 좌우한다 (fx/PostFX.js) */
export const POST = {
  enabled: true,
  bloomStrength: 0.28,     // 손전등 핫스팟·비상등이 번지는 정도
  bloomRadius: 0.7,
  bloomThreshold: 0.85,    // 이 밝기 위만 번진다. 낮추면 전체가 뿌예진다
  grain: 0.03,             // 필름 노이즈. 어두운 곳일수록 강해진다. 0.08 넘으면 화면이 지저분해진다
  vignette: 0.5,           // 화면 가장자리 어둡게 — 시야가 좁아 보인다
  aberration: 0.004,       // 가장자리 색수차. 과하면 싸구려로 보인다
  // 후처리 버퍼의 MSAA. 4 → 2 → **0**. 그레인·블룸이 가장자리를 어차피 뭉개서
  // 어두운 화면에서는 차이를 거의 못 느끼는데, 약한 GPU 에서는 이게 비싸다.
  // 심사자 PC 를 고를 수 없으므로 안 보이는 품질보다 안 끊기는 쪽을 택한다.
  //   측정(Radeon 780M · 1600x900 · 좀비 14): MSAA2 18.3ms → MSAA0 17.1ms.
  //   계단이 거슬리면 2 로 되돌려라. 대신 프레임 1.2ms 를 낸다.
  msaaSamples: 0,

  // 블룸을 굽는 해상도 배수. 측정해 보니 1.0 과 0.5 의 차이가 0.5ms 라 사실상 공짜였다
  // (UnrealBloomPass 가 내부에서 이미 절반부터 밉을 쌓기 때문). 공짜면 좋은 쪽을 쓴다 —
  // 절반으로 구우면 손전등 핫스팟 둘레에 계단진 번짐이 생긴다.
  bloomScale: 1.0,

  // 앰비언트 오클루전. 소품이 바닥에 닿아 보이게 하지만 **이 게임에서는 너무 비싸다.**
  //   측정(2F + 좀비 14): 14.5ms → 30.2ms. 해상도를 절반으로 낮춰도 29.5ms 로 거의 그대로다.
  //   비용이 해상도가 아니라 깊이·노멀 프리패스(씬을 한 번 더 그린다)에 있기 때문이고,
  //   스킨 메시 14개가 두 번 렌더되는 순간 60fps 예산이 깨진다.
  // 좀비 수를 줄이거나 고사양만 노린다면 true 로 켜면 된다.
  ao: false,
  aoRadius: 0.28,      // m. 크게 잡으면 벽 전체가 지저분하게 어두워진다
  aoStrength: 1.1,
  aoSamples: 8,        // 낮추면 얼룩이 생기고, 높이면 프레임을 먹는다
  aoScale: 0.5,        // AO 를 굽는 해상도 배수. 1.0 은 프레임을 배로 먹는다

  /**
   * 맞았을 때 **렌더된 화면 자체가** 반응한다.
   * 예전에는 피격 반응이 DOM 빨간 오버레이와 카메라 흔들림뿐이라, 물리는 순간이
   * "세계가 흔들렸다"가 아니라 "UI 가 알림을 띄웠다"로 읽혔다.
   * 좀비 사망·헤드샷까지 여기에 물리면 화면이 계속 출렁여 멀미가 난다 —
   * **플레이어가 맞았을 때만** 건다.
   */
  hurtPulse: 1.0,      // 한 대 맞을 때 차오르는 양
  hurtDecay: 5.5,      // 초당 감쇠
  hurtDesat: 0.55,     // 채도가 빠진다 — 피가 도는 소리가 안 들리는 그 느낌
  hurtAberration: 0.05,
  hurtVignette: 0.35,
  /**
   * 붉은 기의 세기. **곱셈이 아니라 덧셈이다.**
   *
   * 처음에는 화면 밝기에 비례해서 붉게 만들었는데, 이 게임에서 맞는 순간은 대부분
   * 손전등 밖의 칠흑이라 **화면에 아무 일도 안 일어났다** (검정에 뭘 곱해도 검정이다).
   * 지금은 밝기와 무관하게 더한다 — 가운데는 rimBase 만큼, 가장자리로 갈수록 rim 만큼.
   * r² 는 화면 구석에서 0.5 이므로 구석의 실제 가산량은 tint x (rimBase + 0.5 x rim) 이다.
   *
   * **이 셰이더는 OutputPass 앞이라 선형 공간이다.** 여기서 더한 0.2 가 화면에서는
   * sRGB 130 이 되어 나온다 — 처음에 0.9/1.15 를 넣었다가 화면이 통째로 빨간 판이
   * 됐다(평균 빨강 3 → 118). **감마를 생각하고 값을 잡아라.**
   *
   * 값은 **찍지 말고 재라.** 선형 + ACES 톤매핑을 거쳐서 값과 화면이 비례하지 않는다.
   * 손전등을 끈 칠흑에서 잰 표 (빨강 채널 평균, scratchpad/qa/hurt_sweep.mjs):
   *
   *   tint  edge0 │ 전체   중앙   구석
   *   ─────────────┼──────────────────
   *   평상시        │  0.0    0.0    0.0
   *   0.30  0.12   │ 31.7    1.3  134.7
   *   0.60  0.12   │ 53.1    4.1  195.2
   *   0.80  0.12   │ 64.0    6.2  218.4
   *   0.60  0.06   │ 73.8    4.1  199.9   ← edge0 를 낮추면 화면 전체가 번진다
   *   0.60  0.18   │ 34.9    4.1  187.4   ← 채택. 구석은 그대로, 번짐만 빠진다
   *   1.00  0.18   │ 49.1    8.4  227.7
   */
  hurtTint: 0.60,
  hurtRim: 1.0,          // 구석 — 시야가 붉게 닫히는 느낌을 만드는 쪽
  hurtRimBase: 0.012,    // 화면 전체 최소치. 올리면 곧바로 "빨간 필터"가 된다
  hurtEdgeStart: 0.18,   // 이 r² 부터 붉어진다. r² 는 화면 구석에서 0.5, 한가운데 0
};


/** 핏자국 · 의료폐기물 산포 (world/Scatter.js) — 폐병원의 "사람이 있었다"는 증거 */
export const SCATTER = {
  seed: 20290417,          // 기본 시드 (randomPerRun 이 false 일 때만 쓴다)

  // **판마다 전리품·잔해 자리를 섞는다.** 같은 자리에서 같은 것이 나오면 두 번째 판부터
  // 탐색이 사라지고 외운 길만 걷게 된다.
  //   무엇이 바뀌나: 수납장에서 나오는 물건 종류, 잔해·핏자국 배치, 소품 각도.
  //   무엇이 안 바뀌나: 벽·문·사건·스폰 지점 — 레벨 구조는 손대지 않는다.
  //     (구조까지 흔들면 QA 로 검증한 동선이 매 판 달라져서 보증할 수 없다)
  randomPerRun: true,
  // 디버깅용 고정 시드. 숫자를 넣으면 randomPerRun 을 무시하고 그 판을 재현한다.
  // 이상한 배치를 발견했을 때 `game.stageLoader.lastSeed` 를 여기 적으면 다시 볼 수 있다.
  forceSeed: null,
  bloodRoughness: 0.42,    // 바닥보다 매끈 = 손전등에 젖은 듯 반짝인다

  debrisPerSqm: 0.35,      // 1m² 당 잔해 개수. 0.8 넘으면 쓰레기장처럼 보인다

  // 병실 내부 밀도. 복도는 사람이 지나가는 곳이라 비어 있어도 되지만, 병실은
  // **사람이 살던 방**이라 비어 있으면 세트장처럼 보인다. 여기만 따로 올린다.
  wardDebris: 0.62,        // 병실 바닥 잔해 밀도 (복도 0.3 대비)
  wardBloodMin: 2,         // 방 하나에 들어가는 핏자국 최소 개수
  wardBloodMax: 4,         // 데칼 하나가 드로우콜 하나다 — 예산(300) 안에서 올린다
  debrisMaxTotal: 420,     // 인스턴싱이라 드로우콜은 3개로 고정, 삼각형 예산만 본다
  debrisWeights: { syringe: 5, vial: 3, paper: 2 },

  /**
   * 좀비가 죽은 자리에 남는 자국 (world/StageLoader.js).
   *
   * 예전에는 `const MAX_KILL_MARKS = 22` 로 코드에 박혀 있었고, 자국 하나가
   * 메시 하나 = **드로우콜 하나**였다 — 잘 싸운 판에서 예산(300)의 7%를 먹었다.
   * 지금은 인스턴싱 하나라 개수와 무관하게 1개다. 그래서 상한을 올릴 수 있다.
   * 다 차면 링버퍼가 가장 오래된 것부터 덮어쓴다(사라지는 게 아니라 옮겨간다).
   */
  killMarkMax: 48,
  killMarkSizeMin: 0.85,   // CORPSE.bloodSize 에 곱한다 — 다 같은 크기면 도장처럼 보인다
  killMarkSizeMax: 1.20,
  // 전투 중 핏자국(IMPACT.decalY=0.007)보다 아래, 배치 핏자국(0.006)보다 위.
  // 셋이 겹치는 자리가 반드시 생기므로 층을 확실히 나눈다 (z-파이팅)
  killMarkY: 0.0065,

  // 잔해 색 — 바닥이 어두워서 너무 낮추면 아예 안 보인다. 주사기는 반짝여야 눈에 띈다
  syringeColor: 0x5c666d,
  vialColor: 0x49563f,
  paperColor: 0x615c4e,
};


/**
 * HUD 피드백 (ui/HUD.js · index.html).
 *
 * 지금까지 HUD.js 안에 흩어져 있던 문턱값 12개를 여기로 모았다 (CLAUDE.md §1-1).
 * 흩어져 있는 것이 규칙 위반이라서가 아니라 **서로 어긋나 있었기 때문**이다 —
 * 막대 맥동은 0.35, 화면 가장자리는 0.4 라 그 사이 구간에서 두 신호가 엇갈렸다.
 * 체력 단계를 하나의 사다리로 정리한다.
 *
 * CSS 로 내려보내는 값은 HUD 생성 시 **한 번만** 커스텀 프로퍼티로 심는다.
 */
export const HUD_FX = {
  // ── 체력 사다리 ──
  hpLimpAt: 0.50,      // "다리 부상 — 절뚝인다"
  hpHurtAt: 0.40,      // 화면 가장자리 심박 시작 + 막대 맥동 (예전 0.4 · 0.35 를 하나로)
  hpCritAt: 0.25,      // "치명상" + 막대 발광
  battLowAt: 0.20,
  ammoLowMag: 3, ammoLowThrow: 1,

  // ── 크로스헤어 ──
  // 벌어짐은 JS 에서 지수 보간한다. CSS transition 을 걸어 놓고 매 프레임 덮어쓰면
  // 트랜지션이 매번 처음부터 다시 시작해 **목표에 영영 도달하지 못한다** —
  // 달리기를 멈춰도 벌어진 채로 남아 "지금 쏴도 된다"는 신호가 늦게 온다.
  crossSpread: 0.90, crossOpacity: 0.85, crossOpacityDrop: 0.45,
  crossLerp: 12,       // 지수 보간 속도. dt 기반이라 프레임률과 무관하다
  crossKick: 0.35,     // 한 발 쏠 때마다 벌어지는 양 — 크로스헤어가 무기와 연결된다

  // ── 타격 확인 ── 어두워서 좀비가 잘 안 보이는 게임인데 지금까지 화면이
  // "맞혔는지"를 한 번도 말하지 않았다. 밝거나 크면 아케이드가 되므로 짧게 끝낸다.
  hitFlash: 0.22, headFlash: 0.30, killFlash: 0.42,

  // ── HP 지연 잔상 ── 채움 뒤에 늦게 따라오는 겹. 이 지연 하나가 "방금 이만큼
  // 깎였다"를 만든다. 스태미나에는 붙이지 않는다 — 항상 움직여서 계기판이 시끄러워진다.
  ghostDelay: 0.35, ghostFall: 0.50,

  // ── 심박 ── 예전에는 HP 39% 와 3% 가 똑같은 속도로 맥동했다. 죽기 직전이라는
  // 정보가 화면에 없었다. 사인파 한 번은 심박이 아니라 호흡이다 — 두근·쿵·쉼이어야 한다.
  heartMinBpm: 64, heartMaxBpm: 150,

  // ── 피격 방향 ── 붉은 물듦은 "아프다"를 맡고, 방위 호가 "어느 쪽"을 맡는다.
  // 물듦보다 오래 남아야 정보다 — 고개를 돌리는 동안에도 보여야 한다.
  dmgFlash: 0.40, dirArcLife: 1.10, dirArcSlots: 4,

  // ── 물림 ── 남은 시간이 이 비율 아래면 제한시간 막대가 맥동한다
  grabUrgentAt: 0.28,
};


export const SURFACE = {
  // 텍스처 한 장이 덮는 실제 크기(m). 작을수록 무늬가 촘촘해진다.
  wallTile: 2.4,
  floorTile: 2.0,
  ceilingTile: 1.2,
  propTile: 0.55,        // 소품은 작아서 벽보다 촘촘해야 결이 보인다

  // 색 보정 — 텍스처는 회색 중립이다. 병원 톤은 여기서 입힌다. (ASSETS.md §3)
  wallTint: 0x7e8c78,      // 병든 institutional green
  floorTint: 0x585d56,
  ceilingTint: 0x4a504a,

  // 요철 세기. 1보다 크면 손전등이 지나갈 때 음영이 강해진다 — 실사감의 핵심
  wallNormalScale: 1.35,
  floorNormalScale: 0.85,
  ceilingNormalScale: 1.0,
  propNormalScale: 0.7,

  // 바닥을 비스듬히 볼 때의 선명도. 8 → 16 (하드웨어 최대치). 측정 +0.4ms 로 거의 공짜인데,
  // 복도를 앞으로 보며 걷는 게임이라 **화면 대부분이 비스듬한 바닥**이다. 체감이 가장 큰 값.
  anisotropy: 16,
  viewModelDim: 0.22,      // 무기 뷰모델 색을 누르는 배수 (손전등 바로 앞이라 안 누르면 탄다)

  // 외부 GLB 소품(world/PropModels.js). Sketchfab 재질은 밝은 실내등 기준이라
  // 손전등(26cd) 아래에서는 그대로 쓰면 하얗게 탄다. 텍스처는 살리고 색만 누른다.
  propModelDim: 0.55,
  propModelRoughMin: 0.45,   // 너무 반질거리면 손전등 반사가 점으로 튄다
  propModelMetalMax: 0.55,   // 환경맵이 생겨서 예전(0.35)보다 금속을 살려도 된다
  propModelEnv: 1.0,         // 소품의 환경맵 반사 세기 (전체 세기는 FX.envIntensity)
};


/**
 * 성능 — **심사자의 PC 사양을 고를 수 없다.** 웹 링크로 제출하므로 사무용 노트북의
 * 내장 그래픽에서 열릴 수도 있다. 그래서 "내 PC에서 60fps"가 아니라
 * "약한 PC에서도 안 끊긴다"가 기준이다.
 */
export const PERF = {
  /**
   * 캔버스 컨텍스트의 MSAA.
   *
   * 후처리가 켜져 있으면 씬은 컴포저의 렌더타겟에 그려지고, 기본 프레임버퍼에는
   * 마지막 패스가 **화면을 덮는 사각형 하나**만 그린다. 사각형 하나에는 내부
   * 에지가 없으므로 MSAA 가 개선할 대상이 아예 없다 — 그런데도 브라우저는
   * 멀티샘플 백버퍼(1600x900 기준 컬러+깊이 약 46MB)를 잡고 매 프레임 리졸브한다.
   * POST.msaaSamples 를 4→2→0 으로 내린 결정의 **빠진 절반**이다.
   *
   * **POST.enabled 를 false 로 되돌린다면 여기도 true 로 바꿔야 한다** —
   * 그 경로에서는 캔버스 MSAA 가 유일한 안티앨리어싱이다.
   */
  canvasMsaa: false,

  // 렌더 해상도 배율 상한. devicePixelRatio 2 인 노트북에서 2 로 두면 픽셀을 4배 그린다 —
  // 후처리까지 그 해상도로 도니 가장 큰 낭비였다. 1.5 면 선명함은 거의 그대로다.
  // 해상도 배율의 천장·바닥·출발점.
  //
  // **천장은 devicePixelRatio 를 넘어도 된다** — 넘으면 크게 그려서 줄이는 것(수퍼샘플링)이
  // 되어 계단이 가장 깨끗하게 사라진다. MSAA 보다 비싸지만 가장자리만이 아니라 화면 전체가
  // 좋아진다 (측정: MSAA2 +3.3ms 로 가장자리만 vs 해상도 1.25배 +7.9ms 로 전부).
  // 천장을 높게 두는 것이 안전한 이유는 **적응형이 실제로 동작하기 때문**이다.
  // 강한 PC 는 여기까지 올라가고, 약한 PC 는 알아서 바닥으로 내려간다.
  pixelRatioMax: 1.75,
  pixelRatioMin: 0.75,     // 적응형이 내려갈 수 있는 바닥
  // 출발점. 천장에서 시작하면 약한 PC 가 첫 몇 초를 버벅이며 연다.
  // 낮게 시작해서 여유가 확인될 때만 올라가는 편이 첫인상이 훨씬 낫다.
  pixelRatioStart: 1.0,

  // 적응형 해상도 — 프레임이 나쁘면 스스로 낮추고, 여유가 있으면 되돌린다.
  // 심사자 PC 를 모르는 상태에서 "안 끊긴다"를 보장하는 유일한 방법이다.
  //
  // **주의**: 이 값들은 PostFX.setSize 가 컴포저의 pixelRatio 를 같이 갱신해야만 의미가 있다.
  // 그 갱신이 빠져 있던 동안에는 배율을 낮춰도 실제 렌더 해상도가 안 변해서 적응형이 무효였다.
  adaptive: true,

  // **이 두 값은 vsync 를 기준으로 읽어야 한다.** 브라우저는 주사율에 맞춰 화면을 내보내므로,
  // 12ms 에 그려도 60Hz 에서는 프레임 간격이 16.7ms 로 측정된다. 그래서 예산을 16.5 로
  // 잡으면 여유가 있어도 "항상 초과"로 읽혀 해상도가 바닥까지 내려가고 다시 안 올라온다.
  //   16.7ms 근처 = vsync 를 지키고 있다 → 여유가 있다는 뜻
  //   20ms 이상   = vsync 를 놓치기 시작했다 → 낮춰야 한다
  frameBudgetMs: 20.0,     // 이보다 오래 걸리면 낮춘다 (60Hz 에서 vsync 를 놓치는 구간)
  frameGoodMs: 17.5,       // 이보다 빠르면 여유가 있다고 본다 (vsync 를 지키고 있다)
  adaptWindow: 1.0,        // 판단에 쓰는 관찰 시간(초). 짧으면 해상도가 출렁인다

  // **비대칭으로, 그리고 수렴하게 조정한다.**
  // 60Hz vsync 에서 프레임 간격은 16.7 아니면 33.3 으로만 튄다 — 중간이 없다.
  // 그래서 조금만 잘못 잡아도 두 값을 오가고, 배율이 바뀔 때마다 렌더 버퍼를 다시
  // 잡느라 그때마다 한 번씩 끊긴다 (측정: 오르내림이 반복되자 33ms 초과가 2 → 38).
  //
  //   많이 초과 → adaptStep 으로 성큼 (버벅임을 빨리 끝내는 게 우선)
  //   조금 초과 → adaptStepFine 으로 한 칸만 (멀쩡한 배율을 지나치지 않게)
  //   여유 있음 → adaptStepUp 으로 한 칸, 그것도 연속 확인 뒤에만
  //   한 번 못 버틴 배율은 기억해 두고 **다시는 그 위로 안 올라간다** → 결국 한 값에 정착한다
  adaptStep: 0.15,         // 많이 초과했을 때 내리는 폭
  adaptStepFine: 0.05,     // 조금 초과했을 때 내리는 폭
  adaptStepUp: 0.05,       // 올릴 때 늘리는 폭
  adaptUpAfter: 4,         // 이만큼 연속으로 여유가 확인돼야 올린다 (adaptWindow 단위)
  adaptBadRatio: 1.5,      // 예산 대비 이 배를 넘으면 "많이 초과"로 본다

  // 이보다 오래 걸린 프레임은 평균에서 뺀다. 셰이더 컴파일·GC 로 한 번 튄 것은
  // "렌더가 무겁다"는 신호가 아닌데, 넣고 세면 시작 직후 스톨 몇 번에 해상도가
  // 바닥까지 내려가고 정착 후에도 안 올라온다 (측정: 0.75 에 눌린 채 76fps).
  stallMs: 50,

  /**
   * 구역을 연 뒤 **제자리에서 한 바퀴 돌며 미리 그려 두는** 프레임 수
   * (`Game._warmupShadows`). 그림자 패스의 깊이 재질은 물체가 손전등 원뿔에
   * 처음 들어올 때 컴파일되므로, 이걸 안 하면 플레이 중 시야를 크게 돌릴 때마다
   * 그 각도에서 78~234ms 씩 튄다.
   *
   * 늘리면 빠지는 각도가 줄지만 구역 전환이 그만큼 길어진다. 12면 30도 간격이라
   * 원뿔 각(26도)보다 촘촘해서 빈 각도가 남지 않는다. 0 으로 두면 예전 동작.
   */
  warmupSteps: 12,
  warmupPitch: 0.42,       // 위아래도 번갈아 본다 (rad). 천장·바닥은 벽과 재질이 다르다
  warmupDt: 1 / 60,        // 손전등 갱신에 넘길 dt. 깜빡임 위상만 움직인다
  // 워밍업은 **아주 작게** 그린다. 컴파일되는 프로그램은 해상도와 무관하다.
  // 풀 해상도 + 4096² 그림자로 36프레임을 돌렸더니 로딩이 7초 → 44초가 됐다.
  warmupPixelRatio: 0.1,
  warmupShadowSize: 256,   // 워밍업 동안의 그림자 맵. 끝나면 FLASHLIGHT.shadowMapSize 로 되돌린다

  /**
   * 프레임 상한. **분위기를 위한 값이다.** 0 = 상한 없음(브라우저 vsync 에 맡긴다).
   *
   * 기본이 0 인 이유 — vsync 가 이미 주사율로 묶어 준다. 그 위에 상한을 또 걸면
   * 이득 없이 지터만 는다 (측정: 60 상한을 걸자 33ms 초과 프레임이 663 중 2 → 9).
   * 게다가 심사자 모니터를 고를 수 없는데, 120·144Hz 에서 60 은 나눠떨어지지 않아
   * (144/60 = 2.4) 프레임 간격이 불균일해진다.
   *
   * 값을 넣는다면 **주사율의 약수만** 쓸 것:
   *   30 — 영화적 무게감. 60Hz 에서 정확히 2 vsync 라 저더가 없다. 다만
   *        **이 게임에는 모션블러가 없어서** 필름 룩이 아니라 끊김으로 읽힐 수 있고,
   *        1인칭 마우스 조준이라 조준 지연이 커진다. 눈으로 보고 판단할 것.
   *
   * 무게감이 목적이라면 프레임보다 카메라 관성·헤드밥·애니메이션 배속을 먼저 만져라.
   */
  fpsCap: 0,
};


/**
 * 열쇠 아이템(카드키) 후광.
 *
 * 카드는 실물 크기 그대로 **8.5cm × 5.4cm** 다. 방이 어둡고 검사대 상판이 3.6m 라
 * 문가에서 보면 몇 픽셀이고, 손전등을 정확히 그 지점에 비추지 않으면 그냥 안 보인다.
 * "가끔 1층에서 카드키가 안 나온다"의 정체가 이것이었다 — 배치는 매 판 똑같다.
 *
 * 자체발광만으로는 부족하다. 발광은 **면적에 비례**해서, 면적이 작으면 밝기를
 * 올려도 픽셀 수가 안 늘어난다. 그래서 카드에 붙어 다니는 후광을 따로 띄운다.
 * 벽 뒤로는 안 비친다(깊이 판정을 그대로 쓴다) — 방에 들어와야 보인다.
 */
export const KEY_ITEM_GLOW = {
  size: 0.5,        // 후광 지름(m). 이보다 크면 "아이템"이 아니라 "조명"으로 읽힌다
  color: 0x51ffb2,
  opacity: 0.4,     // 평균 밝기
  pulse: 0.26,      // 밝기 진폭 — 숨쉬듯 흔들려야 눈이 잡는다
  pulseHz: 0.5,     // 초당 왕복 횟수
  y: 0.06,          // 카드 위로 띄우는 높이(m)
};
