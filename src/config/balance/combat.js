/**
 * 전투 — 무기 휘두름·재장전·뷰모델·총구·타격·넉백·화면 흔들림
 *
 * balance.js 에서 갈라져 나왔다. **직접 임포트하지 말고 balance.js 를 임포트한다** —
 * 그쪽이 전부 다시 내보내므로 어느 값이 어느 파일로 갔는지 몰라도 된다.
 * (CLAUDE.md §1-1: 수치는 config/ 안에만 존재한다)
 */


/**
 * 무기 스윙 궤적 (weapons/SwingCurves.js).
 * 사람 근접공격 모션(Mixamo)에서 오른손 궤적만 뽑아 뷰모델에 입힌다.
 * 곡선은 진폭 1 로 정규화돼 있으므로 **실제 크기는 여기서 정한다.**
 * 0 으로 두면 곡선을 끄고 기존 절차적 스윙으로 돌아간다.
 */
export const WEAPON_SWING = {
  enabled: true,
  // 근접. 원본 회전 진폭이 300도라 그대로 쓰면 무기가 화면에서 한 바퀴 돈다
  posScale: 0.19,
  rotScale: 0.30,
  // 총기 반동. 훨씬 작게 — 총은 휘두르는 게 아니라 튀는 것이다
  gunPosScale: 0.05,
  gunRotScale: 0.09,
  // 곡선을 얼마나 섞을지. 1 이면 곡선만, 0 이면 기존 절차적 스윙만.
  // 절차적 쪽에 예비동작(뒤로 당김)이 잘 잡혀 있어서 조금 남겨 두는 편이 낫다
  blend: 0.82,

  /**
   * ── 근접 데미지가 들어가는 시점 ──
   *
   * 예전에는 **클릭한 그 프레임에** 판정이 끝났다. 그런데 무기가 화면에서 가장
   * 뻗는 순간은 스윙의 54% 지점(쇠파이프 기준 0.258초 뒤)이다. 즉 화면에서는
   * 아직 쉬는 자세인데 좀비가 이미 피를 뿜고 젖혀졌다. 히트스톱까지 그 시점에
   * 걸려서 "클릭 → 정지 → 그제서야 휘두름" 순서가 됐다.
   *
   * 좀비 쪽은 `ATTACK.contact` 로 클립별 접촉 지점을 실측해 이미 해결한 문제인데,
   * **플레이어 쪽만 옛날 구조로 남아 있었다.**
   *
   * 부수 효과로 판정이 **접촉 시점의 시선**을 쓰게 된다 — 휘두르는 도중 몸을 돌리면
   * 빗나간다. 좀비와 같은 규칙이 되는 것이라 이쪽이 맞다.
   * 쿨다운은 클릭 시점에 그대로 걸리므로 **연타 리듬은 1프레임도 안 바뀐다.**
   */
  contact: 0.46,          // 스윙 길이 대비 접촉 비율. arc 피크(0.54)보다 살짝 앞 —
                          // 정확히 피크면 이미 되돌아오기 시작한 것으로 보인다

  /**
   * 접촉까지의 초 상한. **0.17 → 0.30 (2026-08-08).**
   *
   * 위 `contact: 0.46` 이 설계값인데, **이 상한이 그 값을 조용히 덮고 있었다.**
   * `_swingDur = min(cooldown x 0.92, 0.62)` 이므로 근접 5종 중 4종이 상한에 걸렸다 —
   * 무겁고 느린 무기일수록 더 크게 어긋나는, 정확히 반대로 된 상태였다.
   *
   * 궤적값은 `easeIn(strike)^3` 이라 앞쪽이 아주 납작하다. 그래서 접촉 시점이 조금만
   * 앞당겨져도 무기는 **거의 안 움직인 자리**에 있다. 쇠파이프 실측:
   *
   * | | 접촉 시각 | 스윙 진행 | **그때 궤적값** |
   * |---|---|---|---|
   * | 이전 (상한 0.17) | 0.170s | 36% | **0.057** |
   * | 지금 (상한 0.30) | 0.220s | 46% | **0.394** |
   *
   * 즉 예전에는 **궤적의 5.7% 지점**, 사실상 쉬는 자세에서 피가 튀고 좀비가 젖혀졌다.
   * "휘두르는 것 같지 않다"의 정체가 이것이다. 지금은 6.9배 뻗은 자리에서 닿는다.
   *
   * 반응이 느려지지 않는 이유 — 쿨다운은 **클릭 시점**에 그대로 걸리므로 연타 리듬은
   * 1프레임도 안 바뀐다. 예비동작(0~24%)은 클릭 즉시 시작하므로 화면은 바로 반응한다.
   * 늘어난 것은 클릭에서 피해까지의 0.05초(쇠파이프)~0.115초(도끼)뿐이고,
   * 그 시간에 무기가 실제로 뻗기 때문에 지연이 아니라 **무게**로 읽힌다.
   */
  contactMaxDelay: 0.30,

  /**
   * 닿았을 때의 **반발**. 이게 없으면 살을 치든 허공을 치든 궤적이 100% 같아서
   * "휘두르는 중에 화면이 흔들렸다"로만 읽힌다. 무기가 닿은 자리에서 멎었다가
   * 손 쪽으로 튕겨 돌아와야 "부딪혔다"가 된다.
   */
  impactSlow: 0.72,       // 접촉 순간 스윙 진행이 이 비율만큼 느려진다 (무기가 걸린다)
  impactDecay: 8.0,       // 반발이 잦아드는 속도(초당)
  impactPush: 0.085,      // 카메라 쪽으로 되튀는 거리(m)
  impactPitch: 0.30,      // 되튀며 젖혀지는 각도(rad). 0.4 를 넘기면 무기가 화면 밖으로 빠진다
  impactRoll: 0.22,       // 진행 방향 **반대**로 비틀리는 각도(rad)

  /**
   * 벽·소품을 쳤을 때. Collision 은 2D AABB 라 높이를 모르므로 **벽으로 등록된
   * 박스만** 잡힌다 (천장·낮은 침대는 못 잡는다). 탐침 위치를 값으로 빼 둔 이유는
   * 문틀 옆에 서서 정면을 치면 벽으로 오판할 수 있기 때문이다.
   */
  wallProbe: 0.85,        // 사거리 대비 탐침 거리 — 여기까지의 **경로를 훑는다**
  // 훑는 간격(m). **벽 두께(0.2m)보다 작아야 한다** — 크면 벽을 건너뛴다.
  // Collision.segmentBlocked 의 기본값과 같은 이유의 값이다.
  wallProbeStep: 0.12,

  // 근접 헤드샷 판정. 몸을 선분으로 봤을 때 이 위(0~1)를 맞히면 머리다.
  // 총 쪽 포복체 판정과 같은 값을 쓴다 — 규칙이 둘이면 하나는 반드시 틀어진다.
  meleeHeadS: 0.74,
};


/**
 * 재장전 동작 — **재장전 중의 취약함이 이 게임의 공포 장치인데**(CLAUDE.md §5-4)
 * 그동안 화면에 있는 것은 HUD 의 진행 링뿐이었다. 총 자체는 미동도 없어서
 * "UI 가 기다리라고 한다"로 읽혔지, **손이 바빠서 쏠 수 없다**로 읽히지 않았다.
 *
 * 총을 내려 몸 쪽으로 당기고 탄창이 보이게 눕혔다가 제자리로 올린다.
 * 총구가 화면 밖 아래로 빠지므로 "지금은 못 쏜다"가 조준선 없이도 전달된다.
 */
export const WEAPON_RELOAD = {
  dropY: 0.135,      // 아래로 내리는 거리(m). 더 키우면 총이 화면에서 사라진다
  pullZ: 0.055,      // 몸 쪽으로 당긴다
  pitch: 0.52,       // 총구를 아래로 (rad)
  roll: 0.40,        // 탄창 쪽이 보이게 눕힌다
  // 동작 곡선의 뾰족함. 1 이면 정확히 중간에 가장 내려가고, 작을수록 **빨리 내리고
  // 천천히 올라온다** — 실제 재장전은 총을 내리는 게 먼저 끝난다.
  curve: 0.75,
  // 탄창 삽입음 시점(진행률)과 재생속도. 음원이 하나뿐이라 **같은 소리를 두 번,
  // 다른 속도로** 쓴다 (빼는 소리는 낮게, 꽂는 소리는 높고 짧게).
  outRate: 0.92, outVolume: 0.7,
  inAt: 0.62, inRate: 1.28, inVolume: 0.62,
};


/**
 * GLB 무기 뷰모델의 배치. 모델마다 축·원점이 달라서 값으로 맞춘다.
 * (Kenney CC0 킷은 Y-up · 원점이 바닥이라 눕히고 앞으로 밀어야 한다)
 */
export const WEAPON_VIEW = {
  // Kenney 모델은 원본이 작다 (도끼 0.267m). 실물 크기로 키워야 한다 —
  // 안 그러면 손에 장난감을 든 것처럼 보인다.
  //   rot X = -90°  : 원본이 Y 로 서 있으므로 눕혀서 -Z(정면)로 향하게
  axe:     { scale: 2.55, rot: [-Math.PI / 2, 0.28, 0.10], pos: [0, 0.02, -0.02] },

  // Sketchfab 3종 — 이미 실물 크기(m)라 scale 은 1 이다. 원점은 물체 중심.
  //   pipe·crowbar : 원본이 Y 로 서 있다 → X -90° 로 눕혀 -Z(정면)를 향하게
  //   pistol       : 원본이 X 로 누워 있다 → Y 로 돌려 총구를 -Z 로
  // colorMul 은 개별로 준다 — Kenney(민무늬 회색)와 달리 PBR 텍스처가 있어서
  // 0.12 로 누르면 새까매진다.
  pipe:    { scale: 1.00, rot: [-Math.PI / 2, 0.34, 0.14], pos: [0.02, -0.02, -0.06], colorMul: 0.60 },
  crowbar: { scale: 1.00, rot: [-Math.PI / 2, 0.36, 0.12], pos: [0.02, -0.02, -0.10], colorMul: 0.60 },
  pistol:  { scale: 1.00, rot: [0, -Math.PI / 2, 0.04], pos: [0, 0.05, -0.04], colorMul: 0.55 },
  // GLB 재질은 밝은 회색(c0c0c0). 손전등이 0.45m 앞에서 26cd 라
  // 알베도 0.09 정도로도 클리핑된다. 화면 보고 이 값만 조절하면 된다.
  colorMul: 0.12,
};


/**
 * 총구 화염 (weapons/WeaponSystem.js).
 * 칠흑 속에서 총을 쏘면 한순간 주변이 전부 드러나야 한다. 이게 없으면
 * 소리만 나고 화면은 그대로라 "쐈다"는 감각이 안 산다.
 * 그림자는 만들지 않는다 — 그림자 광원은 손전등뿐이다 (CLAUDE.md §3).
 */
export const MUZZLE = {
  color: 0xffd9a0,
  intensity: 420,     // 손전등(26)보다 훨씬 세다. 한 프레임짜리라 눈이 안 아프다
  range: 16,
  duration: 0.065,    // 초. 길면 손전등이 하나 더 생긴 것처럼 보인다
};


/**
 * 피격 시 튀는 피 (fx/Impact.js).
 * 소리와 애니메이션만으로는 "닿았다"가 약하다. 맞은 자리에서 뭔가 튀어야 한다.
 * 입자를 미리 만들어 돌려쓴다 — 드로우콜 1개.
 */
export const IMPACT = {
  // 260 → 340. 헤드샷(25개) + 벽 먼지가 겹치면 링버퍼가 앞부분을 덮어써서
  // **아직 살아 있는 피가 순간 사라진다.** 입자 수는 드로우콜에 영향이 없다.
  count: 340,
  perHit: 14,        // 한 대당 튀는 입자 수
  color: 0x7c1512,   // 어두운 적갈색. 밝으면 페인트처럼 보인다
  // 헤드샷은 더 밝고 크게 튄다. 지금까지 헤드샷의 시각적 보상이 "입자가 조금 더
  // 많다"뿐이었다 — 데미지가 2.2배인데 화면이 그걸 말하지 않았다.
  // 0xd0 이상으로 올리면 위 주석대로 페인트처럼 보이므로 어두운 적색 범위를 지킨다.
  // 0xb8261c · 1.9배는 **너무 갔다.** 실제로 찍어 보니 CLAUDE.md 가 경고한
  // "밝으면 페인트처럼 보인다" 그 자체였다 — 어두운 복도에 빨간 물방울이 떠 있었다
  // (증거: scratchpad/qa/s3_04_headshot_burst.png). 색은 한 단계 낮추고,
  // 크기는 "조금 더 크다"가 알아볼 정도까지만 남긴다.
  colorHot: 0x93201a,
  headSizeMul: 1.28,
  headUpMul: 1.5,
  headSpreadMul: 1.4,
  speed: 2.6,        // 맞은 방향으로 날아가는 속도(m/s)
  spread: 2.2,       // 좌우로 흩어지는 정도
  up: 2.4,           // 위로 튀는 속도. 옆으로만 튀면 물총처럼 보인다
  gravity: 11,
  life: 0.55,        // 초. 길면 공중에 오래 떠 있어 가짜로 보인다
  sizeMin: 0.8, sizeMax: 2.1,
  pixelScale: 26,    // 화면상 크기 배수 (거리로 나눈다)
  // 입자의 **속이 꽉 찬 부분**의 반지름(0~0.5). 0.2 였을 때는 지름의 40% 가 통짜라
  // 가까이서 보면 원반이 떠 있는 것처럼 보였다. 0 에 가깝게 두면 가장자리부터
  // 끝까지 부드럽게 빠져서 뿌려진 것처럼 읽힌다.
  coreRadius: 0.04,

  /**
   * 바닥에 남는 혈흔. 지금까지 좀비를 다섯 발 맞혀도 **죽기 전까지 바닥은
   * 처음과 똑같이 깨끗했다** — 핏자국은 사망 이벤트에서만 생겼다.
   * "여기서 싸웠다"가 지도처럼 읽히게 하려던 의도가 사망에만 걸려 있었다.
   * 입자가 바닥에 닿는 첫 프레임에 확률로 찍는다. 인스턴싱이라 드로우콜은 1이다.
   */
  decalCount: 64,       // 링버퍼 크기. 다 차면 가장 오래된 것부터 재사용한다
  decalChance: 0.22,    // 바닥에 닿은 입자가 자국을 남길 확률. 1이면 온통 피바다가 된다
  decalSizeMin: 0.10, decalSizeMax: 0.26,
  // 사망 핏자국(0.006)·서류 잔해(0.0012)보다 위로 띄운다. 겹치면 z-파이팅이 난다
  decalY: 0.007,
  decalFade: 12,        // 이 시간(초)에 걸쳐 옅어진다
  // 입자가 decalY 위 이만큼까지 내려오면 "바닥에 닿았다"로 치고 멈춘다.
  // **판정과 정지 위치가 같은 값이어야 한다** — 코드에 따로 쓰면 어긋난다.
  decalLandY: 0.013,
};


/**
 * 총알이 **벽에 맞았을 때** (weapons/WeaponSystem.js · fx/Impact.js).
 *
 * 지금까지 빗맞히면 총구 화염 0.065초 말고는 화면에 아무 일도 안 일어났다.
 * 칠흑 속에서 조준이 맞았는지 틀렸는지 판단할 단서가 소리 하나도 없었다.
 */
export const WALL_IMPACT = {
  // 천장 높이(m). world/StageLoader.js 의 WALL_H 와 같은 값이어야 한다 —
  // 레이가 천장을 뚫고 나가면 탄착점이 영영 안 잡힌다.
  ceilingHeight: 3.1,
  // 레이를 전진시키는 간격(m). **벽 두께(0.2m)보다 작아야 한다** —
  // 크면 벽을 통째로 건너뛴다 (Collision.segmentBlocked 이 이미 기록한 함정).
  step: 0.12,
  // 권총 사거리(60m)를 그대로 쓰면 500회를 훑는다. 그 너머 탄흔은 어차피 안 보인다
  maxRange: 18,
  dustCount: 7,
  dustColor: 0x9a958b,   // 회색 콘크리트 가루. 피와 확실히 달라야 한다
  dustSpeed: 1.6, dustUp: 0.9, dustLife: 0.35,
  dustSide: 1.4,         // 법선 말고 좌우로도 흩어지는 폭. 0 이면 한 방향으로만 뿜는다
  dustSizeMin: 0.6, dustSizeMax: 1.4,
  // 소리는 기존 둔기 타격음을 빠르게 돌려 쓴다. 재생속도를 올리면 짧고 딱딱한
  // '틱' 이 되어 콘크리트 파편음 대역에 들어온다. 1.6 을 넘기면 만화 소리가 된다.
  sfxVolume: 0.34, sfxRate: 1.6,
};


/**
 * 피격 반응 (enemies/Zombie.js). 애니메이션만으로는 "닿았다"가 안 느껴진다.
 * 좌표가 아니라 **보이는 위치만** 민다 — 실제로 밀면 벽을 뚫거나 경로가 꼬인다.
 */
export const KNOCK = {
  // 2026-08-07: 0.34 → 0.18 로 줄였다. 맞을 때마다 몸이 훌쩍 밀리니 무게가 없어 보이고,
  // 여러 발을 맞히면 좀비가 뒤로 떠내려간다. 타격감은 히트스톱·화면흔들림·피가 만든다 —
  // **밀림은 거들 뿐인데 키워 놓으면 그것만 눈에 띈다.**
  distance: 0.18,   // 최대 밀림(m)
  duration: 0.20,   // 초. 스턴이 클수록 길어진다

  /**
   * ── 젖힘을 group 에서 **척추 뼈**로 옮겼다 (2026-08-07 저녁) ──
   *
   * 예전에는 `group.rotation.x/z` 를 돌렸다. group 의 원점이 **발 높이**라 몸이
   * 통째로 기울어서, 널빤지가 넘어가는 것처럼 보이는 데다 발이 바닥을 뚫었다
   * (sin(0.26)x0.19m ≈ 5cm — PROGRESS.md 에 함정으로 기록돼 있다). 그 대응으로
   * 각도를 0.26 → 0.15rad(8.6°)까지 줄여 놓았는데, 정작 hit 클립 자체는 63~108°
   * 를 움직인다. **연출로 더한 반동이 클립의 1/10 인데 그 대가로 뚫림을 떠안고 있었다.**
   *
   * 사람이 맞으면 발은 그 자리에 있고 상체만 꺾인다. 척추 체인에 얹으면
   *   - 발이 안 움직이므로 뚫림이 원천적으로 사라지고
   *   - 각도를 오히려 2배로 키울 수 있다
   * mixer 가 매 프레임 뼈를 덮어쓰므로 그 **뒤에** 곱한다 → 누적되지 않고,
   * 플린치가 끝나면 저절로 원래대로 돌아온다.
   */
  boneBend: 0.34,   // 상체 반동 총량(rad). 아래 가중치로 나눠 걸린다
  // 뼈별 분배. 합이 1.0 이다. 아래(Spine)는 조금, 가슴(Spine2)이 가장 크고, 머리가 따라온다.
  // 한 뼈에 몰아주면 목이 꺾인 인형처럼 보인다 — 체인 전체로 흘려야 사람으로 읽힌다.
  boneWeights: { Spine: 0.16, Spine1: 0.24, Spine2: 0.28, Neck: 0.16, Head: 0.16 },
  // 몸통 전체가 밀리는 최소량. 0 으로 두면 하체가 통째로 뻣뻣해 보인다.
  // 이 값이 곧 발이 바닥을 파고들 수 있는 최대치이므로 작게 유지할 것.
  groupBend: 0.03,

  /**
   * 임팩트 곡선 — **감쇠 진동**이다.
   * 예전 식(`sin(t*PI/2)`)은 첫 프레임이 최대이고 그 뒤로는 줄기만 했다.
   * 실제 타격은 (a) 아주 짧은 상승 (b) 최대 (c) 반대쪽으로 살짝 오버슈트 (d) 수렴 이다.
   * (c) 가 없으면 "꺾였다"가 아니라 "기울었다"로 보인다 — 역동감이 여기서 나온다.
   */
  punchIn: 0.05,    // 최대까지 올라가는 데 걸리는 비율. 0 이면 예전과 같은 팝, 0.15 면 물렁하다
  punchDamp: 5.5,   // 감쇠. 크면 한 번 꺾이고 끝, 작으면 계속 흔들려 경련처럼 보인다
  punchFreq: 9.0,   // 진동수(rad). 12 를 넘기면 0.42초 창에 3회가 들어가 경련이 된다

  /**
   * 데미지가 반응 **크기**에 들어간다 (시간이 아니다 — 시간을 늘리면 AI 가 멈춘 것처럼 보인다).
   * 예전에는 권총 1발과 도끼 한 방이 픽셀 단위로 똑같이 움찔했다.
   */
  powerRef: 34,     // 이 데미지에서 배율 1.0 (권총 1발 기준)
  powerMin: 0.55,   // 약한 타격도 최소한 이만큼은 움찔한다
  powerMax: 2.2,    // 상한. 없으면 상체가 90° 꺾여 리그가 찢어져 보인다
};


/**
 * 카메라 흔들림 · 히트스톱 (player/Player.js · core/Game.js).
 * 넉백·핏자국은 **맞는 쪽**의 반응이고, 이건 **때리는 쪽·맞는 쪽 모두의 손맛**이다.
 * 이게 없으면 총을 쏘든 파이프로 치든 화면이 가만히 있어서 전부 물렁하게 느껴진다.
 */
export const SHAKE = {
  // trauma 를 더하고 제곱해서 쓴다 — 약할 때는 티가 안 나고 셀 때 확 온다
  gunshot: 0.40,
  melee: 0.30,       // 휘두를 때가 아니라 **닿았을 때**만
  hurt: 0.62,
  decay: 2.8,        // 초당 trauma 감소. 크면 툭 끊기고, 작으면 계속 흔들려 멀미가 난다
  maxPos: 0.05,      // 위치 흔들림 최대(m)
  maxAngle: 0.032,   // 회전 흔들림 최대(rad)
  freq: 24,          // 흔들림 주파수(Hz). 낮으면 배 멀미, 높으면 지직거림
  hitStop: 0.055,    // 근접이 닿는 순간 멈추는 시간(초). 0.1 을 넘기면 렉으로 느껴진다

  /**
   * 히트스톱 동안의 시간 배율. **0 이면 완전 정지(예전 동작)** 다.
   *
   * 완전히 멈추면 "묵직하게 걸렸다"가 아니라 "한 프레임 씹혔다"로 읽힌다.
   * 0.12 면 좀비 애니메이션과 핏방울이 아주 느리게 흐르고 화면흔들림도 살아 있어서
   * 부딪힌 것이 눈에 보인다. 값 하나로 두 안을 화면에서 비교할 수 있다.
   *   주의: 쿨다운·재장전도 같이 느려진다. 타격당 0.048초라 연타 리듬에 미치는 영향은 미미하다.
   */
  hitStopScale: 0.12,
  hitStopWall: 0.035,  // 벽·소품을 쳤을 때. 살을 칠 때보다 짧다 — 걸리되 파고들지는 않는다

  headshot: 0.20,    // 헤드샷이 **닿았을 때만** 추가로. 빗나간 총알에는 안 온다
  meleeWall: 0.18,   // 근접이 벽을 쳤을 때. 살(0.30)보다 약하되 확실히 느껴져야 한다

  /**
   * 총을 쏘면 조준점이 **실제로 위로 튄다.** 지금까지는 화면이 흔들리기만 하고
   * 조준점은 제자리라, 반동이 "연출"이지 "대가"가 아니었다.
   * 되돌아오는 것이 핵심이다 — 안 돌아오면 연사할수록 천장을 보게 된다.
   */
  gunKickPitch: 0.028,   // 한 발당 젖혀지는 각도(rad). 0.05 를 넘기면 조준을 못 한다
  gunKickYaw: 0.008,     // 좌우로도 조금. 매번 같은 쪽이면 기계로 보인다
  gunKickRecover: 11,    // 초당 되돌아오는 속도. 크면 튀자마자 제자리라 티가 안 난다
};
